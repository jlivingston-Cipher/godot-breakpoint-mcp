# Breakpoint MCP — the queue

> 🔴 **THIS FILE EXISTS BECAUSE THE QUEUE WAS THE ONE TABLE IN THIS REPOSITORY NOTHING
> READ.** Every counter a handoff prints is bound to the instrument that printed it and
> compared to the digit; `handoff_gate.py` refuses a status block over a single atom that
> does not bind. The section of the same document that says *what should happen next* was
> prose, in a file `.gitignore` keeps out of the tree, and no gate has ever opened it.
>
> The consequence is measurable and it was measured before this file was written. Across
> sessions 220–239 the Open section held **20–25 items and never drained**: generation and
> closure were equal, and what got closed was always the newest. Every session closed the
> two items the previous session had created and carried the rest. Measured ages at 239:
> the review charter's P0 had been open **34 sessions**, `D1b` 33, the
> `declared-outside-this-file` five 25, `prepack` 24. The typed ages disagreed with each
> other about their own convention — one row counted from the session that opened it,
> another from the session after — which is exactly the drift a reader catches and prose
> does not.
>
> **So the ages are not written here.** There is no column to type one into.
> `queue_gate.py` derives every age from `opened` and `QUEUE_HEAD`, and the strongest pin
> available on a number nobody rechecks turned out to be deleting the place it could be
> written.

<!-- QUEUE_FORMAT 1 -->
<!-- QUEUE_HEAD 309 -->

## The table

`state` is one of `OPEN`, `SCHEDULED`, `DONE`, `KILLED`. `—` means not applicable.

`reach` is one of `user`, `internal`, and it is the **priority order, enforced**.

* `user` — an installer or operator of the shipped package can observe it: `host/src` and
  addon behaviour, CLI and error copy, packaging, `README.md`, `docs/USER_GUIDE.md`.
  **Subject to the ceiling and to the schedule check.**
* `internal` — everything else: gates, instruments, the handoff apparatus, CI,
  contributor tooling. Real work, none of it reaching a customer. **Deferred, with no
  clock** — no ceiling, no targets — because `QUEUE_TIER_ORDER` already forbids anyone to
  finish one while a `user` row is live, and a ceiling over work nobody is permitted to
  touch demands a decision every eight sessions about nothing. That is churn wearing
  governance.

`paths` is the **evidence for the `reach` cell beside it** — the tracked paths the row's
work touches, `;`-separated, `—` if undeclared. Added at 271.

🔴 **THE TIER WAS A PATH PREDICATE THAT EXISTED ONLY AS THE SENTENCE ABOVE.** 248 defined
`user` by the files a row's work touches, and then asked every session to apply that
sentence by hand and type the answer into a cell nothing read. 267 and 268 re-tiered five
rows `internal` → `user` between them and never once the other way; 268 read that as a
typing bias, 269 found that `QUEUE_TIER_ORDER` had made `user` the only answer that let a
session close its work. **Both readings were unfalsifiable, because there was nothing to
check the column against.** Now there is: `queue_gate.py` carries `REACH_PATHS`, an
ordered roster tiering each path with the reason it lands where it does; it derives the
row's tier from the row's own paths and refuses a disagreement. **Any path a user can
observe makes the row `user`** — one file an installer can see is enough — and every
declared path must be one this repository actually tracks, which is what makes this a join
to the tree rather than a second sentence agreeing with the first.

The column turns on at 271 rather than over the back catalogue: retro-fitting it to
ninety-six historical rows would be ninety-six guesses about what long-finished sessions
were touching, typed by somebody who was not there. A row opened or closed at 271 or later
must declare its paths; older rows carry `—` and are **counted, not excused** — the live
undeclared count is ceilinged at what it was when the column landed, so it can only fall.

🔴 **THE COLUMN WAS ADDED AT 248 AGAINST A MEASUREMENT, NOT A HUNCH.** Of the sixteen
live rows at 247, **zero** were observable by anyone who installs this package. Twenty-five
commits had landed since the `v1.74.0` tag — 15,664 insertions — and **exactly one touched
a file a user can reach.** The apparatus measuring this product had reached 36,987 lines
against the product's 20,885: **1.77×**.

That is not a queue whose priorities slipped. It is a queue with **no intake**. Every row
in it was generated by a session reading the previous session's output, because nothing
else was feeding it: 0 open issues and 0 open pull requests against ~3,700 downloads. A
loop with one input generates rows about itself, and it will do so indefinitely without
ever being wrong about anything.

🔴 **SO THE COLUMN IS HALF THE FIX AND THE SMALLER HALF.** Sorting an empty tier does
nothing. The rows that opened Tier 1 at 248 came from installing the published tarball and
running it the way `docs/USER_GUIDE.md` says to — which had never been done, and which
found four user-facing defects in the first few minutes. **An empty Tier 1 is a reading to
distrust before it is a reading to act on.**

* `OPEN` — no decision has been taken. **Subject to the ceiling**: `queue_gate.py` refuses
  an `OPEN` row older than `AGE_CEILING` sessions. The ceiling does not demand the work;
  it demands a decision, because an item nobody has picked up in twenty-five sessions is a
  decision not to do it that nobody wrote down.
* `SCHEDULED` — a `target` session is required, and **the target is checked**. A
  `SCHEDULED` row whose target has passed without becoming `DONE` or `KILLED` is refused.
  Without that half, `SCHEDULED` is just `OPEN` with the ceiling laundered off it.
* `DONE` / `KILLED` — a `closed` session is required, and `KILLED` requires a `why`.
  Closed rows **stay in the table**; they are the only record that the decision was taken,
  and `QUEUE_ROW_FLOOR` refuses a table that lost rows.

| id | state | reach | paths | opened | closed | target | title | why |
|---|---|---|---|---|---|---|---|---|
| host-failures-carry-no-remedy | DONE | user | — | 264 | 267 | — | Seventeen host-raised failures say nothing while holding something that would name a cause 🟢 **CLOSED AT 267 — the five that remained were never unanswerable, only unhousable.** With the field on both classes, `host/src/timeout-cause.ts` answers the four request deadlines (one sentence, four knobs, each naming the peer its own late-reply ledger names) and `CS_START_REMEDY` answers the C# session start. **Eleven sites, not five**: the four `closed` handlers and the two restart refusals were among 264's SEVEN that named an action inside their message, and they moved to the field in the same pass — rendered output asserted **byte-identical**, because a change of channel that is also a change of wording is two changes and only one of them was decided | 🟢 Closed on the whole population rather than the five the handoff named: the seventeen and the seven are both empty on these two classes. `ComposeError`'s twenty-four bad-input refusals were set aside by 264's own census and are not this row |
| dap-initialized-wait-is-a-timeout | DONE | user | — | 266 | 267 | — | The wait for the adapter's `initialized` event RESOLVES on timeout, so a slow adapter gets its breakpoints applied out of order after five silent seconds 🟢 **CLOSED AT 267. REPORTED, NOT REFUSED, AND THE CHOICE IS WRITTEN DOWN.** `waitEvent` on both planes now resolves `true` for the event and `false` for the timer — the old `Promise<void>` is why the defect could not be seen, since the failing and succeeding cases returned the identical value. `dbg_launch`, `dbg_attach`, `cs_dbg_launch` and `cs_dbg_attach` answer `initialized_seen` ALWAYS (a key present only on the bad path cannot be branched on — it cannot separate *this adapter was conformant* from *this host is too old to say*), plus a warning naming the window actually waited and the symptom to expect. Refusing outright would have broken every adapter in the field to fix a silence; the shape chosen is the one `stop_on_entry_honored` already ships on the same tool | 🟢 Closed as a report. **A stricter refusal is still available and is a separate decision** — the measurement, the stub harness and the false-direction test all survive, so re-deciding it costs the decision and not the work. Reopen under a new row rather than this one |
| process-output-omits-signal | DONE | user | — | 266 | 267 | — | `godot_process_output` answers `exit_code: null` for a signal-killed child and has no key for the signal that killed it 🟢 **CLOSED AT 267.** The key is `signal`, `z.string().nullable()` beside `exit_code` — nullable rather than optional, because a key that vanishes cannot be told apart from a host too old to send it. Asserted as a PAIR against real children (`SIGTERM` vs a clean `exit 0`) and the pair asserted to DIFFER, which is the assertion a constant `null` defeats. 🔴 **AND THE ROW NAMED A TOOL THAT DOES NOT EXIST**: the tool is `godot_output`; `godot_process_output` appears in this row, in three handoffs and in two source comments, and `registerTool` has never been given that name. Check 28c joins backticked tool names inside REMEDIES to the registry — nothing joins the ones in this table | 🟢 Closed on the wire, with the catalog and the output schema moved in the same cut. The name defect is recorded here rather than opened as a row: the four wrong spellings are corrected in this cut, and whether QUEUE.md's own prose should be joined to the tool registry the way a remedy is, is a question for `queue-claims-unread`'s successor |
| dev-tree-lags-its-own-range | DONE | user | — | 267 | 268 | — | `package-lock.json` pins the MCP SDK at 1.29.0 while `package.json` declares `^1.29.0`, so this repo's own tree carries four advisories that no consumer receives 🟢 **CLOSED AT 268, AND THE ROW'S STATED MECHANISM WAS WRONG.** It read *installing the SDK at 1.30.0 directly clears them at source too*. Driven: `npm update @modelcontextprotocol/sdk` moved the lock to **1.30.0** and **all four advisories survived** — they are transitive pins beneath the SDK, not the SDK. `npm audit fix --omit=dev` on top cleared them, `package.json` byte-identical, lockfile-only, and the suite came back 886/886 on the new resolution. 🟢 **THE CONSUMER CLAIM WAS RIGHT AND IS NOW DRIVEN RATHER THAN REASONED**: a fresh `npm install breakpoint-mcp@1.79.0` resolves `hono` 4.13.3, `@hono/node-server` 2.1.1, `fast-uri` 3.1.5 and `ip-address` 10.5.0 and audits **0 vulnerabilities** — clean because there is no lockfile in the tarball, which is what the row's first half said, and not because of the SDK version, which is what its second half said. 267's postscript corrected one reading of this row by driving it; 268 corrected the correction the same way 🔴 **RE-TIERED `internal` → `user` AT 268, AGAINST ITS OWN ARGUMENT, AND THE ARGUMENT WAS ABOUT THE WRONG HALF.** It reasoned from the ADVISORIES: the lockfile is not in the tarball, so the four advisories reach nobody — true, and re-driven at 268. But the row's own title is the other half: the repo built, tested and gated against SDK **1.29.0** while every consumer resolved **1.30.0**. The host was being proved against a version no user runs, and that difference is user-facing whichever way the proof comes out. | 🔴 **FOUND AT 267 WHILE VERIFYING A PUBLISH, AND THE FIRST READING OF IT WAS WRONG.** `npm audit` on a clean clone at `v1.79.0` reports FOUR advisories that survive `--omit=dev` — two moderate, two high — every one arriving through the MCP SDK: the Hono server adapter and `hono` beneath it, `fast-uri` under `ajv`, and `ip-address` under `express-rate-limit`. Reading the tree, that looks like a user-facing exposure. **It is not, and driving it is what said so:** installing the PUBLISHED package fresh resolves the SDK to **1.30.0** — the declared `^1.29.0` floats up, and the lockfile is not in the tarball — and the consumer audit reports **0 vulnerabilities**. The lockfile governs this repo's dev and CI trees and nothing else, which is why the row is `internal` rather than `user`. 🟢 **THE REPAIR IS MEASURED AND IT IS ONE LINE OF LOCKFILE.** `npm audit fix --omit=dev` at the tag exits 0, leaves `package.json` UNCHANGED, moves four transitive pins (adapter 1.19.14 -> 1.19.17, `fast-uri` 3.1.3 -> 3.1.5, `hono` 4.12.27 -> 4.13.3, `ip-address` 10.2.0 -> 10.5.0) and clears all four. Installing the SDK at 1.30.0 directly clears them at source too, so refreshing the pin is the same fix by the other route. 🔴 **DO NOT RUN IT DURING A PUBLISH.** 264 shipped a tarball from a tree that an `audit fix` had dirtied between the porcelain check and the upload; since the lockfile reaches no user, doing it there buys nothing and costs the one guarantee that step exists to give. 🔵 **AND IT IS ENTANGLED WITH `sdk-v2-migration`,** whose standing instruction is to re-read the registry: `dist-tags` is `latest: 1.30.0`, 79 published versions, still none in 2.x. Refreshing the pin is the cheap half of that row's re-read and does not commit to the rest |
| set-property-reports-a-write-that-did-not-happen | DONE | user | — | 270 | 270 | — | `runtime_set_property` and `node_set_property` answered with a fresh read-back they never compared to the request, so every way a write can fail to land reported success carrying the OLD value | 🔴 **FILED BY A USER AS ISSUE #327 AND THE REPORTER'S DIAGNOSIS WAS WRONG IN A WAY WORTH KEEPING.** They read *vectors dropped, falsy scalars dropped, indexed paths null* as one truthiness guard in the codec's decode path. Measured at 270 live on Godot 4.7, driving BOTH SIDES OF THE WIRE SEPARATELY — a raw socket to isolate the addon, the host's own tool handlers to isolate the host — real `false`, `0` and `0.0` round-trip correctly and always have, and that path had not changed since the 1.78.3 they were running. What they actually hit was a String reaching a bool property: Godot coerces any non-empty String to `true`, so `"false"` reads back as `true`. 🔴 **SIX CAUSES, ONE UMBRELLA.** The umbrella is that the tool could not tell a write that landed from one that did not. Underneath it: the String coercion; an untagged `{"x":..,"y":..}` map storing (0,0) over a real position; untagged array and string forms as silent no-ops; `get`/`set` where the colon form needs `get_indexed`/`set_indexed`; and `inject_input`'s `if pos is Vector2:` with no else, which injected mouse events at the origin and answered `{"injected": true}`. 🔴 **AND THE HALF NOBODY REPORTED WAS THE WORST.** `z.any()` is optional in zod, so `value` was published OUTSIDE the JSON Schema's `required` list on seven shipped tools whose own descriptions call it *New value* — a client omitting it was obeying our schema, and the addon then wrote the property type's ZERO over whatever was there. Measured: `rotation` 1.25 → 0.0, `position` (123, 456) → (0, 0), success reported both times. 🟢 **CLOSED AT 270 BY A COMPARISON RATHER THAN A GUARD**, which is the point: a guard has to enumerate the ways a write can fail and a comparison asks the only question that matters. `set_ignored` and `set_mismatch` are errors, a refused write puts the property back, and `coerced`/`requested` mark a write the engine landed and then changed so a clamp is not confused with a failure |
| any-typed-inputs-optional-by-accident | DONE | user | — | 270 | 270 | — | Nothing read the INPUT side of the tool surface for keys published as both unconstrained and optional — the direction check 29 had never taken | 🔴 **THE SET DIFFERENCE NOBODY HAD TAKEN, AND #327's WORST HALF LIVED IN IT.** `schemas.test.ts` has read the OUTPUT schemas' unconstrained-**required** keys since 255, in both directions, and never asked the same question of the inputs. Seven `value` keys were optional on the wire and required in their own prose. 🟢 **CLOSED AT 270 WITH A ROSTER RATHER THAN A BAN**, because an unconstrained optional input is a legitimate thing to publish — `editorsettings_get_set`'s `value` means *if provided, set it* and `runtime_inject_input`'s `position` means *wherever it lands by default*. Those three are declared; anything else is refused. 🔴 **AND THE FIRST DRAFT OF THE DETECTOR WAS A TAUTOLOGY.** It asked `Object.keys(sub).length === 0`, which is correct for output schemas because they carry no prose — every input key carries a `.describe()`, so `z.any().describe("New value")` compiles to a subschema of key count ONE. It would have found nothing, passed on day one, and been blind to all seven of the keys it was written for. It measures CONSTRAINT now, not emptiness |
| block-claims-a-count-its-own-read-could-not-make | DONE | internal | scripts/handoff_gate.py | 270 | 271 | — | A header atom the `--open` run could not READ can still be typed into the block, and `gh.issues` was carried as `0` while a user's bug report sat open | 🔴 **MEASURED AT 270 AND IT COST A DAY OF A USER'S TIME.** Issue #327 was opened at `2026-08-18T18:44:45Z`. 269's status block, written after that, says `0 open issues / 0 open PRs` — and 269 shipped a release without anyone seeing a three-symptom report against a shipped tool. 🔴 **THE GATE IS NOT WRONG, IT IS UNHEARD.** `handoff_gate.py --open` says exactly the right thing about this atom: *UNREAD — `gh` is not installed for this run — the tool never ran, which is not the same observation as zero open items.* It prints that and then accepts a block asserting a number anyway. Three of 269's four header atoms were re-read against the world; these two could not be, and nothing distinguishes *re-read and found to be zero* from *not read and carried forward* once the digit is in the document. 🔵 The repair is one comparison and it is the same shape as every other atom here: an atom whose reader answered UNREAD may not be CLAIMED. Either the block omits the count, or the run that checks it has `gh` — and which of those two is right is the decision this row carries, because forcing `gh` into every checking environment is a real cost and omitting a count a maintainer wants to see is a real loss. 🔴 **AND THE GENERAL FORM IS 269's OWN FINDING POINTED AT THE RITUAL**: 269 closed `handoff-block-ungated-by-its-own-ritual` on the argument that the document every session stands on is the one artifact no gate reads. It reads it now — and this is the class of claim it still cannot judge, because the judgement is about what the READER could see rather than about what the block says 🟢 **CLOSED AT 271, AND THE DECISION THE ROW CARRIED WAS TAKEN: NEITHER OF THE TWO IT OFFERED.** Forcing `gh` into every checking environment makes a third-party CLI load-bearing for the handoff gate forever; omitting the count loses a number a maintainer wants and that a real user's report would have surfaced. The third answer is that the REFUSAL is about the CLAIM and not about the environment: an atom whose reader answered UNREAD is now a problem rather than a note, and a block that cannot read the counter writes it as UNREAD and asserts nothing — the spelling 249's own block already used. Underneath it, `gh_open` gained a second route (`api.github.com` over plain HTTPS, no CLI, token optional) so far fewer environments have to decline; `--gh-open` emits the two lines the roster has extracted since 236 and nothing had ever printed; and `--open` accepts `--measured`, which 241 §1 recorded as impossible and which is the only reason a session without a socket can still be handed the reading |
| reach-paths-unjoined-to-the-diff | DONE | internal | scripts/queue_gate.py | 271 | 281 | — | `QUEUE_REACH_JOIN` compares the typed tier to the paths the row DECLARES, and nothing compares those paths to the files the closing commit actually changed | 271 §3 gave `reach` evidence and the evidence is still typed by the same hand that types the tier — a row can declare `scripts/` and land in `host/src/`, and the join agrees with itself. The reader that would answer it is `git diff --name-only` between the session's base and its merge, and it was deliberately NOT built at 271: CI's `actions/checkout` is a `--depth 1` clone, so a history reader answers differently in CI than on the authoring machine, which is 246's lesson and the reason the declared-path form shipped first. The shape available: the close gate already knows the session's base sha (the replay declares it) and already reads the block's main row, so the comparison is affordable at CLOSE time on the authoring machine even where it is not affordable in CI — which makes this a claim about where a check may run, not about whether it can 🟢 **CLOSED AT 281, AND THE PREDICATE IS NOT THE ONE THE ROW SUGGESTED.** *Did the work land where the row said* has an honest NO — a row can close because a MEASUREMENT settled it, and a measurement lands in the reader that took it. `QUEUE_PATHS_JOIN` asks the answerable question instead: a row whose close touched none of its own declared paths did not do the work it was TIERED for, and this table already has one state for that. `KILLED` requires a `why`; `DONE` claims the title happened; 244's rule that only `DONE` answers `QUEUE_NOT_ONLY_NEWEST` is what keeps the two apart. 🔴 **IT REFUSED ITS OWN SESSION ON ITS FIRST RUN** — `sdk-v2-migration` was drafted `DONE` and is `KILLED` because of it, which is the fourth session running that a gate has beaten its author and the second this session. 🔵 The diff is `merge-base(origin/main, HEAD)..HEAD`, with `HEAD~1` substituted on `main` itself — otherwise the post-merge run the close gate measures would read *this session changed nothing* on exactly the commit where it changed everything. `objects` is declared in `GATE_INPUTS`, the job that runs this gate has carried `fetch-depth: 0` since 220, and a diff git cannot produce is `QUEUE_PATHS_UNREAD` — a refusal, never a skip |
| assetlib-claim-has-no-reader | DONE | internal | scripts/handoff_gate.py | 271 | 272 | — | The Asset Library's live version is a world-facing fact asserted in every handoff and read by nothing | 🔴 **MEASURED AT 270 BY READING THE LIVE API, AFTER THREE SESSIONS HAD CARRIED THE OPPOSITE.** Every handoff from 257 said addon 1.11.0 was *in review*; `godotengine.org/asset-library/api/asset/5335` said it had been ACCEPTED and served at `b76ff11` since 2026-08-17. Nobody was careless — the claim was INHERITED, which is exactly the class 271 §1 closed for `gh.issues` and this is the same class one population wider. 🔵 271 made UNREAD refuse a claim, so the missing half is now only a READER: an `assetlib.live` header row dialing `api/asset/5335` for `version_string` and `download_commit`, with the pending-edit endpoint beside it, so a block claiming what the library serves is compared to what it serves and a block that cannot dial says UNREAD. 🔴 The container cannot reach godotengine.org, which is the case the UNREAD half already handles and the reason this is a reader and not a requirement 🟢 **CLOSED AT 272 — one reader, one emitter, one comparison, and it obeys 271's rule at both ends.** `assetlib_live()` dials `api/asset/5335` for `version_string`; `--gh-open` emits `ASSETLIB_VERSION <x.y.z>` beside the two gh counters so the reading is paid for once on whichever machine can make it; `check_header` compares it to the block's own `assetlib` row. 🔴 **UNREACHABLE PRINTS A REASON AND NEVER A VERSION** — the container cannot reach godotengine.org at all, so this is the second atom whose honest answer here is UNREAD, and under 271 a block may not claim it in that environment. 🔴 **THE ROW IS OPTIONAL AND THE CLAIM IS NOT**: `BLOCK_POPULATION` carries forty blocks that predate it, so a missing row is a note, and a row that is present and wrong is a refusal — which is the defect 270 actually hit |
| reach-column-joins-nothing | DONE | internal | scripts/queue_gate.py;QUEUE.md;scripts/floor_pin_gate.py | 268 | 271 | — | `reach` is typed by hand and no gate joins it to the files a row's work actually touches | 🔴 **267 NAMED THIS AND DID NOT OPEN IT; 268 GOT IT WRONG ON FOUR ROWS OUT OF FOUR, ALL IN THE SAME DIRECTION.** 267 re-tiered `remedy-channel-one-class-of-five` `internal` → `user` and wrote: *the tier was wrong from the day it was written and nothing would have caught it, because `reach` is typed by hand and no gate joins it to what the row's work actually touches.* At 268 every one of the four rows closed had to be re-tiered the same way, each for a concrete reason — a sentence two shipped tools return, a shipped error message and its remedy, packaging, and the SDK version the host is proved against. Five corrections in two sessions, never once in the other direction, is a systematic bias and not four mistakes. 🔵 **AND THE COST IS NOT COSMETIC**: `QUEUE_TIER_ORDER` refuses a session that closes an `internal` row while any `user` row is live, so a mis-typed `reach` either blocks a legitimate session or lets one through — 268 was refused by it, correctly, and the refusal was resolved by finding that the column was wrong rather than by arguing with the gate. The join that would answer it exists in outline: a row's work lands in a commit, and `git diff --name-only` against the tier definition's own file list — `host/src`, `addons/`, CLI copy, packaging, `README.md`, `docs/USER_GUIDE.md` — is a reader, not an opinion 🟢 **CLOSED AT 271 AND THE JOIN IS A DECLARED PATH, NOT A DIFF.** The outline this row carried was `git diff --name-only` against the tier definition, and it was rejected for a measured reason: CI's `actions/checkout` is a `--depth 1` clone, so a reader of commit history is a reader that answers differently in CI than on the authoring machine — 246's own lesson about shallow clones. What shipped instead makes the row declare the paths its work touches, in a ninth column, and joins THOSE: `REACH_PATHS` tiers each path with the reason it lands where it does, any path an installer can observe makes the row `user`, and every declared path must be one `git ls-files` returns — so the cell is checked against the tree rather than against another sentence. The back catalogue is grandfathered and COUNTED under a ceiling that can only fall; a row opened or closed at 271 or later must declare |
| write-failed-names-two-producers | DONE | user | — | 268 | 269 | — | `write_failed` is raised by the addon for a file it could not open and by the host for a socket that went away, and the remedy table can only answer one of them 🟢 **CLOSED AT 269 — HE TOOK THE WIRE DECISION AT PICKUP AND THE ANSWER IS A RENAME.** The population was measured before the word was chosen and it was ONE: eleven codes originate in the host, fifty are raised in the addon, and `write_failed` was the entire intersection. A single collision wants the word fixed; a family would have wanted a discriminator field on every error the wire carries, and this is not a family. 🔴 **THE YOUNGER PRODUCER YIELDED**, which is the whole argument. The addon's `write_failed` has crossed this wire since the addon had a wire; the host's branch was believed unreachable until 268 measured it 200/200, and until 1.80.0 it shipped node's own `Cannot call write after a stream was destroyed` — so a caller branching on what THIS site produced was branching on a Node internal. `BRIDGE_SEND_FAILED = "send_failed"` also says the true thing outright: the request was never SENT. 🔴 **AND THE NEXT COLLISION IS A REFUSAL RATHER THAN A DISCOVERY.** Check 32 gained a fourth arm — a code raised on BOTH sides of this wire is refused at `git add`, with `host_origin_error_codes` as the reader and `32.collide` as its control, the mutation being the defect itself put back. Check 23 has compared the addon's vocabulary to the codes TypeScript BRANCHES on since 192, in both directions, and had never asked what the host RAISES: the one set difference that could have caught this was the one nobody had taken | 🔴 **FOUND AT 268 THE MOMENT THE HOST'S BRANCH BECAME REACHABLE.** `operations.gd` raises `write_failed` when `FileAccess` cannot open a path, and `error_remedies.gd` answers it with *Check the target path is inside the project and not read-only; the number is a Godot Error code.* `bridge.ts` raises the same word when a TCP write cannot be handed to the kernel — no path, no file, no Godot error code. While the host's branch was believed unreachable the collision cost nothing; 268 drove it 200/200 and it is live. 🟢 **268 TOOK 262's DISPOSITION AND STOPPED THERE**: the code is kept, and the host site now carries its own message and its own `remedy` FIELD, so what a reader acts on is right even though the word is shared. What is NOT decided is the wire question — whether a caller branching on `write_failed` can be expected to tell a file from a socket, and therefore whether the host's should become a code of its own. It is `user` because the answer changes an error code that crosses the wire, and it is HIS to steer for the same reason 267 §3 was. 🔵 The cheap half is already done and would not be wasted either way: check 32 judges every raise site in `host/src`, so a new code would be joined to its message the day it is written |
| tier-order-counts-unreached-schedule | DONE | internal | — | 269 | 269 | — | `QUEUE_TIER_ORDER` counted a `SCHEDULED` user row as live whatever its target, so one row parked forward since 226 closed the whole internal tier | 🔴 **MEASURED OFF THIS FILE AT HEAD 268, WHICH IS THE ONLY REASON IT WAS FOUND.** From 240 to 247, every closed row was `internal` — twenty-three of them. From 248, the session that made the ordering enforceable, to 268: ZERO out of thirty-nine. Not one internal row closed in twenty sessions, while Tier 2 grew to twenty rows aged eight to forty-five. The cause is one row: `sdk-v2-migration` is `user`, has been SCHEDULED since 226, and re-targets itself every time a session re-reads the registry and finds the 2.x it would migrate to still does not exist — permanently live, and permanently live is permanently blocking. 🔴 **AND THE PRESSURE CAME OUT SOMEWHERE.** 267 and 268 re-tiered five rows `internal` -> `user` between them, and 268 read that as a typing bias in the `reach` column: five corrections, never once the other way. Every one was defensible on its merits AND was the only direction that let the session close its work. A gate that makes one answer free and the other cost the session does not measure the column, it selects it — which is why `reach-column-joins-nothing` was never going to get balanced evidence. 🟢 **CLOSED AT 269 ON HIS INSTRUCTION**, offered at pickup with the measurement: a row scheduled for a session that has not arrived does not block, its schedule is governed by `QUEUE_SCHEDULE_HONOURED` where a claim was written for it, and an `OPEN` user row blocks exactly as before — as does a SCHEDULED one that has come due. What it sets aside is NAMED on every run, because the defect being replaced was silent |
| handoff-block-ungated-by-its-own-ritual | DONE | internal | — | 269 | 269 | — | The status block every session stands on is the one artifact no gate in the ritual reads, and 268's fails five of this module's own claims | 🔴 **FOUND BY RUNNING `--open` AT 269's PICKUP, WHICH REFUSED.** 268's block dropped `ci.checks`, `instrument.not_loaded` and `instrument.late_constructed`; carried ONE header atom against a floor of two, because the npm row lost `lag` and `tags`; claimed `error-code discipline 54 reads / 29 raise sites / 0 problems`, a counter 268 added to its own block and never gave a reader; and abbreviated its replay list to *267's plus one, in full, in the script*, which `REPLAY_MISSING` refuses in thirty-five places and 248 had already written down as a rule. 267's block, run through the same path on the same tree, has NONE of it. 🔴 **THE MECHANISM IS TOTAL AND BORING.** `HANDOFF*.md` is in `.gitignore`, so no CI job can see the document; the replay runs `handoff_gate.py --selftest` and `--patterns`, which prove this gate's predicates on FIXTURES rather than pointing it at the block being written. The close path's real first reader is the NEXT session, at pickup, after the block has been published, acted on, and used as the base for a release. 🟢 **CLOSED AT 269 WITH THE POPULATION THAT WAS ALREADY HERE.** 253's rule copies each block into `BLOCK_POPULATION` one session after it ships, so CI can read it from then on: `population_block_shape()` judges every block from `POPULATION_SHAPE_FROM` by the rules a live block is judged by, with a positive control that a block claiming one unbound atom is refused on all three arms. The boundary is MEASURED — over all forty-one blocks the reader's only complaints were dropped counters on 227-232, against a roster that has grown since, and 233 through 267 are clean on every claim, thirty-five in a row. The guarantee is ONE SESSION OF LAG and it is stated rather than papered over |
| dap-timeout-predicate-reads-prose | DONE | user | — | 267 | 268 | — | `isDapTimeout` decides a shipped tool's behaviour by regexing `/timed out after/` against a message body, on both DAP planes 🟢 **CLOSED AT 268, AND THE ROW NAMED TWO OF THE THREE SITES.** `DapError` gained a `code`, typed as a two-member union of the literals `timeout` and `unannounced` rather than as a `string`, so a remedy passed into the code slot is a compile error: the type does the job the comment in `timeout-caveat.ts` was doing. Both `isDapTimeout`s read the field. 🔴 **AND IT WAS NEVER ONLY A HAZARD.** The regex matched the message body of ANY `DapError`, including the relay site that carries the ADAPTER'S own words — so an adapter answering `setVariable` with a failure mentioning its own inner deadline was told by this host that *Godot's GDScript debug adapter does not implement it*, a measured claim about a different build, invented over a reply that had arrived. Three tests drive it on both planes and all three go red against the old predicate. 🔴 **THE THIRD SITE WAS NOT IN THE ROW AND HAD A HUNDRED TIMES THE BLAST RADIUS**: `timeout-caveat.ts` decided whether every mutating tool warns that a timed-out change MAY ALREADY HAVE LANDED by matching the literal `Bridge error [timeout]`, spelled in that file, one file from the template that builds it. It is derived from `bridgeErrorLabel` now — controlled by rewording the label and confirming the derived reader still matches where a hand-copy would not. Check 32 is the gate, in three directions, with four positive controls 🔴 **RE-TIERED `internal` → `user` AT 268, AND THE ROW SAID SO ITSELF.** Its own `why` reads *not user-facing on its own: nothing a caller reads changes until someone edits a message* — and closing it is that someone. `dbg_set_variable` and `dbg_evaluate` now answer differently for any adapter whose failure text mentions a deadline, which is a sentence a caller reads. | 🔴 **FOUND AT 267 WHILE GIVING THE CLASS A FIELD, AND IT IS THE SAME SHAPE AS THE ROW THAT FOUND IT.** the GDScript and C# DAP tool layers each carry `err instanceof DapError && /timed out after/.test(err.message)`, and two live branches hang off it: `dbg_evaluate` and `dbg_set_variable` answer a measured, specific sentence about Godot's unimplemented `setVariable` instead of a bare timeout. So a **copy edit to an error message silently changes which answer a caller gets**, with every test still green — `timeout-caveat.ts` even says out loud that this predicate must not be disturbed, which is a comment doing a type's job. 267 deliberately did NOT rewrite those messages for that reason, and asserted the wording instead. The repair is a discriminator the raise site sets and the tool layer reads — the `command`/`code` fields are already there and a `timeout` code is one line — but it touches two shipped planes' branch selection, so it is a row rather than a fold-in. **Not user-facing on its own**: nothing a caller reads changes until someone edits a message, which is exactly why it is cheap to leave and expensive to hit |
| write-failed-unreachable | DONE | user | — | 265 | 268 | — | `bridge.ts`'s `write_failed` sentence lost every race that could have produced it 🟢 **CLOSED AT 268 BY FINDING THE INPUT, AND 265's CONCLUSION WAS BACKWARDS.** 265 reasoned that `onClose()` always rejects the pending map before a failing write callback lands. Measured at the node level, 300 sockets out of 300: on a destroyed socket the write callback fires with ERR_STREAM_DESTROYED **before** the `close` event. 265's four drives each broke a live socket and then WAITED, which is the one window in which they could not win. The reaching input is anything that destroys the socket inside the await gap `request()` leaves between `await this.connect()` and `socket.write(...)` — `close()` on the statement after an un-awaited `request()` reproduced it **200/200** through the real client. So the branch stays and its sentence was the deliverable: it shipped node's raw `Cannot call write after a stream was destroyed`, and it now says the request was never sent. 🔴 **AND IT TURNED UP A COLLISION**: the ADDON raises `write_failed` too, for a file it could not open, and `error_remedies.gd` answers it with *check the target path is inside the project and not read-only* — nonsense over a socket that went away. 262's disposition applies: the code is kept, the message and remedy are made to fit, and the wire question is enqueued as `write-failed-names-two-producers` 🔴 **RE-TIERED `internal` → `user` AT 268.** Filed internal as a reachability question about a branch; its deliverable is the sentence and the remedy a caller receives when a request is torn down mid-flight, and that crosses the wire on every one. | 🔴 **MEASURED AT 265 BY DRIVING IT, NOT BY READING IT.** The site is in `request()`'s write callback, but a socket whose write fails is also a socket that is closing, and `onClose()` rejects the WHOLE pending map first — so the write callback's `reject` lands on a promise already settled and its `pending.delete` is a no-op. Four ways of breaking a live socket, four times the caller read something else: `destroy()` → `bridge_closed`, `destroy(EPIPE)` → `bridge_closed (write EPIPE)`, `end()` then write → `bridge_closed`. **`write_failed` reached nobody in any of them.** 259's rule with a fresh instance: a name that resolves is not an effect that is reachable — and it means the right answer for this site is not a better sentence, which is why it is a row of its own rather than the last item on `host-failures-carry-no-remedy`. Four drives are not a proof of unreachability; the work is to find the input that DOES reach it, or to say out loud that none does and delete the branch |
| peer-client-editor-knobs | DONE | user | — | 265 | 265 | — | A peer's `BridgeClient` was constructed with the editor bridge's three per-instance knobs | 🔴 `deadlineKnob`, `peerNoun` and `hostKnob` all default to the editor's, and `peers.ts` took all three while dialling `cfg.runtimeHost` on `cfg.runtimeTimeoutMs`. So a peer's late-reply line named `BREAKPOINT_BRIDGE_TIMEOUT_MS` — a knob that cannot move the deadline it just hit — and called the thing that failed to answer *the editor*. That is the exact defect the `deadlineKnob` parameter was added at 257 to prevent, at the one call site nobody passed it at, and nothing had ever looked because a default that compiles asks no question. Found only because 265's connect remedy made the noun load-bearing. Closed the session it was opened, which is what a one-line defect found by a change to its neighbour looks like; asserted on the CONSTRUCTION rather than on a rendered sentence, so a reword cannot green it |
| remedy-channel-one-class-of-five | DONE | user | — | 264 | 267 | — | `remedy` is a field on one error class of five, and the gate that guards it is scoped to the class that already has it 🟢 **CLOSED AT 267, AND THE GATE HALF WAS THE EASIER HALF.** `DapError` and `LspError` each gained an optional `remedy`, spelled exactly as `BridgeError`'s: assigned only when non-empty, absent meaning *nobody has answered this site yet*. The four renderers that were outside the population — the two DAP tool layers and both renderers in the shared LSP tool module — now append `remedyClause(err)`, and `remedy_renderers_read`'s `Partial<BridgeError>` condition is gone: the predicate is the `fail(err: unknown)` signature all nine already shared, so a renderer is in scope because it renders. Two new readers were added beside it — a raise-site join deriving *host-invented* from *relays the peer's own words*, and a grammar pass over the host's own remedy sentences derived from the `*Remedy` / `*_REMEDY` convention — each reporting the size of what it scanned, because an empty list means *nothing wrong* and *did not look* identically 🔴 **RE-TIERED `internal` → `user` AT 267, AND THIS SESSION IS THE EVIDENCE.** It was filed internal because it reads as a gate row — the `why` above is mostly about check 28. But the row's DELIVERABLE is a field whose contents cross the wire: closing it changed the text four planes' callers read on every timeout, every dropped connection, every refused C# session and every restart with no launch behind it. 248's line is *reaches a user of the shipped product*, and this reaches one on eleven sites. The tier was wrong from the day it was written and nothing would have caught it, because `reach` is typed by hand and no gate joins it to what the row's work actually touches | 🟢 Closed by widening the gate off a signature rather than adding four names to a roster, and by giving the two classes the field. The grammar half immediately refused two SHIPPED host sentences nothing had ever read — see the `Fixed` half of 1.79.0 |
| publish-tarball-outruns-its-tag | DONE | user | — | 260 | 268 | — | Nothing refuses `npm publish` when HEAD is not the commit of the tag naming the version being published. 🔴 MEASURED AT 260 BY DOING IT: `breakpoint-mcp@1.75.0` went to the registry from `main` at `ca2d5d8` while tag `v1.75.0` is `49ccb19` — four commits and +888/−72 apart, addon 1.11.0 against the tag's 1.10.0, 89 files against the tag tree's 81. npm reads the version out of `host/package.json` and nothing else, and that file reads `1.75.0` at BOTH commits, so the upload was accepted without a complaint from anything. `release_names.py` check 4 asks whether the ADDON version names exactly one tree and refuses a cut when it does not; **nothing asks that question of the HOST version, which is the one npm actually publishes under** — the one place this project's own principle has no reader. Three things that should have caught it did not, and each failed differently: the publish plan's step 0 is `git status --porcelain` plus `git log -1`, both of which read perfectly clean on a HEAD that has moved past its tag; `registry_bytes.py`'s freshness check compares `host/addon/` against `addons/`, both taken from the working tree, so they agree with each other and with nothing outside it; and a hand-written HEAD-against-tag comparison DID fire, printing a refusal, inside a multi-line pasted block — which the shell then ran to completion, because an echo is not an exit. The home is `prepublishOnly`: refuse unless the tag named by `host/package.json`'s version resolves to the same commit as HEAD, with a named env opt-out so a deliberate off-tag publish stays possible and stays visible. 🟢 NO USER ROW IS OWED FROM THE INSTANCE — the artifact over-delivered rather than under-delivered, and the false claim lived only in the release notes, which were corrected the same session. This row is the guard, not the damage. 🔴 **AND 264 FOUND A SECOND, INDEPENDENT WAY IN, ON A PUBLISH THAT WAS OTHERWISE CORRECT.** `1.78.0` went out from a clone checked out AT `v1.78.0`, so the HEAD-against-tag guard this row proposes would have passed it. Between step 0's `git status --porcelain` and `npm publish`, an `npm audit fix` bumped four RUNTIME dependencies in `host/package-lock.json` — the tree was dirty at the moment of upload and nothing asked again. Verified afterwards and **no damage: `dist/` byte-identical to a clean build from the tag, `package.json` unchanged, `package-lock.json` not in the tarball, 89 files** — but the guard as written reads HEAD and not the working tree, and a clean `git status` taken before a step that dirties the tree is 260's shape exactly: the check ran, and then the world moved 🟢 **CLOSED AT 268 — HE TOOK THE REACH QUESTION AT PICKUP AFTER EIGHT SESSIONS, AND THE ANSWER IS BOTH HALVES.** `host/scripts/publish_guard.mjs` runs from `prepublishOnly` and asks two questions: does the tag named by `host/package.json`'s version resolve to HEAD (260), and is the working tree clean AT THE MOMENT OF PACKING (264). It is deliberately the LAST link in that chain — `build` and `stage-addon` write only to gitignored paths, so a clean tree is still clean there, and a porcelain read taken after them is the difference between a check and a check that is still true. Fails CLOSED when git cannot answer, refuses by EXIT CODE rather than by printing (260's own near-miss was a refusal that printed inside a pasted block the shell ran to the end anyway), and waives only through `BREAKPOINT_ALLOW_OFF_TAG_PUBLISH=1`, which prints what it let through. Eleven self-test claims driven against throwaway repositories, in CI on every push; the live half cannot run there because HEAD is not its tag before a release, which is the whole reason its refusals are proved separately. 🔴 **IT REFUSED ON ITS FIRST LIVE RUN**: at 268's pickup `main` was `e620964`, one commit past `v1.79.0` 🔴 **RE-TIERED `internal` → `user` AT 268.** 248's line names *packaging* in the user tier explicitly. The row was filed internal on the strength of one instance's DAMAGE — *the artifact over-delivered rather than under-delivered* — which is a fact about how 260 happened to go wrong, not about what the row's work touches. | — |
| registry-lag-blind-past-tag | DONE | internal | — | 260 | 269 | — | `registry_lag.py` counts TAGS newer than the registry, so it is structurally unable to see commits newer than the newest tag. 🔴 IT PRINTED 🟢 ON BOTH SIDES OF THE SAME DEFECT WITHIN TWENTY MINUTES AT 260. Before the publish: `distance 1 · ceiling 3 · within ceiling`, while four merges of user-reaching work sat past the newest tag — among them the pipe truncation that silently cut every piped `tools --json` at one buffer. After the publish: `distance 0 · within ceiling`, over a registry artifact differing from its own tag by +888/−72. Same blindness, opposite sign, green both times. The instrument answers *how many tags has npm not got yet* and is read as *how far is the registry behind the work*, and those are the same number only in a history where every commit is tagged. 248 already recorded one face of this — a merged-but-untagged release cut reads lag 0 — and treated it as a curiosity of the counter rather than opening the row. The missing measurement is the commit count from the newest tag to HEAD, printed beside `distance` and ceilinged separately, because the two numbers go stale in opposite directions and a single ceiling over their sum would hide whichever one moved 🟢 **CLOSED AT 269 WITH THE SECOND COUNTER THE ROW ASKED FOR IN WORDS.** `head_past_newest_tag()` counts the commits from the newest `vX.Y.Z` tag to HEAD and `untagged()` judges them, printed on every live run beside `distance` and ceilinged SEPARATELY — the row's own argument, which is that the two numbers go stale in opposite directions and one ceiling over their sum would hide whichever moved. 🔴 **AND THE CEILING DOES NOT REFUSE 260's OWN INSTANCE, WHICH IS WRITTEN INTO THE TABLE AS ITS FIRST ROW RATHER THAN ENGINEERED AWAY.** 260 sat FOUR commits past its tag, and a ceiling tight enough to refuse four would refuse every ordinary mid-session state — a session legitimately sits several merges past its last tag while it works. What 260 was missing was never a refusal, it was a NUMBER: `lag` printed 🟢 and no line anywhere carried the four. `UNTAGGED_CEILING = 8` is MEASURED — every interval between consecutive tags across the last twenty-five releases is 7 or under except `v1.74.0 -> v1.74.1`, which is 26, the window 248 measured as twenty-five commits carrying exactly one user-observable change. Nine self-test rows, five refusing, and a claim that the two ceilings are never one number | — |
| cli-stdout-truncates-on-pipe | DONE | user | — | 258 | 258 | — | `breakpoint-mcp tools --json` loses everything past 65,536 bytes when stdout is a pipe | 🔴 MEASURED ON THE PUBLISHED 1.74.1 AND ON 1.75.0, SAME COMMAND, SAME BUILD: redirected to a file the export is 202,960 bytes of valid JSON; piped to `cat` it is **65,536 bytes** ending in an unterminated string — one pipe buffer, exit 0, nothing raised and nothing logged. `process.stdout` is SYNCHRONOUS to a file and to a TTY and ASYNCHRONOUS to a pipe, and the dispatch called `process.exit()` on the subcommand's return value without waiting for the queued write to drain. The flag's own usage text is *stable and timestamp-free, so releases can be diffed* and the README sells the export as *checkable without running it* — both describe a pipe, and the file redirect a developer reaches for while writing the feature is the one destination that hides it. 🔴 AND `cli_entry.test.ts` IS WHY IT LASTED: that file exists BECAUSE of an earlier published-tarball walk — *722 tests imported this package's functions and not one of them executed its binary* — and it has spawned the binary through a pipe since the day it landed. Every argv it passed produced less than one buffer. The mechanism was under test from the first commit and the only member of the population that could exercise it was never in the list. 🟢 CLOSED AT 258 AT THE DISPATCH AND NOT INSIDE `runTools` (254): `tools --json` is the only subcommand over a buffer TODAY — `doctor --json` is 2,138 bytes and `init --dry-run` 1,186 — so the population is the predicate OUTPUT LARGER THAN ONE PIPE BUFFER and the next command to cross it must not rediscover this from a bug report (257). Two rows in `cli_entry.test.ts`: the regression, which goes RED against the blinded fix, and a positive control asserting the payload still EXCEEDS a buffer, because the day the surface shrinks the regression starts passing for a reason that has nothing to do with the drain |
| addon-skew-unreadable-and-permanent | DONE | user | — | 258 | 259 | — | Nothing compares the host version to the addon version the user is running, and `init` makes the skew permanent | 🔴 THE ADDON IS THE HALF OF THE PRODUCT npm CANNOT VERSION, AND `registry_bytes.py` SAYS SO IN ITS OWN COMMENT — *the only name npm has for that artifact is the HOST version*. Four things compound. (1) `init.ts:167` tests `fs.existsSync(dest/plugin.cfg)` and returns a skip — the destination `plugin.cfg` is never opened and its `version=` never read, so an upgrading user keeps their old addon FOREVER. Measured: install 1.74.1 and the project gets addon 1.9.9; upgrade the host to 1.75.0 and re-run `init` and it prints `addon: skipped`, still 1.9.9, while the addon bundled in that same host is 1.10.0. (2) `doctor.ts:167-184` READS the version and interpolates it into a display string with `status` hardcoded ok — measured live: `✓ addon-installed addons/breakpoint_mcp (version 1.9.9)` against a 1.75.0 host, and `doctor.ts` does not import `version.ts` at all, so the host's own version is not even in scope in the file that prints the addon's. (3) No version crosses the wire: `bridge.ts:172-190` sends the auth secret and nothing else, and the `addon_version` that `operations.gd:378` puts in `ping` is discarded by all four of its consumers. (4) 🔴 AND THE REMEDY THAT NAMES THE CAUSE SHIPS ONLY IN THE ADDON THAT IS NOT STALE: `error_remedies.gd:85` answers `unknown_method` with *the addon running in the game is older than the host* — and `error_remedies.gd` was ADDED IN 1.10.0, absent at 1.9.9 and at every version before it. The population that can hit the error is exactly the population that cannot receive its remedy. The remedy is also wrong: it says re-run `breakpoint-mcp init`, which SKIPS — it needs `--force`. 254's rule is that a remedy is an instruction somebody will execute and every name in it is joined to the registry; nobody joined `init` to what init does. Blast: an addon at 1.9.1 lacks 5 methods the current addon answers, 2 of which the host calls (`main_screen.get`, `main_screen.set`). The reader this wants is the PAIR — host version against the addon actually loaded — and it has three possible homes (`doctor` as a check, `init` as a warning, the bridge as a handshake); price all three before starting, per 249 🟢 **CLOSED 259 IN ALL FOUR PARTS.** `host/src/addon-version.ts` spells the comparison once (dotted-numeric, `unknown` rather than a guessed direction — string order puts *1.9.9* AFTER *1.10.0*, the exact measured pair). (1) `installAddon` now opens the destination `plugin.cfg` and returns the pair, and `init` warns on stderr and names `--force` when a skip left an OLDER addon in place — silent when the skip was over a current one, because a warning on the correct path is how a warning stops being read. (2) `doctor` grew `addon-version` (on disk) and one `addon-running-<plane>` row per bridge that answers, `info` severity so a pre-flight other tooling gates on does not go red over an addon two releases behind. (3) `handshakeOk` keeps the `addon_version` it was throwing away, and `runtime_bridge.gd` answers `ping` with one at all for the first time — the plane whose own remedy made a claim about a version it could not report. (4) `host/src/remedies.ts` answers `unknown_method` from the side that is current by definition, and both `error_remedies.gd` rows now say `init --force`. Gate: check 28e joins every `breakpoint-mcp …` span in a remedy to `usage.ts` and refuses an `init` without `--force`; 28f ceilings the host table at one row. Ten blinds, ten refusals. |
| header-unmoved-unread | DONE | internal | — | 239 | 243 | — | `UNMOVED` is the same counter with no digit in it and nothing has ever read it | — |
| population-reach-floor | DONE | internal | — | 239 | 244 | — | Nothing floors how far back `BLOCK_POPULATION` must reach, only how wide it is | — |
| queue-claims-unread | DONE | internal | — | 240 | 246 | — | `QUEUE_SELFTEST n claims` is a counter no roster reads — the reader cannot be added until a real block carries the atom, and 240's block is the first that will | — |
| queue-unread | DONE | internal | — | 240 | 240 | — | The Open section is the one table in the tree no gate reads | — |
| ritual-tier | DONE | internal | — | 240 | 240 | — | The opening ritual re-measures a tree nobody has touched since it was last measured | — |
| queue-kill-loophole | DONE | internal | — | 240 | 240 | — | `QUEUE_NOT_ONLY_NEWEST` counted a KILL as a closure, so the first session under it satisfied the claim by abandoning five items | — |
| discover-rosters | DONE | internal | — | 233 | 246 | — | Ask the DISCOVER question of `SCOPE_LEDGER` (47), `CHECKS_EXPECTED` (23), `PROSE_NUMERAL_PINS` (14) | — |
| blind-py-gates | DONE | internal | — | 234 | 245 | — | Blind the seventeen tracked `.py` gates — none is swept by the instrument that asks whether an instrument collapses loudly | — |
| lint-roster-py-only | DONE | internal | — | 230 | 242 | — | The lint roster covers `*.py` and nothing else; the carried `.mjs` number is wrong by ~16x (~1,014, not 63) | — |
| cause-rule-py-only | DONE | internal | — | 229 | 242 | — | The cause rule covers `scripts/*.py` and nothing else | — |
| git-hooks-unset | DONE | internal | scripts/handoff_gate.py;scripts/instrument_gate.py | 228 | 286 | — | `git diff` and `git add` have no hook, and `core.hooksPath` is unset in a fresh clone | 🔴 **CLOSED AT 286, AND THE SESSION THAT CLOSED IT IS THE SESSION THAT PAID IT.** 286 made three commits in a fresh container clone with NO hook firing at all, while the identical commit on the device VM was refused by it — `GATE_TREE_UNRESTORED`, a `floor_pin_gate` killed by the 45s command cap leaving its record open. Same tree, same commit, one environment protected and one not, and nothing said which was which. 🔵 **THE READER WAS ALREADY WRITTEN AND NOTHING REFUSED ON IT.** 228 shipped `hook_state()` and `--install-hook` in `tree_quiet.py` and printed the answer as a 🟡 NOTE on the read path when the tree is quiet, saying so in its own docstring: *the honest reading here is that this half is a promise, so it is labelled one instead of counted as coverage*. A promise printed beside a green is 211 §19's shape and it held for fifty-eight sessions. 🔴 **AND THE PICKUP NAMED THE GAP IN PROSE EVERY SINGLE SESSION.** `--open` prints `INHERITED ON TRUST — n atom(s) declared CLONE: THIS CHECKOUT's configuration — hooks, remotes, git config` and then inherits it; the word *hooks* has been in that sentence since the provenance table was written. 🟢 **WHAT SHIPPED** is `hook_problems()` in the pickup — 284 §1.2's rule, the trigger being something every session does anyway — refusing `HOOK_UNINSTALLED` and naming `tree_quiet.py --install-hook` as the fix. It refuses at the pickup and NOT in CI, because a refusal on a machine that never commits is a claim about the wrong thing. 🔴 **AND IT ASKS WHETHER GIT WOULD RUN THE *TRACKED* HOOK, WHICH IS THE SHARPENING.** `core.hooksPath` unset resolves to `.git/hooks`, untracked by construction, so a hand-copied `pre-commit` there satisfies *an executable hook exists* while being a second copy that drifts — that case refuses too, and it is one of the four states driven in a throwaway repository rather than against a stub. 🔵 **THE OTHER HALF OF THIS ROW CANNOT BE CLOSED AND THE REFUSAL SAYS SO** (281 §1.2): git has no `diff` or `add` hook, so `git add` and `git diff` walk into no reader and never will. The refusal declares that it covers `git commit` only, in its own text, rather than implying it covers the row |
| gate-lock-prevention | DONE | internal | scripts/_gate_lock.py | 225 | 272 | — | `_gate_lock`'s coverage is detection plus recovery, not prevention | 🟢 **CLOSED AT 272: TWO OF ITS THREE HAZARDS ARE ALREADY PREVENTED AND THE THIRD CANNOT BE, BY ANY PROCESS.** Re-read in full. (1) Two mutating gates at once — `acquire()` takes `flock` with `LOCK_EX` and `LOCK_NB` and refuses the second before it touches anything. That is prevention, shipped. (2) Catchable termination — `_arm_restore_on_signal` turns SIGTERM/HUP/INT into `sys.exit()` so the `finally`/`atexit` restores unwind; added after a harness SIGTERM left `CHECKS_RUN_FLOOR` at zero. Prevention, shipped. (3) SIGKILL or OOM mid-write — no userspace code can catch it, so the answer is and can only be the baseline record plus `tree_quiet.py --recover`, which is detection-then-recovery and has caught two real incidents. 🔴 **THE ONLY REMAINING LEVER IS ATOMIC WRITES AND IT DOES NOT LIVE IN THIS FILE**: ~12 `write_text`/`write_bytes` sites across `control_gate.py`, `floor_pin_gate.py`, `instrument_gate.py` and `tree_quiet.py`, plus `mutation_lock_gate.py`'s AST deriver, which would misclassify every gate as unguarded unless taught the new helper — and even then `floor_pin_gate` rewrites FIVE tracked files, so per-file atomicity still leaves the SET non-atomic. That is a different, larger, only-partly-effective row and it is opened as one |
| mutating-gate-writes-not-atomic | KILLED | internal | scripts/_gate_lock.py;scripts/tree_quiet.py | 272 | 277 | — | The mutating gates rewrite tracked files with `write_text`, so a kill between the open and the flush leaves a torn file that only `tree_quiet.py --recover` can answer for | 🔴 **SPLIT OUT OF `gate-lock-prevention` AT 272 RATHER THAN LEFT INSIDE IT**, because that row's title was answerable and this is the part that is not. `_gate_lock` prevents the two hazards a process CAN prevent; the third — SIGKILL or OOM between a write's open and its flush — is only ever detectable after the fact, and the one lever that would narrow the window lives in the gates rather than in the lock: write to a temp file and `os.replace`, at roughly a dozen `write_text` / `write_bytes` sites across `control_gate.py`, `floor_pin_gate.py`, `instrument_gate.py` and `tree_quiet.py`. **Not one such site exists in this tree today** — every gate writes in place. 🔴 **AND IT IS ONLY PARTLY EFFECTIVE, WHICH IS THE HONEST PART OF THE ROW AND THE REASON IT IS TIER 2 WITH NO CLOCK.** `floor_pin_gate.py` rewrites FIVE tracked files; per-file atomicity leaves no torn file and still leaves the SET half-applied on a kill between file two and file three. Real atomicity is two-phase or a single source of truth, which is a redesign. 🔴 **THE COST NOBODY WOULD PREDICT**: `mutation_lock_gate.py` derives the guarded population by AST over `WRITE_CALLS = {"write_text", "write_bytes"}`, so introducing an atomic-write helper makes every converted gate read as UNGUARDED — the exact class of blindness that deriver exists to catch, caused by the fix. Its pinned self-tests and `floor_pin_gate`'s `SIZE_LEDGER` fixtures move with it. 🔵 Exposure is narrow and stated: CI runs these gates sequentially in one job, so the only realistic trigger is a local `kill -9` or an OOM, and detect-plus-recover has caught two real incidents already  🔴 **PRICED AT 277 AS 276 NEXT 3 ASKED, AND THE ANSWER IS THAT THE LEVER BUYS NOTHING.** A torn MUTANT is already recoverable byte-for-byte on both routes: a file clean when the lock was taken comes back from `git checkout --`, a file already dirty comes back from `STASH_DIR`, and `tree_quiet.py --selftest` drives both directions today. Per-file atomicity adds no recoverability to that. 🔴 **AND IT COSTS SIGNAL.** A half-written file does not parse, which is loud; an atomically-written one is a valid module that behaves exactly like a blinded instrument, which is quiet. The set-atomicity objection this row was told to price first dissolves the same way — `diverged(baseline)` already treats the five files as a set and `--recover` restores all of them — so the redesign it was holding the row open for was buying a property the record already has. 🔴 **THE PRICING FOUND THE WINDOW THAT DOES LOSE BYTES, AND IT IS ONE FILE OVER AND FIVE LINES WIDE.** `_gate_lock._stash()` cleared `STASH_DIR` and then re-filled it, so between the `rmtree` and the last `copyfile` the ONLY copy of a previously-dirty file's bytes did not exist — not in git, which holds the committed version, and not in a stash that had just been deleted. That module's own docstring says the sentence (*the stash is the only copy of the bytes*) about `--recover` and not about here. Copy first, sweep the stale names after; shipped at 277 with a fixture that kills a re-stash mid-copy and reads `GONE` against the old ordering. 🔵 **The row is KILLED rather than DONE because what shipped is not what it asked for** — it asked for atomic writes at a dozen sites and the measurement says not to build them |
| duration-assertions-unguarded | DONE | internal | host/test/;host/scripts/tautology_gate.mjs | 273 | 275 | — | Two assertions in the unit suite claim a wait ACTUALLY HAPPENED by bracketing a `setTimeout` with a clock reading, and nothing in the tree stops the next one being written the same way | 🔴 **OPENED AT 273 OUT OF A RED `main`, NOT OUT OF A REVIEW.** `initialized_wait.test.ts` read `expected at least 200ms, waited 199ms` on the post-merge run at `4a718f7` — one millisecond, on a tree whose own PR run was green, and the only signal was a red default branch that looked exactly like a regression. Both sites are repaired in the same cut against `TIMER_SLACK_MS`, whose value is measured rather than chosen. 🔴 **WHAT IS NOT BUILT IS THE GUARD, AND THE MEASUREMENT SAYS WHY IT IS WORTH ONE.** The first fix attempted was to bracket with `performance.now()` on the reasoning that `Date.now()` is the wall clock and `setTimeout` is not; two thousand rounds refuted it — the monotonic bracket goes early `24` times to the wall clock's `0`, worst shortfall `0.609` ms, because Node schedules against libuv's loop clock cached at the top of each iteration and the truncation in `Date.now()` was hiding it. A reviewer who reasons about this from first principles reaches the wrong answer, which is the argument for a check rather than a convention. 🔵 The shape available: `tautology_gate.mjs` already walks every assertion in both test trees with a TypeScript AST and already judges leaves by what they are satisfied by, so the population and the reader both exist — a lower-bound comparison whose left side is a difference of two clock readings, refused unless the right side carries the slack. Its cost is one leaf kind, one counter line, one self-test fixture, and `handoff_gate.py --patterns` re-run because a new printed line is a roster change (245). 🔵 Exposure is stated rather than assumed: the two sites are the whole live population, both are repaired, so this row buys nothing today and buys the third one — and the failure mode it prevents is a session spending a pickup diagnosing a red branch that was never a regression 🟢 **CLOSED AT 275, AND THE POPULATION IS FOUR RATHER THAN THE TWO THE ROW NAMED.** `durationClaim` reads the condition of every claim site the gate already walks: two lower bounds, both carrying `TIMER_SLACK_MS` since 273, and two upper bounds — `TAUT_DURATION` prints all three numbers and `TAUT_DURATION_UNGUARDED` refuses a lower bound with no slack term. The upper bounds are counted and not refused, which is 273's own judgement rather than a softening: *it finished in time* is a different claim from *the wait happened*. 🔴 **AND THE FIRST DRAFT WAS REFUSED BY THE GATE'S OWN SELF-TEST**, which is the part worth carrying: the fixtures pass the idiom they are about as a STRING, so the live gate reported four of its own self-test claims as unguarded lower bounds in a file that asserts nothing about a clock. A comparison inside a quoted span is not a comparison — `unquoted()` blanks them, and the rule generalises to every assertion whose message quotes its own expression |
| sdk-v2-migration | KILLED | user | host/package.json;host/package-lock.json | 226 | 281 | — | 🔴 RE-TARGETED AT 250 AGAINST A MEASUREMENT, NOT A GUESS: there is no 2.x of `@modelcontextprotocol/sdk` on npm. 79 published versions — 5 in 0.x, 74 in 1.x — `dist-tags` carries only `latest: 1.30.0`, and there is no `next` or `beta`. The row has read *unblocked and not done* since 226 while the release it would migrate to has never shipped; `release_names.py` carries a whole arm to keep this cut cuttable and `contract_check.py`'s `engines` reasoning is downstream of it, so the apparatus is ready and the dependency is not. The target is a date to RE-READ THE REGISTRY, not a promise of work — check `npm view @modelcontextprotocol/sdk dist-tags` first and re-target again if 2.x still does not exist. The tree meanwhile pins `^1.29.0` and installs 1.29.0 against a published 1.30.0. 🔵 RE-READ AT 257, EARLY AND ON PURPOSE — the target is a date to check the registry, and checking it costs one command. `dist-tags` is still `latest: 1.30.0` alone, still no `next` and no `beta`, and the published version list is unchanged in shape: 79 versions, none of them 2.x. Nothing about the row's argument has moved, so the target stands at 262 rather than being re-set from today — re-setting it on every early look would make the schedule a function of how often somebody happened to check. 🔵 RE-READ AT 262, WHICH IS THE TARGET, AND THE ANSWER IS THE SAME ONE: `dist-tags` is `latest: 1.30.0` alone, no `next`, no `beta`, and the published list still ends at 1.30.0 with nothing in 2.x. The row is RE-TARGETED to **270** rather than worked, because the work does not exist to do — and the target is re-set from here rather than left at a lapsed date, since a schedule check that has passed is the one signal that would tell a future session nobody looked 🔵 RE-READ AT 267, EARLY AGAIN AND FOR THE SAME ONE-COMMAND COST: `dist-tags` is still `latest: 1.30.0` alone, no `next`, no `beta`, and the published list is still 79 versions with **zero** in 2.x. The target is NOT re-set from here — 257 established that re-setting on every early look makes the schedule a function of how often somebody happened to check, and 270 is the date a lapse would be visible. | — 🔵 **RE-READ AT 270, WHICH IS THE TARGET, AND THE ANSWER IS THE SAME ONE FOR THE FIFTH TIME**: `dist-tags` is `latest: 1.30.0` alone, no `next`, no `beta`, and the published list is still 79 versions with **zero** in 2.x. RE-TARGETED to **278** rather than worked, because the work still does not exist — and re-set from here rather than left at a lapsed date, per 262. 🔴 **ONE FACT IN THIS ROW HAD GONE STALE AND IS CORRECTED HERE**: it has said since 250 that the tree *pins `^1.29.0` and installs 1.29.0 against a published 1.30.0*. The lockfile resolves **1.30.0** and has since some lockfile refresh nobody attributed. The pin is unchanged; the install is current. A row that carries its own evidence has to have that evidence re-read, not inherited 🔵 **RE-READ AT 278, WHICH IS THE TARGET, AND THE ANSWER HAS NOT MOVED IN FIFTY-TWO SESSIONS.** `npm view @modelcontextprotocol/sdk dist-tags` is `latest: 1.30.0` alone — no `next`, no `beta` — and the published list is still 79 versions with **zero** in 2.x. The row is RE-TARGETED to **286** rather than worked, because the work does not exist to do, and the target is re-set from here rather than left to lapse: 262's rule, since a schedule check that has passed is the one signal that would tell a future session nobody looked. The tree still pins `^1.29.0` and now INSTALLS 1.30.0 against it, which is 276's measured toolchain move and not a defect. 🔴 **RE-READ AT 279 AND THE ANSWER IS THE OPPOSITE ONE. VERSION 2 SHIPPED ON 2026-07-27, UNDER THREE NAMES THIS ROW NEVER ASKED ABOUT.** `@modelcontextprotocol/core`, `/server` and `/client` are each at `latest: 2.0.0`, joined by `/node` and `/express`, all five published from `github.com/modelcontextprotocol/typescript-sdk` — the same repository that publishes `@modelcontextprotocol/sdk`, which stayed frozen at 1.30.0 with zero in 2.x exactly as five re-reads correctly reported. Alphas were public from 2026-06-25 and the stable from 2026-07-27, so the readings at 267 and 278 were both taken after it landed. 🔴 **THE ROW'S OWN INSTRUCTION IS WHAT MADE THE ANSWER UNREACHABLE.** It said *check `npm view @modelcontextprotocol/sdk dist-tags` first*, and a Tier 1 row then waited fifty-three sessions on a reader pinned to one package name — a population nobody derived (277), in the one place this project had written the population down as a command. Five sessions ran that command correctly. 🔴 **AND THE EARLY WARNING BUILT FOR THIS CLASS IS STRUCTURALLY BLIND TO IT**: `sdk-drift.yml`'s `latest-sdk-build` rebuilds against the newest version IN RANGE, and a range cannot contain a new major. 🟢 **WHAT SHIPPED AT 279 IS THE READER, NOT THE MIGRATION.** `registry_lag.py --upstream` derives the population — every package the registry serves from the same repository as the dependency `host/package.json` actually pins — and refuses when any of them carries a major the tree does not; it runs in `sdk-drift.yml` as its own job, six fixture rows drive it offline, and the tree's live reading refuses today naming all five packages. 🔵 **THE MIGRATION ITSELF IS HIS.** It changes what the shipped server links against, which is a behaviour decision steered before it is built, and the row is SCHEDULED **280** — not re-targeted to buy time, but pointed at the session that already holds `release-1820-bump-under-wire`, the other row whose decision is his, because 278 established that read-then-decide rows are a set one session can honestly carry. Its age keeps accruing, and it should: fifty-three sessions is the measurement, not the embarrassment 🔵 **MEASURED IN FULL AT 280 AND THE DECISION IS DELIBERATELY HELD FOR 281 — his call, not a lapse.** The target is re-set from here rather than left at a lapsed date, on 262's rule: a schedule check that has passed is the one signal that would tell a future session nobody looked. **The import surface: 70 files, 115 statements, 7 subpaths, 17 symbols.** `sdk/server/mcp.js` 48 -> `@modelcontextprotocol/server`; `sdk/client/index.js` 23 -> `/client`; `sdk/types.js` 19 -> `/core`; `sdk/client/stdio.js` 17 -> `/client/stdio`; `sdk/inMemory.js` 6 -> either (both export `InMemoryTransport`); `sdk/server/stdio.js` 1 -> `/server/stdio`. All rename-only: `registerTool`, `registerResource` and `registerPrompt` are all still on v2's `McpServer`. `ErrorCode` is gone and `ProtocolErrorCode.InvalidParams` is the same `-32602`, one site. **zod is NOT a blocker** — v2 wants `^4.2.0` and this tree has pinned `^4.4.3` since 255, so `zod-3-to-4` paid for this in advance. 🔴 **THE WHOLE DECISION IS ONE IMPORT: `sdk/experimental/tasks` HAS NO v2 HOME.** v2 kept the PROTOCOL — all four task RPCs, byte-identical method names, in `core` — and dropped the SERVER-SIDE implementation: `InMemoryTaskStore`, `isTerminal`, `ExperimentalMcpServerTasks`, `assertToolsCallTaskCapability`, the `extra.taskStore` injection and the `{ taskStore }` constructor option, 18 symbols absent from core+server+client. `GodotTaskStore extends InMemoryTaskStore`, and `host/src/tasks.ts` is 211 lines. 🔵 **AND v2 DOES NOT MOVE THE WIRE REVISION BY DEFAULT**: its `LATEST_PROTOCOL_VERSION` is `2025-11-25`, the same as v1's, and `SUPPORTED_PROTOCOL_VERSIONS` is byte-identical. 2026-07-28 is reached by OPT-IN era negotiation, and what it unlocks is the 26 schemas v1 lacks — `Discover`, `SubscriptionsListen` + `SubscriptionFilter`, and the OAuth metadata set. v1.30.0 is **not** deprecated on npm; it shipped 2026-07-27 and nothing has followed it against a previously monthly cadence. **The three doors, priced: close it against the measurement (v1 stays, re-open on evidence); migrate and port the task store in-tree; migrate and drop the experimental task capability, which is a user-visible removal from `initialize` and a MAJOR.** 🟢 **CLOSED AT 281 AGAINST 280's MEASUREMENT, BY HIS DECISION, AND THE DOOR HE TOOK IS THE ONE THAT REPAIRS WHAT THE ROW ACTUALLY COST.** Of the three priced at 280 he took *close it against the measurement and bind the re-open*: v1 is not deprecated, v2's default handshake is byte-identical to v1's (`LATEST_PROTOCOL_VERSION` `2025-11-25` on both), so no client can tell the two apart today — and the other two doors each pay a permanent cost for that nothing. Porting means owning ~600 lines of server-side task machinery v2 deliberately dropped and still calls experimental; dropping the capability withdraws something advertised in `initialize` and declared on all 289 tools, a MAJOR one session after 1.82.0 measured what that costs a caller. 🔴 **AND THE ROW'S REAL DEFECT WAS NEVER THE DEPENDENCY — IT WAS THAT ITS TRIGGER WAS A SENTENCE.** Fifty-three sessions on *check `npm view @modelcontextprotocol/sdk dist-tags` first*, run correctly five times, against a package that could not answer. So the deferral is a ROW a reader refuses: `UPSTREAM_DEFERRED` in `registry_lag.py` will not take an entry without `seen` — the family's `MAJOR.MINOR` coordinates at the moment the argument was made — and `deferral_problems` re-derives it on every run. It refuses when a MINOR moves (a MINOR can publish an export; a PATCH cannot, which is semver's own line and not a taste), when a sibling joins or leaves the derived family, when the registry DEPRECATES a package the argument rests on, and when a run honours a deferral without having asked about deprecation at all — this tree read npm's `deprecated` field nowhere before 281, while this row asserted *not deprecated* in prose. Eight refusing fixtures, plus the SHIPPED row driven in both directions: it holds against the upstream it names and must NOT hold against one MINOR different. 🔵 `SDK_UPSTREAM` reads 🟢 DEFERRED today instead of the standing red 279 left, and that is the opposite of silencing it: the green is conditional on a measurement re-taken every Monday, and 279's own refusal text asked for exactly this — *migrate, or say in the row which packages the answer lives in and why they do not apply* — with nowhere to say it 🔴 **AND THE STATE IS `KILLED`, NOT `DONE`, WHICH IS THIS SESSION'S OWN NEW READER REFUSING ITS AUTHOR.** `QUEUE_PATHS_JOIN` (271, closed at 281) fired on this row first: it closes declaring `host/package.json;host/package-lock.json` — the paths the MIGRATION would have touched — and the commit closing it touches `scripts/registry_lag.py`. Nothing is wrong with either fact; what a reader of the row alone cannot see is that they disagree. `DONE` would claim the work in the title happened. It did not: a measurement settled it and a decision was taken, which is precisely what `KILLED`-with-a-reason is for — and 244's rule that only `DONE` answers `QUEUE_NOT_ONLY_NEWEST` is what stops that distinction becoming a laundering route. The `reach` cell stays `user`: the migration WOULD reach one, and the tier is about the work, not about where the decision landed |
| zod-3-to-4 | DONE | user | — | 226 | 255 | — | The Zod 3 → 4 migration, with its measured blast radius. 🟢 **B2 IS ANSWERED AND IT DOES NOT REPRODUCE — MEASURED AT 252 AGAINST A LIVE GODOT 4.7, WHICH IS WHY THE TARGET MOVED RATHER THAN LAPSED.** The fear was that `runtime_get_monitors` puts `Performance.get_monitor()` straight onto the wire and zod 4's `z.number()` rejects non-finite values where zod 3 accepted them, so an `INF` from a division would turn a working tool into one that fails validation on exactly the frames a user cares about. Walked: all 59 monitor ids, over 91 real frames including frame 0, and again under `Engine.time_scale = 0` — the state this project's own docs name as the one that zeroes `delta`. **Zero non-finite readings; `TIME_FPS` reads 1.0 where a naive `1/delta` would read INF, so the engine clamps it.** Two limits worth carrying rather than hiding: the walk is headless, so the rendering-server monitors read 0.0 throughout, and it is one platform on one engine build. `ZOD_BLAST_RADIUS_2026-08-10.md`'s option (ii) — the `zod/v4` subpath on the installed `zod@3.25.76` — is still the cheap door and needs no `package.json` edit; `sdk@1.30.0` declares zod `^3.25` or `^4.0`, both majors. The row is now an ordinary day of work with no blocker in front of it | 🟢 CLOSED AT 255 BY MOVING THE TREE TO `zod@^4.4.3` — the endpoint, once, rather than the subpath the spike recommended. **The measurement that changed the door: the `zod/v4` subpath inside `zod@3.25.76` is core 4.0.0, and B1 does not reproduce there.** `z.object({v: z.any()}).parse({})` PASSES on 4.0.x through 4.3.6 and FAILS from 4.4.0 — so an `any`-typed key becoming required is not a 3 → 4 break at all, it is a 4.4.0 break shipped inside a MINOR, and the spike's plan to *collapse to a full move as a no-op dependency bump* would have carried the whole change into whichever later session ran `npm update`. B2 needed nothing: `finiteness.ts` has contained every non-finite reading at the single `JSON.parse` in `bridge.ts` since 226, so no non-finite number reaches a schema on either major. B3 is inert — every `.int()` site is a frame id, a tile coordinate or a timeout, none within 2^53 of the new bound. The hard breaks were already zero because 224 wrote the two-argument `z.record` form. **What actually moved: 16 output keys and 7 input keys joined `required`, and `schemas.ts` had been writing that distinction by hand the whole time** — `runtime_assert_scene_structure` is the one entry spelling `encodedValue.optional()`, for exactly the two fields its handler omits — so the wire now says what the file already meant. Check 29 joins every required-any key to the handler that answers that tool, per tool rather than per file; `schemas.test.ts` pins the set off the CONVERTED schema, so a future zod cannot re-optionalise them quietly. Three instruments moved with the major: `wire_invisible_gate.mjs` was reading zod 3 internals and its strip had to be rebuilt through `core.clone` because the converter reads `_zod.bag` and not the checks; `wire_diff.mjs` gained `propertyNames` as a constraint and walks `definitions`; and `wire-defaults.ts` learned that a definition container reached only by a local pointer is dialect-inert, which is what kept the two recursive schemas from shipping a draft-07 declaration |
| breakpoint-lite | KILLED | internal | — | 226 | 243 | — | Decide "Breakpoint Lite, zero install" — priced in `docs/BREAKPOINT_LITE_PRICED.md` (242) | Decided in 243 on the price 242 measured. 74% of the 292 tools need a live editor and the fifteen `dbg_*` tools ride Godot's debug adapter on 6006, which exists only while the editor is open — so three of the four readings of "Lite" ship a Godot debugger that cannot debug, and the fourth is an Asset Library listing, which is a distribution channel for the product that already exists rather than a variant of it. The four remaining install steps are each an action in somebody else's process and none is automatable by anything this project can ship. Reopen only against evidence that install friction is what separates the downloads from the feedback — 242 §6.6's untested premise, and the cheaper thing to test |
| alt-mcp-source-pass | DONE | internal | docs/alternative_mcp_roster.json | 223 | 272 | — | A source-level pass on `Wick` and `godot-mcp-bridge` | 🟢 **CLOSED AT 272 AS ALREADY DONE, RE-PRICED AGAINST THE TREE RATHER THAN RE-READ FROM THE TITLE.** The row's literal ask — a source-level pass on `Wick` and `godot-mcp-bridge` — was performed on 2026-08-10 in commit `2451270`, essentially concurrent with the row's own opening, and the results are in the tracked roster with commit-level evidence: `godot-mcp-bridge` `last_analysed_commit 8852c9c`, `Wick` `99806df`, each carrying `real_dap_client` / `real_lsp_client` / `csharp` facts that Asset Library metadata cannot produce and only reading the source can. 🔴 **WHY IT STAYED OPEN FOR FORTY-NINE SESSIONS IS THE INTERESTING PART**: `QUEUE.md` did not become a tracked, gate-read file until `2bd3906`, two days AFTER the roster answered it, and the row was carried forward from a gitignored handoff without anyone checking it against the answer that had just landed. Then 248's tiering made `internal` rows deferred with no clock, so nothing ever forced the recheck. Not outstanding work — unclosed bookkeeping. A FRESH pass is `assetlib_sweep.py`'s ordinary cadence and not this row |
| declared-outside-five | DONE | internal | — | 214 | 246 | — | The `declared-outside-this-file` five | — |
| sweep-227-15 | DONE | internal | scripts/;host/scripts/ | 228 | 290 | — | Of each instrument in this tree, ask which of two things its green line actually says: *this classifier can classify fixtures*, or *the thing it classifies is checked* | 🔴 **RE-POINTED AT 289, AFTER 287 MEASURED THE POINTER DANGLING.** The row read `227 §15's sweep` for sixty sessions and `HANDOFF_SESSION227.md` has nine sections, §1 through §9 — there is no §15, and 287 recommended a kill on that measurement. 🟢 **THE REFERENT EXISTS ONE LEVEL DOWN AND IS QUOTED HERE SO IT CANNOT GO MISSING AGAIN**: §8 of that document is a numbered list running 1–19, and item 15 reads *“A green gate is a claim about a question, not about a tree. `wire_diff.selftest.mjs` was green on every push for eleven releases and the thing it proved was ‘this classifier can classify fixtures’. Nobody read it as that; it was read as ‘the wire is checked’. … Worth one pass asking, of each of the eleven instruments, which of those two things its green line actually says.”* Neighbouring handoffs — 223 to 226 — do carry sections numbered into the teens, which is how a §-shaped citation attaches itself to an item inside §8. 🔵 **AND THE QUESTION IS THE GENERAL FORM OF DEFECTS THIS TREE HAS PAID FOR ONE AT A TIME**: a blinded reader returning `[]` with every fixture still green (287 §5.4), a positive control computing its expectation from the reader under test (286 §7.4), a derivation whose corpus contains its own fixtures (282, and 289 closed one instance of it). A specimen from outside the tree arrived at 288: `GeminiAnalysis.md`'s `test_concurrency.js` prints *“✅ Result: Server proxy proved stable and completely crash-proof under simulated load.”* unconditionally, whatever its two hundred iterations did — a green line whose subject is the printer. 🔴 **IF IT IS EVER KILLED, THE KILL NOTE CARRIES THE REFERENT ABOVE**, or the next reader inherits the same dangling pointer with a *we looked* stamp on it 🟢 **CLOSED AT 290 — THE PASS RAN, AND ITS CORPUS WAS THE REPLAY THIS SESSION HAD TO RUN ANYWAY.** Asked of every instrument in the tree, the answer is already DERIVED and printed; what had never happened is somebody reading the three census lines as one population. 🟢 **THE JS HALF SAYS *the thing it classifies is checked*, WITHOUT RESIDUE**: `discover 56/15/15/28` — 56 files walked under `host/scripts` and `host/test-integration`, 15 export-bearing, all 15 declared instruments and 28 gate/driver — with `0 exempt · 0 undeclared`, and `instrument ok across 23` blinds every one of them and requires the run to go RED. `contract_check.py`'s 75 enumerators and `control_gate.py`'s 83 controls are the same claim over two more populations. 🔴 **THE PYTHON HALF DOES NOT, AND THE NUMBER SAYING SO HAS BEEN IN THE STATUS BLOCK SINCE 246**: `py gates 18/6/12` — of 18 tracked `scripts/*.py`, SIX are swept and TWELVE carry a written reason for not being. Eight of those twelve are structural and honest — `instrument_gate.py` cannot blind itself from inside its own mutation lock, `scope_gate.py`, `control_gate.py` and `floor_pin_gate.py` take that lock, `_gate_lock.py` is a library asserted from outside, `contract_check.py` is swept by `scope_gate.py` instead, and `registry_lag.py` and `assetlib_sweep.py` measure a network this gate is allowed to fail on. 🔴 **FOUR ARE NOT, AND THEIR REASONS RECORD A MEASURED RESIDUE RATHER THAN AN ARGUMENT**: `spec_conformance.py` 3 of 7 caught, `registry_bytes.py` 4 of 10 with `{SIG:tree_shas}` STILL GREEN under `--selftest`, `lint_ceiling.py` 4 of 8, and `tree_quiet.py` measured and harness-shaped. For those four the green line says *this classifier can classify fixtures* over part of its own surface, which is item 15's question answered in the affirmative sixty-two sessions after it was asked. 🔵 **AND THE ANSWER IS NOT WHERE A READER FINDS IT**: it is twelve dict values in `PY_NOT_SWEPT`, and a roster of exemptions read one entry at a time is not a population anybody has looked at — filed as `instrument-green-residue-is-a-population-nobody-reads`, which is the successor this row earned by being answerable at all |
| d10-capability-matrix | DONE | user | — | 223 | 251 | — | D10 capability matrix with a drift gate. 🔴 CLOSED AT 251 AGAINST A MEASUREMENT, AND THE MEASUREMENT FOUND 21 DEFECTS. Priced first: of the four clauses, *build the matrix from the live registry* was already built (`buildToolsReport()` in `cli/tools.ts`, deterministic and `generatedAt: null` for exactly this purpose), *commit it* was already done in a different form (`docs/TOOL_CATALOG.md`'s Tool Index — 292 rows, four columns), and *fail CI on any published count that disagrees* has been covered since checks 11/11b/11c/13 derive every published count. That left *fail CI on drift*, and the finding is that check 4 — the only committed-artifact-vs-registry comparison in the tree — read the NAME cell and stopped: its regex ended at the first column separator. `Plane`, `Status` and `Destructive` were shipped, published and gated by nothing for as long as the catalog has existed. Check 4 now reads the `Destructive` column against `annotations.ts`'s `DESTRUCTIVE` in both directions, and its first run over the shipped tree reddened on 21 of 292 rows: 13 tools carrying `destructiveHint: true` on the wire and no ✔ in the doc (`anim_remove_key` and `godot_stop` read `–`), and 8 marked ✔ that the contract does not call destructive. All 21 corrected; the remaining column is split out as `catalog-plane-column-unread` rather than carried inside a twenty-eight-session bundle | — |
| review-charter-p0 | DONE | internal | — | 205 | 241 | — | P0 of the code review charter | — |
| npm-token-scope | KILLED | internal | — | 228 | 247 | — | The npm publish token's scope is broader than the package needs | Nineteen sessions, and nothing in this tree can observe the thing it asks about. The publish is manual and local: measured this session, the three workflows contain ZERO `secrets.` references and no publish step, so the credential never reaches CI and there is no repository setting to narrow. What is left is a property of a token on one machine and of an account page at npmjs.com, and the only gate that could read it would have to handle the token itself — a worse hazard than the one this row names, in a repository whose auth reader (`registry_bytes.auth_state`) is deliberately PURE and never touches npm. The actionable half is a step for the human doing the cut and it is written into `CONTRIBUTING.md` where that human already reads the release ritual: a granular token limited to this one package, with an expiry. Same disposition as `prepack-unwired` (243) and for the same reason — the property is real, and the place it can be enforced is not this tree |
| prepack-unwired | KILLED | internal | — | 215 | 243 | — | `prepack` is unwired | Refused on purpose in 204 §8.28 and listed there under *decided — do not re-open*, then transcribed forward as open work for twenty-eight sessions. Re-measured in 243 and the refusal is stronger than it was: `prepack` runs before BOTH `pack` and `publish`, so a `prepack` that re-staged the addon would hand check 6 a tarball the pack had just repaired, and check 6 now reads the tarball rather than the working tree. The property it would enforce is MEASURED instead, by `registry_bytes.py`'s first comparison. The gap is real and was reproduced — `npm pack` on a clean checkout of 1.74.0 ships 71 entries and zero addon files against 83 and twelve after `prepublishOnly`'s two steps — and it is now written into `CONTRIBUTING.md` where a human packing a tarball will read it |
| check-7-9-overlap | KILLED | internal | — | 205 | 240 | — | CHECK 7 / CHECK 9 overlap | Thirty-four sessions open with no session ever electing it and no defect ever traced to the overlap. Reopen with an id and a session if one is |
| concise-blind-late-axis | KILLED | internal | — | 205 | 240 | — | `concise_blind` late axis | Thirty-four sessions open, superseded in practice by the LATE_LIVE counter the instrument roster now reports on every run |
| d1b-not-built | KILLED | internal | — | 206 | 240 | — | D1b was never built | Thirty-three sessions open. It was priced once and never elected; carrying it has cost more reading than building it would have |
| twenty-defects | KILLED | internal | — | 219 | 240 | — | The twenty defects were not driven down | Twenty sessions as a bare count with no roster anyone could act on. A number without a list is not a queue item |
| audit-204-205-backlog | KILLED | internal | — | 204 | 240 | — | 205 §8.8–§8.11 (D6, D5, D3) and 204 §8.9–§8.24 not re-audited | Thirty-five sessions, carried as a citation of two documents rather than as work. Anything still live in them belongs here as its own row with its own id |
| replay-vs-ci-unread | DONE | internal | — | 241 | 242 | — | The session replay list and `ci.yml` are two rosters of the same commands and nothing compares them — `spec_conformance.py` is in CI and not in the replay, and it refused a document the full local ritual had passed | — |
| p0-reporters-unblinded | DONE | internal | — | 241 | 244 | — | The two P0 reporters are in `DISCOVER_EXEMPT` rather than `INSTRUMENTS` — a printing reporter has no live command that can redden, so the late axis has nothing to blind | — |
| required-any-reachability | DONE | internal | scripts/contract_check.py;scripts/scope_gate.py;scripts/control_gate.py;scripts/lint_ceiling.py;host/test-integration/;.github/workflows/ | 255 | 272 | — | Check 29 joins each required-`any` output key to the handler that answers its tool, and that join is PRESENCE and not reachability: a key written in one arm of a branch and forgotten in the other reads as present. 16 keys, 15 tools. The SDK validates `structuredContent` on every SUCCESS result, so the untested half is a thrown happy path — and 249 measured what a live capability probe costs, which is why this is written down rather than attempted at the end of a migration | 🟢 **CLOSED AT 272 AFTER THE PRICING 270 AND 271 BOTH ASKED FOR AND NEITHER DID — AND THE PRICING INVERTED THE ANSWER.** 🔴 **The defect count is ZERO and that is measured, not assumed**: thirteen of the fifteen handlers have exactly one success return, and the two that branch (`_editorsettings_get_set`, the host's `runtime_await_condition`) write the key on EVERY arm. The one place in the tree with the branch-omission shape this row describes — `_assert_scene_structure` — is already `encodedValue.optional()` in `schemas.ts`, which is why it is not in the population. So the row was never a bug report; it was a request for a tripwire. 🔴 **AND THE PRICING FOUND A LIVE WEAKNESS IN THE SHIPPED GATE INSTEAD.** Check 29 searched the handler's WHOLE BODY for `key:`, which a GDScript local's type annotation and the tool's own zod `inputSchema:` line both satisfy. Measured by deleting each real emit and re-running the join: **EIGHT of the sixteen keys still read PRESENT with nothing left writing them.** The join is `emitted_key_regions` now — `_ok(..)`, `.append(..)`, `var x := {..}`, `const x = {..}` — and the same experiment leaves **0 of 16** standing, with no false positive on a healthy tree. `29.emitdecoy` is the control, and it is the defect itself: a mutation the old join could not see. 🔴 **THE REACHABILITY HALF IS ANSWERED BY EXECUTION AND NOT BY A MODEL.** A static branch walker was priced and REFUSED: it needs a GDScript CFG in a file where every reader is a regex, and it false-positives on 5 of 16 against the `var out := {..}` and loop-built-element idioms this tree is written in. The live route turned out to be nearly free — 14 of the 15 tools were already driven against a real engine and 9 of the 16 keys already SDK-validated by `authoring-plane`'s real MCP client. What was missing: `node_set_property` (the one tool no probe drove) and the runtime plane's TRANSPORT, since sixteen of eighteen probes build a fake `{registerTool}` object and reach `validateToolOutput` never. Both closed |
| orphan-ceiling-outruns-its-reading | DONE | internal | host/scripts/tautology_gate.mjs;host/scripts/tautology_gate.selftest.mjs | 307 | 308 | — | The tautology gate prints how many claim sites nobody has attributed to a test or a section, and refuses if that number goes above a written ceiling. 307 taught the two attribution readers to ask the file's code instead of its text, and the live reading fell from 44 to 26 — but the ceiling is still 44, so there is an 18-claim budget for a regression nothing would report. Worth doing because the reading is now trustworthy and the cost of following it down is already measured. Benefit to the user: none directly. Benefit to the product: a ceiling that means what it says, instead of one carrying slack left over from a defect that has been fixed | 🔴 **THE COST IS MEASURED AND IT IS ELEVEN CLAIMS.** Setting `ORPHAN_CEILING = 26` reddens eleven self-test claims — 184 §7 and 248's *lowering a FLOOR-shaped ceiling can cost ten absolute pins* measured almost exactly. 🔵 **AND THEY ARE ABSOLUTE ON PURPOSE**, which is why 307 priced the move rather than making it: the file's own header says `🔴 THE CLAIM FLOOR IS A LITERAL`, and several of the eleven are boundary fixtures built from the shipped value (*the shipped tree sits exactly ON the ceiling and passes*, *the shipped orphan ceiling is 44, not 26*). Deriving them from the constant would be the wrong fix — a test that computes its expectation from the value it is checking cannot catch a wrong value. So this row is a deliberate retype of eleven pins plus the one reading that justifies it, in its own commit, and it must not ride on a release cut (248). 🟢 **CLOSED AT 308, RETYPED AND NOT DERIVED.** The ceiling reads `26`, measured `5062 sites - 5036 attributed`, and the rule the eleven retypes followed is one sentence: every fixture keeps its POSITION against the ceiling — exactly on it, one over it, the same distance under it — and the literal is retyped to say that against the new value. 🔵 **AND ONE PIN WAS MADE LOAD-BEARING RATHER THAN MERELY RETYPED**: *the ceiling is a PARAMETER* was passed `45` against a fixture at `44` and would have passed at any value; it is `27` against a fixture at `27` now, so it is red at the shipped ceiling and green at the raised one, which is what the sentence has to mean. Driven three ways — at `27` the retyped pins redden four claims, at `25` they redden six, at the old `44` they redden four — so they discriminate in BOTH directions, which they could not do while the ceiling carried eighteen of slack for a defect that no longer exists | 
| review-charter-p1-p6 | OPEN | internal | — | 241 | — | — | P1–P6 of the charter, scope now measured: P1 is three symbols, P4 is one decision, P6 is 918 branches | — |
| open-npm-lag-unread | DONE | internal | — | 241 | 242 | — | `handoff_gate.py --open` re-reads 5 of its 6 header atoms — `npm.lag` returns UNREAD, and lag is a fact about the world | — |
| p0-node-engine-floor | DONE | internal | — | 242 | 242 | — | Both 241 reporters import `globSync`, exposed in Node 22, against a declared `engines.node >=18` and a CI matrix of 18/20/22 — an ESM link error, not a runtime one | — |
| replay-ci-flag-granularity | DONE | internal | scripts/handoff_gate.py | 242 | 274 | — | The replay/CI comparison is at SCRIPT granularity; CI runs seven flag variants the replay does not (`--patterns`, `--assert-addon`, `--assert-map`, three `--selftest`, `--refresh`) and nothing compares those | 🟢 **CLOSED AT 274, AND THE ROW'S OWN LIST WAS THIRTY-TWO SESSIONS OUT OF DATE.** Measured before building: five of the seven variants it named have since joined the replay, and the live divergence at flag granularity is THREE. `release_names.py --assert-map` and `spec_conformance.py --selftest` run merge-blocking in `ci.yml` and the replay ran neither — 241's defect one resolution finer, and both are now in the fence. `registry_lag.py` bare runs in the replay and in no workflow, and that one is not a defect: it DIALS THE NPM REGISTRY to answer `npm.lag`, a fact about the world, and a workflow asserting it would redden every unrelated pull request the moment somebody publishes. It is declared in `REPLAY_CI_FLAG_EXEMPT` with that reason, beside `spec_conformance.py --refresh`, which is `sdk-drift.yml`-only and never blocks a merge. 🔴 **AND THE WALK IT NEEDED EXPOSED A ROSTER THAT HAD BEEN ADMITTING NON-SCRIPTS SINCE 242.** `CI_SCRIPT_RE`'s suffix was unanchored, so `${{ github.sha }}` yielded the script `github.sh` and `OPS_UNIT_PASS rb.shot.mime` yielded `rb.sh` — which carried a written CI-only exemption reading *same shape as `gate.sh`*, for a file that has never existed in this repository. `REPLAY_CI_EXEMPT_STALE` structurally could not see it, because the roster DID report the name. Both are gone; `gate.sh` is real (a heredoc at `integration.yml:1064`) and stays |
| p0-reporters-unrostered | DONE | internal | — | 242 | 243 | — | `p0_complexity.mjs`, `p0_testdup.mjs`, their two self-tests and `p0_comments.py` are in NEITHER the replay list nor any workflow — the population that hid the Node-engine defect for a whole session | — |
| ts-strict-mjs-undeclared | DONE | internal | scripts/lint_ceiling.py | 242 | 289 | — | The JS lint roster runs non-strict by choice; `--strict` reports 2,568 findings in 26 classes over the same files, and whether the instruments should be judged at the strictness the product already gets is undecided | 🟢 **DECIDED AT 289, AND THE DECISION IS NO — DELIBERATELY.** Forty-seven sessions of not deciding was the defect; the row was never 2,568 problems to fix. **What the answer rests on, all of it already measured at 242 and none of it re-litigated here**: 1,558 of the 1,558-finding delta between non-strict and strict is `noImplicitAny` reporting that a parameter carries no annotation — an annotation-density measure over untyped JavaScript, not a defect measure; summing it with a real class such as `TS2339` destroys the by-class distinction this gate exists to make; and a ceiling that moves whenever anybody writes a function parameter is a gate people route around. **And the population is not the product**: every `.mjs` in it is an instrument or a build script, none of it is anything an installer of the package runs, and the shipped TypeScript is already `tsc --strict` on every push. 🔴 **WHAT WOULD REVERSE IT, WRITTEN DOWN SO THE NEXT READER DOES NOT HAVE TO RE-DERIVE THE QUESTION**: a strict-only finding in a class that is NOT `noImplicitAny`, or a tracked `.mjs` that starts shipping to users. Either makes this a defect measure again and the row can be reopened on the evidence rather than on the size of the number. The reasoning now lives in `lint_ceiling.py`'s own docstring, where the next person to ask reads it before running anything |
| unreached-main-granularity | DONE | internal | — | 243 | 245 | — | The UNREACHED closure proves a LOAD and not a RUN — `main()` in all three P0 reporters is executed by nothing, so a defect past the import boundary is as invisible as the link error was | — |
| version-row-second-claim | DONE | internal | — | 243 | 244 | — | The `host / addon` row's `unmoved` now has a reader; the VERSION STRINGS beside it are exempt as non-counters and nothing compares them to the tree at all | — |
| backlog-md-stale | DONE | internal | — | 161 | 240 | — | `BACKLOG.md` carries a 1.39.0 baseline against a 1.74.0 tree and still says "read this before anything else" | — |
| schedule-set-unread | DONE | internal | scripts/queue_gate.py | 244 | 280 | — | `QUEUE_SCHEDULE_HONOURED` reads one row at a time; nothing asks whether a target session's whole SET is achievable, and 243 and 244 each opened carrying six | 🟢 **CLOSED AT 280, AS THE OLDEST THING THIS SESSION DID NOT CREATE.** `QUEUE_SCHEDULE_SET` reads the TARGET as the unit rather than the row: `QUEUE_SCHEDULE_HONOURED` refuses a target that has PASSED, which is the right question asked of the wrong thing — a row parked on 281 is a promise about 281, and five of them are a promise about 281 that nothing in this table has ever kept. 🔴 **AND THE BAR IS DERIVED FROM THE `closed` COLUMN, NOT TYPED** — this session's own lesson, applied where it was cheapest to apply: the most `user` rows any ONE session has actually FINISHED. Measured at head 280: **four**, by 248, 252, 261, 267 and 268, over twenty-two sessions that closed any, median one. It rises only when a session really does finish more, which is evidence rather than permission, and 241 §2's rule reaches the INPUT of a derived ceiling rather than the output of a counted one: only `DONE` counts, so a session cannot raise the bar by KILLING five rows — driven as its own claim. 🔵 With a floor of **2**, which is not a taste: it is 278's precedent in 278's own words — *read-then-decide rows are a set one session can honestly carry* — and the smallest bar that does not forbid an arrangement this project has already judged honest. A derived population that empties answers zero and reads as strictness. Self-test 58 -> 63 claims. |
| late-live-blast-unfloored | DONE | internal | — | 244 | 245 | — | The B:live axis prints every instrument's blast and floors none of it — the reason (`no per-claim FAIL line`) is now false for the two commands 244 gave one | — |
| py-cohort-two | DONE | internal | — | 245 | 247 | — | The fifteen tracked `scripts/*.py` in `PY_NOT_SWEPT` — the injector exists and each row carries the measurement that says what is stopping it | — |
| p0-default-path-unrun | DONE | internal | — | 245 | 247 | — | All three P0 reporters run `--selftest` and `--floor` in CI and NOTHING runs them with no flag — the report branch every one of them exists to print is executed by nothing | — |
| scope-ledger-unreached | DONE | internal | scripts/scope_gate.py;scripts/floor_pin_gate.py | 246 | 272 | — | Sixteen of the forty-nine `SCOPE_LEDGER` floors can be collapsed by no blind in `scope_gate.py` — each is a module-level accumulator with no `def` to anchor on, and the work is a second injector that empties a binding AFTER the loop that fills it | 🟢 **CLOSED AT 272 BY THE SECOND INJECTOR THE ROW ASKED FOR, AND IT REACHED ALL SIXTEEN ON ITS FIRST RUN.** `binding_targets` anchors on a module-level accumulator and injects its empty after the LAST top-level statement that mutates it — not after the initialiser, which the loop undoes, and not before `SCOPE_LEDGER`, which would prove the floor bites and leave every check statement that reads the accumulator untested. 🔴 **THE POPULATION IS DERIVED FROM THE LEDGER RATHER THAN LISTED**: every ledger row whose value expression reads exactly one module binding and that no `def` blind already covers. So the sweep grows by itself and `LEDGER_UNREACHED` is EMPTY — a residue table kept for the next unspellable shape rather than deleted. `SCOPE_GATE_LEDGER_REACH` went **58/74 → 74/74**. 🔴 **AND THE FINDING INSIDE THE FINDING IS WHERE THE WIDE RADII TURNED OUT TO BE.** `codec_emitted` and `codec_accepted` — check 23's two halves — redden **13** and **14** FAIL lines each: the two widest accumulators in the file were among the sixteen floors that had never once been tested against the collapse they name. 🔴 `unsupported_kinds` is read as `len(unsupported_kinds["capability"])`, so its blind keeps the KEYS and empties the VALUES — 271's rule, because a blind that raises `KeyError` proves Python throws, not that the floor bites |
| release-names-twelve-readers | DONE | internal | scripts/release_names.py;scripts/instrument_gate.py | 247 | 278 | — | `release_names.py`'s twelve tree readers can each return their empty with `--selftest` green — the command proves the PREDICATES on fixtures and the readers are exercised only by `--assert-addon` and `--assert-map`. Split out of `py-cohort-two` on 246 NEXT 4's instruction |  🔴 **THE ROW SAID SEVENTEEN MEMBERS AND THE FILE HOLDS THIRTY-THREE**, which is 277's finding one file over, in the sibling row 277 handed forward, third consecutive session and the same reason every time: the count was taken by hand and nothing printed it. Every member blinded and the command run against it before a line of the roster was written — then all sixteen survivors blinded AGAIN against both live commands. 🔴 **AND THE BLAST READ ZERO.** This file reports in a FIFTH dialect `failure_lines` could not spell, so the first sweep measured 0 across ten reddening blinds and a floor taken from it would have been a floor at nothing — 277 §4 exactly, one session later. The fix shipped is `E_FAIL` **and** `BLAST_UNREADABLE`, the refusal that stops a sixth being found by hand; it caught one nineteen lines later, in this same commit. 🔴 **SIX MEMBERS CRASHED RATHER THAN REDDENING AND FOUR WERE ONE LIVE DEFECT** — `selftest()` subscripting a detail bag straight off a reader whose own annotation permits the empty, 277 §1.2 one file over — and a FIFTH was on the live path, where `--assert-addon`'s printer died with `KeyError: 'version'` BEFORE its refusal line. `detail_or_refusal` is the third state, refused by name. Measured: 10 red / 6 crash before, 15 red / 2 crash after, blast 63 → 110. 🟢 **CLOSED WITH TWO INSTRUMENT ENTRIES, NOT ONE**, because both of this harness's axes are anchored to one target list and a member green under the gate command but caught by a different live command had nowhere to be declared — which is why this row sat for thirty sessions. `release_names.py` is gate `--selftest` / live `--assert-addon`; `release_names.py (--assert-addon)` is gate `--assert-addon` / live `--assert-map`, on the `path-cohort (compiled walk)` precedent. 19 of 33 members are now a target on some axis; 14 are declared under three measured reasons. The eleven reachable only by the release ritual carry `release-names-ritual-axis` |
| py-cohort-handoff-gate | DONE | internal | scripts/handoff_gate.py;scripts/instrument_gate.py | 247 | 277 | — | `handoff_gate.py` holds FORTY-NINE top-level `def`s and the coverage roster now asks for a target or a written reason for each; the two measured greens (`clone_tags`, `tree_state`) are the smallest part of it | 🟢 **CLOSED AT 277, AND THE ROW'S OWN COUNT WAS WRONG BY TWENTY-FIVE.** The file holds **seventy-four** top-level `def`s, not forty-nine — 247 measured the number, nothing ever printed it, and twenty-nine sessions carried it as a fact about the tree. That is 276's finding-to-carry arriving in the row 276 handed forward: a count is only ever a count of what the reader can spell, and here nobody was spelling it at all. 🔴 **EVERY MEMBER WAS BLINDED AND THE COMMAND RUN AGAINST IT BEFORE A LINE OF THE ROSTER WAS WRITTEN.** Fifty-six redden and are targets; twenty-two do not and are three reasons, not twenty-two — twelve dial a network, seven read an object store a `--depth 1` checkout does not have, three are the invocation. 🔴 **AND ONE OF THE TWENTY-TWO WAS MEASURED WRONG, BY CI.** `previous_main` reddens on a full clone and cannot on a `--depth 1` one — its only consumer runs `git rev-list old..new`, which fails there and returns the same documented refusal blinded or not. The first sweep filed it as a target and all three `host tests` legs refused it. The measurement was taken on one checkout and reported as a fact about the tree, which is this row's own subject happening to the session closing it. 🔴 **AND THE SWEEP FOUND TWO LIVE DEFECTS ON ITS FIRST PASS**, which is `terminology_gate.py` at 247 word for word. Seven members CRASHED the self-test rather than reddening it, all seven through two sites in `selftest()`: `all()` over an empty `ends` is `True`, so a parse failure sent the reader into the branch that asserts a number and then indexed `ends[0]`; and `len()` over a `read_measured` return its own annotation says may be `None`, so the message describing a failure could not be built in the case that failure is about. Blinding `_runs` reddened five claims and killed the command; with both fixed it reddens eighty-seven. 🔵 Six of the twenty-five greens turned out to be REACHABLE and are claims now — `check`, `open_tier`, `tier_trigger`, `measure`, `block_assetlib` and `gh_emit`, the file's two assemblies and the four readers they are made of. Every claim in that file proved a PART worked; none proved the assembly still CALLED it, which is 202 §4 one file over |
| mutlock-controls-unblindable | DONE | internal | scripts/mutation_lock_gate.py;scripts/instrument_gate.py | 247 | 279 | — | Five members of `mutation_lock_gate.py` — the three live control runners, the negative control and `_js_mutators` — are reachable by NEITHER axis: `--selftest` never calls them and the B:live axis is unavailable by the lock. The controls that prove every mutating gate refuses beside another cannot themselves be blinded | 🔵 **278's NEXT ASKED WHETHER 278 §2.3's SECOND-ENTRY SHAPE TRANSFERS HERE, AND IT DOES NOT.** That shape gives a file a second instrument entry over a second COMMAND, so a member green under the gate command can be a target of the other. Here the second command IS the locked one: `mutation_lock_gate.py` bare is what takes the lock, so a sweep that blinds it cannot also run it. The constraint is the lock, not the target list, and no arrangement of entries dissolves a lock. 🟢 **SO IT CLOSED ON THE WORK 247 PRICED, IN 247's OWN WORDS** — *a `--selftest` claim per control that asserts the PAIR each returns, so `(False, "")` fails on the empty reason rather than passing as "did not refuse"*. Ten claims drive the four control members over fixture scripts — a gate that refuses correctly, and the three ways refusing can be wrong: the right marker with the wrong exit, the right exit with no marker, and a hook that exits 0 while the lock is held — plus `_js_mutators` over a fixture directory holding one confined write and one loose one. No lock is taken and no real gate is spawned, which is what lets them live on the A:gate axis at all. 🔴 **THREE OF THE FIVE WERE HARD-WIRED TO ONE PATH**, so they were unreachable by a fixture even in principle; they take a parameter now whose default is the path that was hard-wired, which is 228's own move on `negative_control` repeated for its neighbours. Every live caller is byte-identical. All five left `NOT_A_TARGET`: 10 red / 0 crash across the five, 23 self-test cases where there were 12 where the row's own ninth cell had only a dash |
| js-late-register-in-body | DONE | internal | scripts/instrument_gate.py | 247 | 282 | — | 247 §3 moved the Python late hook's `atexit` registration to module scope so a target an axis never CALLS stops reading as a mutant that never LOADED; `LATE_HOOK` on the JavaScript side still registers from inside the member and no JS target has exposed it yet | 🟢 **CLOSED AT 282 — THIRTY-FIVE SESSIONS, AND THE FIX WAS ALREADY WRITTEN DOWN IN THE FILE, IN THE OTHER LANGUAGE.** `PY_LATE_REGISTER` has separated the two halves since 247 §3: the `atexit` printer is a MODULE-LEVEL statement so it runs on import, and the member's body carries only the counter. `LATE_HOOK` did both from inside the member, so a JavaScript target that LOADED PERFECTLY and was simply never CALLED on that axis printed no marker line at all and `not hook` filed it as a mutant that never loaded — *either the mutant NEVER LOADED … or this target is never called on this axis*, two failures in one bucket and only one of them about the instrument. 🔴 **THE ROW SAT BECAUSE `LATE_NOT_LOADED`'s CEILING OF ZERO CANNOT GO RED UNTIL THE ACCIDENT HAPPENS, AND ON THAT DAY IT NAMES THE WRONG CAUSE.** 247 found the Python instance the first time a target was added that one axis does not call — `queue_gate.py`'s `{SIG:ages}`, `{SIG:render}` and `{SIG:_table}`, three mutants that had loaded, run and behaved exactly as the table says they should. Waiting for the same accident in the other language is waiting to be told a wrong answer. 🔵 **THE ONE DELIBERATE DIVERGENCE FROM THE PYTHON PORT IS A FACT ABOUT THE TWO LANGUAGES**: a Python `def` has a column-zero line above it for the registration to sit on, and a JS member may be a nested object property or an arrow inside an expression with no statement position anywhere near it. `js_late_register` puts it at the top of the module, after any shebang — a statement position in every case, executed at import in every case, which is the only property the registration needs. **No hook line still means no module**, which is the property 247 was protecting: a file that fails to parse or throws at import never reaches this line either |
| cli-version-unaskable | DONE | user | — | 248 | 248 | — | `--version`, `-v`, `-V` and `version` all started the MCP server, so the installed binary could not be asked its version at all — and the bug-report template routed reporters to `plugin.cfg` to find the number instead | — |
| cli-unknown-arg-swallowed | DONE | user | — | 248 | 248 | — | Any unrecognized argument fell through to the stdio server: `init --porject ~/game` parsed the typo, ignored it, and reported `no project.godot at ~` — a true sentence about a path the user never named | — |
| cli-subcommand-help-unread | DONE | user | — | 248 | 248 | — | `init --help` reached the project check and `doctor --help` ran the checks; only `tools` answered, from its own third copy of the text | — |
| bridge-hint-reopen-blind | DONE | user | — | 248 | 248 | — | The editor bridge's remedy asked *is the editor open with the plugin enabled?* — the one question the reopen-trap victim answers YES to while the bridge stays dark | — |
| tool-error-sweep-unrun | DONE | user | — | 248 | 254 | — | The 289 registered tools' failure paths had never been swept for whether the message tells a user what to do next; eight were sampled at 248 and all eight were good, which is a sample of eight out of 216 message templates. 🟢 MEASURED AT 254 AND THE SAMPLE WAS NOT REPRESENTATIVE: 525 `_err(..)` sites across the two engine planes emit 216 distinct templates, and 451 of those sites named NO next action — including the three most-emitted messages in the addon, `No scene is open` at 90 sites, `Node not found: %s` at 53 and `ResourceSaver.save() returned %d` at 35. Closed by attaching the remedy to the error CODE rather than to the message: 216 templates would have been 216 hand-written sentences to keep true, the code vocabulary is 60 rows over two planes, it is already machine-owned, and it is the axis the message varies within. `error_remedies.gd` holds one table per plane because the planes answer differently — `bad_path` is `scene_get_tree` in the editor and `runtime_get_tree` in the game — `_err` attaches it at the single point every failure passes through, and the host's five `Partial<BridgeError>` renderers append it through one `remedyClause`. Check 28 reads the join in four directions: a raised code with no row, a row no code raises, a remedy naming a tool `registerTool` does not register, and either side of the wire dropping the attach. The pre-auth `unauthorized` and `bad_json` denials are outside the population by construction rather than by roster — they are built as dict literals, not raised through `_err`, because the no-echo path must tell an unauthenticated peer nothing | — |
| first-run-editor-half-unwalked | DONE | user | — | 248 | 249 | — | 248's first-run pass covered the pre-Godot surface only (the container has no Godot), and every defect it found was in that half. Install → open the editor → a tool actually answering has never been walked end to end by anything but somebody who already knew it worked | — |
| serverinfo-version-literal | DONE | user | host/src/index.ts;host/src/version.ts;scripts/contract_check.py | 248 | 276 | — | `index.ts` writes the server's advertised version as a literal where `packageVersion()` exists. Check 14's roster stops it going stale, which is why it has no user symptom today and why it is still the exact hazard `version.ts` was written to document | — 🟢 **CLOSED AT 276, AND THE ROW'S OWN ARGUMENT IS WHAT CLOSED IT.** `version.ts` says in its docstring that *the best outcome is not a gated literal but no literal at all — so anything that merely needs to REPORT the version should call this instead of being added to the roster*, and check 14's roster entry was the workaround the row was complaining about. The `McpServer` serverInfo — the version every connected client is told at `initialize` — calls `packageVersion()` now, the roster entry is deleted, and a release bump is **four** files rather than five. 🔴 **AND THE GUARD IS WIDER THAN THE ROW, BECAUSE THE INCIDENT `version.ts` DOCUMENTS IS NOT THIS FIELD.** `lsp.ts` and `cslsp.ts` announced `clientInfo: { version: "0.2.0" }` to Godot's language server and to OmniSharp from the initial commit to 1.26.0 — twenty-odd releases — and neither was ever on check 14's roster. Check 14 now walks all of `host/src` and refuses ANY `{ name, version: "…" }` literal, with the files-scanned count floored beside it because an empty offence list is also what a walk over the wrong directory looks like. Positive control `14.infoliteral` puts the exact literal back. |
| orphan-ceiling-headroom | DONE | internal | host/scripts/tautology_gate.mjs;host/scripts/tautology_gate.selftest.mjs | 248 | 285 | — | 248 taught `enclosingTest` to spell a template-literal test name and the live orphan count fell 69 → 42 against a ceiling of 46. The ceiling was NOT lowered in the release commit because the self-test pins the digit absolutely in ten places — four sessions of headroom is the licence `tautology_gate.mjs` says out loud it will not issue | — 🔵 **RE-READ AND RE-PRICED AT 276, AND THE HEADROOM HAS SHRUNK RATHER THAN GROWN.** `TAUT_ATTRIBUTED units=2220/1200 claims=4725/2500 orphan=43/46` — the live orphan count has drifted 42 → 43 in the twenty-seven sessions since the row was opened, so what 248 called four sessions of headroom is now **three orphans** of it. The row's argument is unchanged and still correct: lowering a FLOOR-shaped ceiling costs ten absolute pins in the self-test (184 §7), and it must not be done on a release commit. What has changed is the direction of the pressure — the number is walking toward the ceiling rather than away from it, so the next session that touches `tautology_gate.mjs` for any other reason is the cheap moment to take it, and a session that waits for the number to reach 46 will be paying the ten pins under a red gate instead of a green one. 🔵 **RE-PRICED AT 278 AND DELIBERATELY NOT TAKEN.** Live orphan is **43** against the ceiling of 46, unmoved since 276 — three sessions of headroom, not four. 278 did not open `tautology_gate.mjs` for any other reason, which is the condition this row has carried since 248, and lowering a FLOOR-shaped ceiling costs ten absolute pins in a self-test (184 §7) on a session already shipping two Tier-1 closes, two instrument entries, two new gate refusals and a new contract check. Named in 278's NEXT and not silently dropped 🟢 **CLOSED AT 285, PINNED AT THE LIVE VALUE, AND THE HEADROOM WAS NOT SPENT GETTING THERE.** The row's condition has been *the next session that opens `tautology_gate.mjs` for another reason* since 248, and 285 opened it to roster a sixth classifier under `NO_CLAIMS_EXPECTED` — `difference_field_gate.mjs`, the same shape as the five gates already there. 🔴 **AND THE SESSION EARNED THE READING BEFORE IT TOOK THE ROW.** 285's own new self-test first funnelled thirty claims through one `claim()` helper, so the classifier saw ONE site it could not attribute and the count went **44 → 45** — half the remaining headroom, spent by the file written to keep a reader honest. That is 171 §2's second class, *a file whose idiom the finder cannot read*. Written out, thirty claims are thirty attributed sites and the count came back to **44**. 🔵 **SO 44 IS A MEASUREMENT AND NOT A ROUNDING**, and the ceiling is set to it rather than above it: `ORPHAN_CEILING = 44` measured `4923 sites - 4879 attributed` on 2026-08-27, exactly the convention 191 used at 508/509 and 193 at 147/148, and exactly what line 173 of that file means by *headroom here is a licence to add orphans nobody reads*. The ten absolute pins the row priced are moved with it — the *sits exactly ON the ceiling* fixture is `3744 - 3700` and the rise that reddens is `3745`, printing `TAUT_ORPHAN_RISE 45 > 44`. `TAUT_SELFTEST ok every claim held (175 claim(s) ran)`; live `orphan=44/44`, `TAUT_GATE ok`. 🔴 **AND IT IS NOT A RELEASE COMMIT**, which is the other half of what 248, 276 and 278 each declined on |
| sweep-evidence-depth-sensitive | DONE | internal | scripts/handoff_gate.py | 277 | 278 | — | A target whose redness depends on the CHECKOUT rather than on the instrument is invisible to every local run, and CI is the only reader of that difference | 🔴 **277 NAMED THIS ROW IN ITS §6 NEXT AND NEVER WROTE IT INTO THIS FILE**, under its own standing rule *edit `QUEUE.md`, not §6*. Nothing refuses a NEXT item that names no row, so a session's own recommendation was carried in prose exactly like the ages this table was built to stop carrying. Opened at 278 against 277 and closed in the same session, which is the honest shape: the work is 277's. 🔴 **PRICED FIRST, AS 277 INSTRUCTED, AND THE PRICE CHANGED THE ANSWER.** A `--depth 1` clone of this repository costs **0.39 s**, so the *second sweep against a shallow clone* 277 called expensive is affordable and the cheap shape it recommended is not the only one available. 🔴 **AND RUNNING IT FOUND A SHARPER CLASS THAN THE ROW'S.** `release_names.py --assert-addon` is green on BOTH machines and answers a DIFFERENT QUESTION on each: the full clone reports the addon stamped at `e35dc3e7`, a `--depth 1` clone of the same commit reports HEAD with *0 file(s) moved over 0 commit(s)* — vacuously true against a store with nothing to move across. Both exit 0, both print a 🟢. So the hazard is not *reads git*, it is **answers instead of refusing**: 277's seven object-store rows are the SAFE kind, where a shallow store produces a documented refusal (271). 🔵 **AND CI IS ALREADY RIGHT, WHICH IS THE POINT.** `contract-check` carries `fetch-depth: 0` and a comment naming check 4 as the reason; every other job in all three workflows is shallow. That invariant lived in a YAML comment with nothing joining it to the reader that needs it. `DEPTH_REQUIRED` in `handoff_gate.py` is the join, refused in both directions — a required command in a shallow job, and a roster row no job runs — with fixtures for the defect, the healthy case and the exemption that outlived its subject |
| release-names-ritual-axis | DONE | internal | scripts/release_names.py;.github/workflows/ci.yml | 278 | 304 | — | Eleven readers in `release_names.py` are reached only by the release ritual, and no tree CI checks out can run it | 🔴 **MEASURED AT 278, AS THE RESIDUE OF `release-names-twelve-readers`.** `shipped_corpus`, `released_block`, the four window readers, the three tag readers, `addon_moved_since` and `release_commit` are called only by `main()`'s no-flag path — the cut itself — and that path refuses before it reaches them on any tree without a released block for the version being cut, which is every tree CI ever checks out. All eleven measured green under the blind against BOTH commands CI does run. 🔵 **AND A PATH EXISTS, WHICH IS WHY THIS IS A ROW AND NOT A DEAD END.** `--head-ref` replays a PAST cut and 278 drove one green — 1.82.0 against 1.81.0 at that tag, claiming MAJOR — exercising checks 1, 2, 4 and 8 and exiting 0. Not adopted, because it pins two tag literals and a bump that are facts about one historical window, and a third axis built on those is a claim about the machine in a different costume (235 §6.3). The work is to find the invocation that is a claim about the TREE 🔴 **PRICED AT 280, BY THE INCIDENT.** `release-1820-bump-under-wire` resolved to this row: check 8 is one of the eleven, it existed at 1.82.0, it refuses 1.82.0, and session 270's replay list ran `release_names.py --selftest` and `--assert-addon` and nothing else — so the invocation that runs checks 1, 2 and 8 was never made at the cut. **A MAJOR shipped under a MINOR name because the reader that refuses exactly that was reachable only by whoever remembered to type it.** 279 remembered; 270 did not. 🟢 280 bound ONE of the eleven — check 8's verdict is a claim the block must carry and the gate compares (`release_verdict_problems`), so the omission is now a refusal at the next session's pickup rather than a discovery ten sessions later. **The other ten are still reachable only by the ritual**, and that is what is left of this row. 🟢 **CLOSED AT 304, AND THE ARGUMENT THAT BLOCKED IT FOR FOUR SESSIONS WAS MEASURABLY FALSE.** 300 §3.1 said three of the ritual's four arguments derive and the fourth does not — *`--bump` is the human claim check 8 tests, so deriving it would compare the tree to itself (235 §6.3)*, and it is *declared in NO tracked file*. 🔴 **IT IS DECLARED IN `host/package.json`: IT IS THE VERSION NUMBER.** A human chose `1.84.0` over `1.83.1` and that choice IS the MINOR claim; the word and the number are one claim spelled twice, and semver is the function between them. Nothing in `bump_of` reads a diff, a tarball or a classifier, so check 8 still compares a human's claim against a measurement that has never seen it. 🔴 **AND 235 §6.3 SAYS SOMETHING ELSE ENTIRELY** — *a self-test claim must be about the READER, not the machine* — which has no opinion about deriving a bump from a version pair. 303 §5.1 learned that a PRICE inherited in a handoff is a hypothesis; a CITATION inherited in one is the same thing. 🟢 `--assert-ritual` derives all four, pins the window to `release_commit(version)` so it is about the CUT rather than about work released after it, and runs the SAME live half — an assignment to `a`, not a second implementation (203 §2). All eleven readers are reached on every push from the `build` job, which is the one with a current `dist/` for `wire_diff.mjs` (4s). 🔴 **ONE THING IS REPORTED RATHER THAN ENFORCED, WITH A TABLE BEHIND THE RULE**: off the release commit check 8's SIZING verdict spans work released after the cut, so `C8_BENEATH` there is a floor on the NEXT release and not a verdict on the last; every other check-8 refusal is about the runner and stays red. `c8_enforced` is pure and driven in both directions, because the first draft downgraded them all and would have hidden a classifier that never ran. 🟢 **SEVEN MUTATIONS DRIVEN, AND THE SEVENTH FOUND A HOLE IN THE FIXTURE**: loosening `previous_tag`'s `<` to `<=` changed no answer, because not one fixture row's tag list contained the version it was asked about — which the LIVE tree's always does. The row was added and the mutation now reddens five claims. |
| release-1820-bump-under-wire | DONE | user | CHANGELOG.md;host/package.json | 278 | 280 | — | Replaying 1.82.0's own window says the wire did MAJOR, and 1.82.0 was cut as a MINOR | 🔴 **FOUND SIDEWAYS AT 278**, while pricing a third axis for `release-names-twelve-readers` rather than while looking at releases. `release_names.py --version 1.82.0 --previous 1.81.0 --date 2026-08-19 --head-ref v1.82.0` refuses `C8_BENEATH` at `--bump MINOR` and passes at `--bump MAJOR`: *the bump is beneath the wire*, which is 1.74.0's shape exactly (226 §2) and the failure this check was written for. 🔵 **AND IT IS NOT YET A DEFECT, WHICH IS WHY IT IS SCHEDULED RATHER THAN OPEN.** `--head-ref` is documented AUDIT ONLY and moves the window while leaving the corpus at the WORKING tree, so this reading is not the one the cut took. 276 also added check 8's toolchain arm AFTER 1.82.0 shipped, and check 8 now floors the bump at the worse of two verdicts. Either could explain it and neither has been measured. **Do not rename a shipped release on this evidence** — re-derive the classifier's answer at the real release commit first, and if the cut was genuinely misnamed the question is what a caller was told, not what the tag says 🔴 **RE-DERIVED AT 280 AT THE REAL RELEASE COMMIT, AND BOTH ALIBIS ARE FALSE. 1.82.0 IS A MAJOR CUT AS A MINOR.** A worktree at `v1.82.0`, `npm ci`, `npm run build`, corpus and window both at `e35dc3e`, and **no `--head-ref`**: `WIRE_VERDICT MAJOR`, `major 7 · minor 4 · patch 0`, both surfaces, `C8_BENEATH` at `--bump MINOR`. And 276's toolchain arm was not involved either — at `v1.82.0`, `release_names.py` contains the string `toolchain` **zero** times and `wire_diff.mjs` never printed `WIRE_TOOLCHAIN`. **The cut's own classifier, at the cut's own commit, refuses the cut.** The seven MAJORs are one change: `value` on `project_set_setting`, `node_set_property`, `resource_set_property`, `anim_insert_key`, `shadermaterial_set_param`, `runtime_set_property` and `runtime_await_condition` went from `type: unknown` — unconstrained AND, because `z.any()` answers true to zod's `isOptional()`, published outside `required` — to a REQUIRED six-variant `anyOf`. 279 tools -> 279; nothing added or removed. A client that omitted it was obeying our published schema and is refused from 1.82.0 on. 🔴 **AND THE CAUSE IS NOT THE CLASSIFIER, IT IS THAT NOBODY RAN IT**: session 270's replay list ran `--selftest` and `--assert-addon`, and the full `--version/--previous/--date/--bump` invocation — checks 1, 2 and 8 — was never run at the cut. 🟢 **WHAT SHIPPED**: the CHANGELOG's 1.82.0 block carries a dated correction naming the seven tools and saying plainly that the number understates the wire (the release is published and cannot be renamed; what is corrected is the claim, not the tag), and `release_verdict_problems` binds the verdict to the commit — a block that CUTS a release must carry `wire <V>` and `toolchain <V>`, the bump is DERIVED from the two `host / addon` rows and never typed, and the comparison is `release_names.wire_floor` **imported** so the gate's rule and the ritual's rule cannot become two opinions. Driven over 1.82.0 itself as row 1 of its own refusal proof. |
| doctor-require-live-overreaches | DONE | user | — | 249 | 252 | — | `--require-live` requires the RUNTIME bridge, so the verification step `docs/USER_GUIDE.md` §3.0 and `README.md` both tell a new user to run once the editor is open exits 1 on a correct install; there is no editor-only assertion level | — |
| run-project-returns-before-bridge | DONE | user | — | 249 | 257 | — | `godot_run_project` returns `running: true` 0.5–3.2s before 9081 binds (measured 3213/2788/566ms cold), and every `runtime_*` call in that window answers *Is the project running?* — it is, and the tool returned its pid; `runtime_await_condition`, the one waiting tool, fails the same way in 2ms with `timeout_ms` 15000 set. 🟢 CLOSED AT 257 BY TWO OF 249's THREE PRICED REMEDIES. A — the launch waits and says so: `readiness.ts` lifts `waitReady` out of `PeerRegistry`, where it has been proven against real peers since 1.21.0 and where `allocatePorts`'s own comment named the default port's missing wait as a property of the design for eight sessions; `godot_run_project` and its unrostered twin `godot_run_managed` now call it and report `bridge_ready`, `bridge_wait_ms` and `bridge_note`, with `wait_timeout_ms: 0` as the documented opt-out that reads as *not waited* rather than *waited and lost*. C — the waiting tool waits for the connection: `runtime_await_condition` retries past `bridge_unavailable` until its OWN deadline and still fails fast on every other code, because those are the game answering and retrying an answer only makes it slower. B — retry inside the 27 runtime tools' shared `call()` — was priced and REJECTED: it makes the honest failure slow, and leaves `running: true` false at the moment it is printed. Check 30 finds launchers by the SPAWN and never by a roster, which is how the twin this row did not name is now in the population | — |
| gated-tool-reads-as-missing | DONE | user | — | 249 | 250 | — | The 13 tools the secure default withholds answer `Tool X not found` — naming no policy, no `--trust full`, no `godot://capabilities` — so a deliberate configuration is indistinguishable from a missing feature to the assistant that reports it onward; §7's quick start names three of them | — |
| privileged-group-copy-names-deleted-group | DONE | user | — | 250 | 250 | — | Found in the file the row above was fixed in, and the same defect one layer up: five shipped strings told a user to type `network`, a capability group deleted from `capabilities.ts`'s own header. `init` printed `--privileged-groups code-execution,network` as the remedy and then answered its own suggested command with `ignoring unknown trust group(s): network`; the always-on `godot://capabilities` resource announced *Two higher-trust tool groups* over one, and offered the same dead token; `doctor`'s hint carried it; and `init` named `backend_*` among the dropped tools when both are deliberately unprivileged. Every count and token is now DERIVED from `CAPABILITY_GROUPS`, and a test reads the shipped source and asks the real parser whether each offered token resolves | — |
| init-emits-unquoted-shell-line | DONE | user | — | 249 | 251 | — | `init`'s `claude mcp add` line is not shell-quoted, so a project path containing a space silently misparses into a truncated `--env` and a stray argument; the JSON block beside it is correct because JSON quotes for you | — |
| catalog-heading-destructive-unread | DONE | user | — | 252 | 252 | — | Found by asking 251's own question — *what does this gate NOT compare?* — of the file 251 had just gated. Check 4 and 4b read the Tool Index; nothing read the per-tool SECTION HEADING, which carries the same `destructive` predicate as free prose and is the page a reader actually lands on for one tool. It disagreed with `annotations.ts` on 44 of 289 sections: 35 silent about a tool the wire calls destructive — `tilemap_clear`, `godot_stop`, `vcs_restore`, `vcs_stash`, every `asset_gen_*` — and 9 claiming it without the annotation. The marker is now the index's own ✔, on the name so it binds in the three combined headings, and check 4c reads it in both directions plus refuses a registered tool with no section | — |
| catalog-plane-column-unread | DONE | user | — | 251 | 253 | — | Split out of `d10-capability-matrix` at 251, which gated the `Destructive` column and left this one measured but unjoined. The catalog's `Plane` cell carried free-form values (`A / Editor`, `B / Process`, `C / Runtime`, `J / Host` …) and the code's toolset ids are a different vocabulary, so nothing compared them. What existed was cardinality only: `toolset_sizes()` resolved a toolset id by regex-COUNTING `registerTool(` sites and captured no names, so a tool moved from `editor` to `runtime` alongside a new `editor` tool left every count in the tree green and every `Plane` cell stale. 🟢 CLOSED AT 253 BY GENERATING THE ATOM AND NOT BY NORMALISING THE PROSE. `toolset_members()` captures the names the counter was already matching and `toolset_sizes()` is now `len()` over it — one reader, so the size a claim is checked against and the membership a cell is checked against cannot disagree. The toolset id is written into every `Plane` cell as a backticked atom beside the group letter, which is 252's shape and is also the only shape that WORKS here: the two vocabularies are different shapes, not different spellings — one `editor` toolset spans nine group letters and group M is two toolsets, so no normalisation table could have been a function in either direction. Check 4d reads the atom in four directions — drift, absent, undefined id, and the registry compared to the registration walk — and that last one is the assertion the column was never the point of: a `tools/*.ts` module imported by no toolset ships a tool that is catalogued, annotated and schema-checked, and that no `BREAKPOINT_TOOLSETS` selection can load | — |
| tileset-set-collision-unannotated | DONE | user | — | 251 | 252 | — | The one row of 251's 21 where the DOC may have been right and the code wrong. Eight tools carried ✔ in the catalog and are not in `annotations.ts`'s `DESTRUCTIVE`; seven are plainly additive (`audio_bus_add`, `inputmap_add_event`, `runtime_node_add`, `tileset_add_tile` …) and the ✔ was over-broad. `tileset_set_tile_collision` is the exception: it OVERWRITES a tile's collision shape, which is `DESTRUCTIVE`'s own definition — *may overwrite or discard state the caller did not supply*. 251 moved the document and not the wire, deliberately: an annotation change alters what a client shows a human before it runs the tool, and that is a behaviour change nobody asked for riding along inside a copy fix (250 §1.2). Decide it on its own | — |
| doctor-passes-under-four-reds | DONE | user | — | 249 | 252 | — | Without `--require-live` `doctor` prints four ✗ bridges and then `All required checks passed.` — both halves defensible, together a contradiction in the tool whose job is telling the user whether they are okay | — |
| tool-results-outside-token-cost | DONE | user | — | 249 | 257 | — | One `gd_completion` returns 342,116 B — 99.6% of the entire 279-tool `tools/list` surface — and `token-cost.mjs` projects onto the catalogue only, so the instrument that governs client cost against a 366,000-byte floor cannot see the largest single expenditure and reports ok. 🟢 CLOSED AT 257 IN THREE PARTS, BECAUSE THE ROW IS TWO DEFECTS AND A SILENCE. The COST: `gd_completion` and its twin `cs_completion` now take `max_results` and return `truncated`, the knowledge family's shipped idiom at the knowledge family's number — completion is the one language-server verb whose result size is a function of PROJECT scope rather than of the cursor, which is why hover and definition are not in the population. The BLINDNESS: `result-cost.ts` meters `JSON.stringify(result)` at the one point that knows the tool name and the result together — the `tools/call` dispatcher, wrapped the way `applyDroppedToolRefusal` already wraps it — off unless `BREAKPOINT_RESULT_COST` names a file, and `token-cost.mjs --results <file>` reads it back against a ceiling on the WORST SINGLE result, because that is what a client pays per call with no catalogue to amortise it against. A missing log REFUSES rather than passing, which is the one thing that must never look like agreement here. The SILENCE: `token-cost.mjs` now prints its own scope beside its verdict — 256 §5.3's standing rule applied to the reader that prompted it | — |
| wire-diff-toolchain-blind | DONE | internal | host/scripts/wire_diff.mjs;host/scripts/wire_diff.selftest.mjs;scripts/release_names.py | 256 | 276 | — | `wire_diff.mjs` builds the baseline ref against the CURRENT `node_modules` on purpose — *one toolchain, two sources, is the only comparison that isolates OUR change* — so a wire change driven by a DEPENDENCY cancels on both sides by construction. Measured at 256 on the 1.75.0 cut: check 8 read MINOR off 254's two source edits while the sixteen output keys that are the release's own headline were invisible to it, and the bump was decided by hand off the emitted surface on both majors. The comment is right about what it isolates and nothing says what it therefore cannot see. 255 §4.4 asks the same question one level down — nothing reads the installed validator's version against the surface it produces — and this is the reader that would answer both: a second baseline built against the LOCKFILE'S OWN resolution, so the two comparisons together separate our change from the toolchain's instead of silencing one of them | — 🟢 **CLOSED AT 276 WITH THE SECOND BASELINE THIS ROW ASKED FOR, AND WITH THE FIRST MEASUREMENT ANYBODY HAS TAKEN OF WHAT IT WAS HIDING.** `buildBaseline` grew an `ownDeps` arm that runs `npm ci` inside the worktree, so the baseline compiles against its OWN lockfile; classifying that surface against the baseline-with-today's-`node_modules` isolates the DEPENDENCY exactly as the existing arm isolates our source. Both are printed, and check 8 floors the bump at the WORSE of the two — a release is a statement about the public API whichever of the two moved it. 🔴 **The atom is the RESOLUTION and never the lockfile**: the root package's own version moves at every cut, so a file-level diff would report a toolchain change at every release and therefore be read at none of them; the declared ranges are read beside the resolved versions, because a caret widened to a tilde resolves the same today and differently tomorrow. `C8_TOOLCHAIN_UNREAD` refuses a classifier that answered one half, and the cross-language round trip is asserted for the second printed line as well as the first. 🟢 **MEASURED `v1.79.0 -> HEAD`: five dependencies moved — the SDK among them, and a MAJOR of `@hono/node-server` — and the wire did not move at all.** Across every other reachable tag the resolved set is identical. The blind was real and, in the windows this repository can still reach, it had nothing to hide — which is a fact about this window rather than about the reader, and is exactly what could not be said before. |
| csdap-plane-unguarded | DONE | user | — | 262 | 263 | — | 🔴 **THE C# DEBUGGING PLANE HAS NEITHER GUARD 262 JUST WIRED ON THE GDSCRIPT ONE.** `host/src/tools/csdap.ts` carries no `requireSession` at all and `host/src/csdap.ts` has no `hasSession` to consult, so every `cs_dbg_*` reader is where `dbg_*` was before 259 AND before 262: a `cs_dbg_stack_trace` with nothing launched, or with a launched program that is running, answers from whatever netcoredbg says rather than from what the host knows. Not measured live — netcoredbg is not installed on the machine that walked this, and a claim about a plane nobody ran is exactly what 261 §1 was about — so this is opened rather than fixed blind. The GDScript half is the template: a `get isStopped()` on the client, `requireSession` / `requireStopped` in the registrar raised before the adapter round trip, and a `CS_DAP_NOTSTOPPED` cohort in `cs-dap-plane.integration.mjs` 🟢 **CLOSED AT 263, MEASURED FIRST.** The premise that deferred it was wrong in one word: netcoredbg is not installed on his Mac, but it has been installed on this repository's own `csharp-plane` runner since session 158 and `cs-dap-plane.integration.mjs` has spawned it on every leg since — the plane nobody ran was running in CI the whole time. Reproduced that pair locally (dotnet 8 SDK + netcoredbg 3.2.0-1092, the pinned version) and drove the whole reader surface. **With NO session: seven of nine answered the adapter's raw hex** — `Failed command 'stackTrace' : 0x80004005` (E_FAIL) and `0x80070057` (E_INVALIDARG) — `cs_dbg_set_variable` said *the debug adapter reported a failure with no message*, `cs_dbg_watch` answered `isError:false` with a hex code in every entry, and only `cs_dbg_restart` refused, **for the same accidental reason it was the sole exception on the GDScript plane: it happens to read `lastStartMode`**. **With the session LIVE and the program RUNNING:** the same, plus `cs_dbg_continue` waiting the full 15,000 ms to answer `{"state":"running"}` and `cs_dbg_restart` 30,054 ms — about 45 s of adapter round trips for a question the client's own state answers in none. 🔴 AND A `hasSession` BUILT ON `state` ALONE WOULD HAVE BEEN UNLOCKED BY THE FIRST CALL IT REFUSED: measured, `cs_dbg_continue` on a never-launched client left `state = "running"` behind, because `resume()` assigns it optimistically — so the port needed `sessionStarted`, the field, not just the getter. Third find, same family one tool over: `cs_dbg_set_exception_breakpoints` with no session read a null `capabilities` and told the caller *the connected C# debug adapter advertises no exceptionBreakpointFilters* — about an adapter that advertises `all` and `user-unhandled`, which the cohort in the same file asserts on every CI leg, and that nothing had spoken to. Closed with `sessionStarted`/`hasSession`/`isStopped` on the client, `requireSession` + `requireStopped` above the round trip on seven readers, `cs_dbg_watch` session-guarded but deliberately still managing its set while running (each entry carrying the state it read instead of a hex code), the exception-filter message split, six unit tests, and a live `CS_DAP_NOTSTOPPED` cohort of 53 claims asserting each refusal three ways — that it refuses, that it names the state it READ, and that it cost under 2 s, which is the one claim the text cannot carry. **Fourteen existing unit tests changed and they are the finding too**: each launched a session and asked a RUNNING program for a frame, and passed because the tools answered — 262 changed fifteen of them one plane over | — |
| dbg-buffered-bp-bind-once | KILLED | user | — | 262 | 266 | — | 🟡 **A BUFFERED BREAKPOINT FAILED TO BIND ONCE, AND FIVE ATTEMPTS TO REPRODUCE IT ALL BOUND.** Measured during 262's §10 B walk against a live 4.7: `dbg_set_breakpoints res://main.gd [15]` then `dbg_launch` answered `buffered:true` / `state:running` as always, and the game ran the breakpointed line without stopping — `runtime_call_method` returned `20` in 16ms where every other run timed out at the stop. Five subsequent runs in the same and a fresh editor all bound. Recorded as a single unreproduced observation and NOT as a defect claim: the handshake applies buffered breakpoints between `initialized` and `configurationDone`, which is the correct order, so if there is a race it is upstream of that ordering. 🟢 **KILLED AT 266 AFTER THE LOOP WAS ACTUALLY RUN.** 300 launches against a real netcoredbg 3.2.0-1092 over a throwaway .NET console app, one fresh adapter per iteration, breakpoint buffered BEFORE the session existed: **bound 300, unbound 0, errored 0**, and the `initialized` event arrived on every single one. Median 331 ms per launch, so the whole measurement is under two minutes — four sessions of prose about a loop nobody ran. The observation is closed as unreproducible rather than carried, per 265's *either run the loop or kill the row*. 🔴 **AND THE LOOP FOUND SOMETHING THE OBSERVATION WAS NOT** — see `dap-initialized-wait-is-a-timeout`, which is a real reachable ordering hazard on BOTH planes and is NOT what 262 saw | 🟢 Killed as UNREPRODUCIBLE on a measurement, not on a judgement: 300 buffered launches against a real netcoredbg bound 300 times with `initialized` present on every one. Reopen only on a fresh observation with a capture, not on a recollection — and read `dap-initialized-wait-is-a-timeout` first, which is the ordering hazard the loop's harness DID expose |
| dbg-not-stopped-hole | DONE | user | — | 262 | 262 | — | 🔴 **NINE `dbg_*` TOOLS ANSWERED AS IF THE PROGRAM WERE STOPPED WHEN IT WAS MERELY LAUNCHED.** `requireSession` asks whether a session EXISTS; every reader then asked the adapter for a frame. Measured against a live 4.7 with a session live and the program running: `{"frames":[]}` in 4ms, `{"scopes":[]}` in 8ms, a fabricated per-expression `error:"timeout"` after 5s, `dbg_evaluate` 5s, `dbg_set_variable` 8s blaming the reader's engine, `dbg_step` and `dbg_continue` 15s each ending in `{"state":"running"}` — eight answers to one question, ~48s of round trips for something `dap.isStopped` answers in none | Closed by `requireStopped`, raised before the adapter round trip on eight tools, plus the session guard `dbg_goto` and `dbg_data_breakpoints` never had. `dbg_watch` deliberately still manages its set. Pinned by four unit tests and the live `GD_DAP_NOTSTOPPED` cohort |
| guide-recipe-b-deadlocks | DONE | user | — | 262 | 262 | — | 🔴 **USER_GUIDE §10 B STEP 5 CANNOT SUCCEED WHEN IT WORKS.** The recipe's documented trigger is `runtime_call_method`, the addon services `runtime_*` from `_process`, and a breakpoint inside the called method halts the frame that owes the reply — so the caller gets `Bridge request 'runtime.call_method' timed out after 15000ms` and no cause, every time the breakpoint is hit. Measured: the stop's stack is `compute` over `_call_method` → `_dispatch` → `_handle_line` → `_drain_lines` → `_process` | Closed both ways: the recipe now names the interaction and offers `runtime_inject_input` as the trigger without it, and the timeout carries the next action when this host's own debugger is what stopped the game — the first instance of the eight-session `host-failures-carry-no-remedy` item, answered for one measured case rather than swept |
| setvariable-blames-the-build | DONE | user | — | 262 | 262 | — | 🟡 `dbg_set_variable`'s only reachable message read *"this Godot build (e.g. 4.3) does not implement setVariable"*. Measured twice at a REAL stop on **4.7**: advertised, unanswered. The sentence told a current-build user they were behind, and there is nothing to upgrade to | Closed by naming the adapter instead of the reader's version, and §11 now says plainly that the tool does not work on any current build |
| queue-age-can-be-negative | DONE | internal | scripts/queue_gate.py | 256 | 271 | — | `age` is DERIVED as `head - opened` and nothing refuses `opened > head`. 255 added `required-any-reachability` with `opened 255` and did not move `QUEUE_HEAD` off 254, so the row rendered at **-1 session(s)** through `--render`, `--ages` and `QUEUE_AGE_CEILING` — every consumer read a negative age as a number and none of them asked whether it was possible. The ceiling is a `>` test so a negative passes it, and the internal tier has no ceiling at all, which is why nothing went red. This file's own header is the argument: the ages are derived BECAUSE a typed number nobody rechecks drifts, and a derived number nobody bounds is the same defect with a computation in front of it |  🟢 **CLOSED AT 271, IN `parse` AND NOT IN `check`, WHICH IS WHAT THE ROW ACTUALLY ASKED FOR.** The finding was never that one claim mis-fired — it was that THREE consumers read the impossible number and passed it on. `parse` is the one function `check`, `--render` and `--ages` all call and all three already refuse on what it returns, so the bound is one none of them can be written without. `QUEUE_AGE_DOMAIN` refuses `opened` or `closed` after the head and `closed` before `opened`; `target` is exempt and says so, because a schedule is a promise about a session that has not arrived and `QUEUE_SCHEDULE_HONOURED` is the claim with an opinion about it. 🔴 **IT FIRED ON THIS FILE'S OWN TEST CORPUS ON ITS FIRST RUN**: three self-test fixtures carried a row opened at 246 against a head of 240, one of them also closed before it was opened. Corrected in the same commit |
| screenshot-captures-a-hidden-viewport | DONE | user | — | 261 | 261 | — | `screenshot_editor` returns a frozen full-size frame of a tab that is not on screen, as a successful capture | 🔴 MEASURED AT 261 AGAINST A LIVE GODOT 4.7, DRIVING THE PUBLISHED `breakpoint-mcp@1.76.0` AS AN MCP CLIENT. Godot collapses a main-screen tab's viewport to 2x2 only until that tab's FIRST VISIT. After one visit it keeps full size while hidden and stops being drawn to, so the capture succeeds, reports its real dimensions, and shows the scene as it was when the tab was last on screen. The four captures: tab 2D capture 2d → 2214x1809 md5 4dcacd9ac2ce; tab 3D capture 2d → the same bytes; add a 900x700 magenta `ColorRect` to the scene with the 3D tab active → STILL the same bytes; switch back to 2D and capture → md5 b2075a8606e7, the rect is there. 🔴 THE GUARD WRITTEN FOR THIS CANNOT SEE IT. `viewport_not_rendered` compares the returned image against `MIN_RENDERED_VIEWPORT_PX` and the SIZE is the one thing that does not change — so it protects exactly the never-visited tab, i.e. once per editor session, and `USER_GUIDE` §10 A step 5 is itself the visit that disarms it. The unit test pinning the guard feeds a mocked 2x2 through a fake bridge, which is a true test of the branch and structurally unable to observe the engine the branch is a model of; it has been green over this since 1.30.0. 🟢 CLOSED AT 261 HOST-SIDE, WHICH KEEPS THE ADDON UNSTAMPED and owes no second Asset Library submission while 1.11.0 is in review: the tool asks `main_screen.get` BEFORE it reads a texture and refuses a mismatch with a new `viewport_not_active` code naming the active tab and the `main_screen_set` call that fixes it. The size check stays underneath as the second line for an addon too old to answer. Three unit tests, including the degradation |
| guide-recipes-name-withheld-tools | DONE | user | — | 261 | 261 | — | §10's worked recipes name five tools a default install does not load, and never say so | 🔴 MEASURED AT 261 BY BEING THE READER: installed the published 1.76.0 into a fresh project, ran `init` as documented, and listed the surface a default client gets — 279 tools. §10 names `godot_run_managed`, `godot_run_headless_script`, `runtime_call_method` and `dbg_evaluate`, and §11 names `runtime_spawn_peers`; all five are in the `code-execution` group and none of them is loaded. Recipe D's only step is one of them, so it cannot begin; recipe C stops at step 1. §7 has carried a warning naming THREE of the five since 248 — written before the recipes existed and never joined to them, which is why it went stale without going wrong. 🟢 CLOSED AT 261 in both directions: every step naming a withheld tool is marked `(higher-trust)`, §10 opens with the list and the two ways to enable the group, and `contract_check.py` check 31 finds the population BY THE SECTION rather than by a roster — any tool a future recipe names is judged the day the recipe is written. Four directions: an unmarked privileged step, a step the declaration block omits, a declaration entry no step uses or that is not privileged at all, and a backticked name in a real tool family that resolves to no registered tool |
| recipe-steps-that-fail-validation | DONE | user | — | 261 | 261 | — | §10 A step 5 captures the wrong viewport; §10 C omits the run `id`; §10 D's `args` reach an accessor the guide does not name | Three smaller findings from the same walk, each measured by running the step as written. (1) §10 A step 5 says `main_screen_set` `{name:"2D"}` then `screenshot_editor` — whose `viewport` defaults to `3d`, so the literal sequence captures the tab the reader just left, and with the row above it captures a frozen one. (2) §10 C step 1 says `godot_run_managed` then `godot_output`, and step 6 says `godot_stop`; both take the `id` the launch returns and both fail input validation without one — measured as `expected string, received undefined at id`. (3) §10 D says to pass `args`: they are appended after the script path with no `--` separator, so a runner reading `OS.get_cmdline_args()` sees them (GdUnit4 and GUT do) and one reading the Godot-documented `OS.get_cmdline_user_args()` sees an empty list with no error — measured `USER_ARGS=[]` against `ALL_ARGS=["-s", "res://test_args.gd", "--suite", "smoke"]`. 🟢 All three corrected in the guide at 261 |
| elicit-failure-reads-as-no-support | DONE | user | — | 261 | 261 | — | A rejected elicitation ANSWER is reported as a client that cannot elicit, and the remedy named skips the confirmation | 🔴 MEASURED AT 261 against the published 1.76.0 with a client that declares elicitation and answers `{action:"accept", content:{confirm:true}}` — an answer that does not satisfy the prompt's `requestedSchema`, which requires `proceed`. The SDK throws on validation and `confirm.ts`'s single `catch` printed *interactive confirmation isn't available on this client*, then offered `confirm: true` — the one remedy that skips the confirmation the user was in the middle of giving. Three causes (no support, transport failure, invalid answer) wearing the sentence for the first, on the safety control itself, pointing at the bypass. That is 260's standing rule — a diagnostic that names a state is not a diagnosis of its cause — landing where it costs the most. 🟢 CLOSED AT 261: the capability is knowable, so `gate` reads `getClientCapabilities()` instead of inferring it from a throw. Only a client that declared no elicitation is told it cannot be asked; a client that can and failed is told nothing was done, what the underlying error was, and the response shape the prompt wanted. Three unit tests, including a server that cannot report capabilities at all, which degrades to the old sentence |
| replay-measured-nowhere-but-locally | DONE | internal | .github/workflows/ci.yml;scripts/handoff_gate.py;scripts/floor_pin_gate.py | 274 | 274 | — | The close gate can only read a log produced on the author's machine, against a PRE-merge branch — so every counter a status block carries describes a tree that is not the one that shipped, and 244's staleness rule is an mtime-shaped stand-in for the question CI already answers | 🔴 **OPENED AND CLOSED AT 274, AND IT WAS ARGUED FOR AT 273 WITHOUT BEING OPENED — WHICH IS 271 §1's CLASS AND 273 §4.7's OWN WARNING.** 273 §5.3 measured the case: every command in the 54-command replay already runs merge-blocking in CI, including the mutating three, and the list is a strict subset of `ci_scripts()`. So the replay buys a captured log and pre-push feedback, and no verification CI does not already provide — while measuring a branch tip that no longer exists after the merge. `ci.yml` now writes a `CI_MEASURED sha=… run=…` line into a log under `runner.temp` (NOT the workspace: four steps in that job check the tree is otherwise clean), tees nineteen counter-bearing steps into it and uploads it with `if: always()`. `--measured` takes a DIRECTORY, so `gh run download` is the input; `MEASURED_SHA_MISMATCH` refuses a log whose commit is not the one the block's `main` row claims, which is 244's rule dissolved into an equality rather than worked around. 🔴 **AND THE THREE MATRIX LEGS BECAME THE FIRST READER THIS REPOSITORY HAS EVER HAD FOR *a green that is true of one disk*** — thirty counters measured three times on one commit, and `MEASURED_LEG_DISAGREEMENT` refuses a difference instead of picking one. The routing question moves with the log: for a CI-measured close it is asked of the workflow files, at flag granularity, and `CI_CAPTURE_UNGUARDED` refuses a captured step without `shell: bash` because the default shell has no `pipefail` and a refusing gate would exit with `tee`'s status |
| post-merge-verdict-unread | DONE | internal | scripts/handoff_gate.py | 275 | 275 | — | A CI artifact is evidence that a run HAPPENED and 274 read it as evidence that the tree was green — the capture uploads `if: always()`, so a download directory from a run whose gates REFUSED binds to the block's commit and reads as bound | 🔴 **THIS IS THE HALF 273 NAMED AND 274 LEFT OPEN, IN 274's OWN §5.9.** *A check nobody reads after it runs is not a check, it is a receipt.* 274 made the run's OUTPUT readable and bound it to a commit; nothing read whether the run PASSED. `main` can go red after a merge and no instrument in the ritual says so — both of 273's finds were sitting in run history, one of them for eighteen hours, and the only reason either was seen is that a pickup happened to ask for a git assessment instead of for a queue row. 🔴 **AND 274's OWN `if: always()` IS WHAT MADE IT URGENT RATHER THAN TIDY.** The upload is deliberate — the run that refused is the one worth reading — and the consequence is that a red run produces a directory of real, attributed, correctly-bound counters. Every word of *bound: these counters describe the tree that shipped* is true of it and the tree is red. 🟢 **CLOSED AT 275 BY JOINING TWO THINGS THAT WERE ALREADY THERE.** The artifacts carry `run=`; GitHub knows that run's conclusion; `MEASURED_RUN_NOT_GREEN` refuses a close whose own run did not pass, `MEASURED_VERDICT_PENDING` refuses one that has not finished, and `MEASURED_VERDICT_UNREAD` refuses a close that never asked. No new block claim and no new emitted line: only a machine that ran `gh run download` has a run id to ask about, and that machine has the network by construction. 🔴 **MEASURED ON THIS SESSION'S OWN PICKUP** — the `ci` run at 274's branch concluded `failure` in `0` seconds, created no jobs, and reported none of the twenty-six required checks; this reader is the one thing in the tree that would have said so |
| workflow-file-unvalidated | DONE | internal | .github/workflows/ci.yml | 275 | 275 | — | Every reader in this tree parses the workflow files as TEXT and none asks whether GitHub Actions would ACCEPT them, so a file the forge refuses passes the whole local fence | 🔴 **FOUND BY DRIVING 274's NEXT 1, WHICH IS WHAT THAT ITEM WAS FOR.** 274 wrote `${{ runner.temp }}` into a job-level `env:` in both capturing jobs. A job's environment is evaluated before a runner is assigned, so `runner` is not among the contexts available there — GitHub refused the FILE, the `ci` workflow failed at startup, created no jobs, and not one required check reported. `ci_scripts()`, `ci_check_runs()` and `ci_capture_steps()` all read the same file happily, and the replay was green. 🟢 **CLOSED AT 275 WITH AN OFF-THE-SHELF TOOL AND A POSITIVE CONTROL**, which is `lint_ceiling.py`'s shape one file over: `actionlint` runs merge-blocking in `contract-check`, and it runs against a fixture carrying 274's own defect FIRST, because a tool that installed wrong and a glob that matched nothing both exit `0` over a broken tree. 🔴 **AND IT CANNOT CHECK ITS OWN FILE, WHICH IS WRITTEN DOWN RATHER THAN IMPLIED.** A file GitHub refuses never runs, so no step inside `ci.yml` can see `ci.yml`'s own breakage. What covers this one is the local fence — the same command runs before a push — and `post-merge-verdict-unread` above, which refuses a close whose run concluded `failure`. Three readers, none able to stand in for the others |
| catalog-status-column-unread | DONE | internal | docs/TOOL_CATALOG.md;scripts/contract_check.py;scripts/scope_gate.py;scripts/control_gate.py | 275 | 276 | — | The Tool Index's `Status` column is the last of 251's four with no reader, and it disagrees with the code on four of 292 rows | 🔴 **OPENED AT 275 AGAINST A MEASUREMENT, AFTER EIGHT SESSIONS OF BEING NAMED IN PROSE AND NEVER ROSTERED.** 253 §4.5 named it, and every handoff since has repeated the naming — which is 271 §1's class exactly: a claim argued for repeatedly and written into no table anything reads. 274's own pickup found the same shape in 273's NEXT 1 and fixed it by opening the row first. This is that, one document over. 🔴 **AND IT IS JOINABLE, WHICH IS WHY IT IS OPENED RATHER THAN KILLED.** The column is not free prose: `282` cells carry a bare tick and the rest carry an engine caveat, and the code's counterpart is the degradation helper `unsupportedLsp` in `host/src/tools/lsp.ts`, whose call sites are the tools that feature-detect a missing capability and answer anyway. Measured at 275: the two sets differ on FOUR rows — `gd_declaration` and `gd_document_link` degrade and are catalogued as plain, `gd_code_action` and `gd_workspace_symbols` are catalogued as caveated and reach the helper by neither name. Same shape as 251's `Destructive` (21 of 292) and 253's `Plane`, and the same fix: read the atom in both directions in check 4, and correct the rows it reddens 🟢 **CLOSED AT 276, AND THE MEASUREMENT THIS ROW WAS OPENED WITH WAS TOO NARROW BY MORE THAN HALF.** The join above is named after `unsupportedLsp`, and the predicate that helper implements has FOUR spellings in this tree: the generic GDScript-LSP helper, a second generic one on the C# LSP plane, two zero-argument helpers that name their tool in the literal, and six inline returns across the two debug-adapter planes. `gd_code_action` and `gd_workspace_symbols` were not miscatalogued at all — they degrade through helpers of their own, and the reader was blind to them. The real disagreement is **ten of 292**, all in one direction, and it includes `dbg_goto`, whose own catalog section says in prose that no Godot build advertises the capability it needs while the column a reader scans said ✅. 🟢 The atom is the MESSAGE — *`<tool>` is unsupported by the connected …* — which every spelling shares; check 4e reads it in both directions against the Tool Index AND the per-tool section heading, refuses a message it cannot attribute to a registered tool, and carries five positive controls, three of which edit the shipped tree. The column has a *Reading the `Status` column* paragraph now, beside the two that already had one. 🔵 **The carried lesson: a join named after a function is a claim about that FUNCTION, not about the thing it does.** |
| dap-capability-dead-surface | DONE | user | host/src/tools/dap.ts;docs/TOOL_CATALOG.md | 276 | 278 | — | Three GDScript debugging tools refuse as unsupported on every Godot build this repository has ever described, and nothing measures how many of them are reachable at all | 🔴 **FOUND BY 276's `Status` COLUMN JOIN, WHICH IS THE FIRST THING THAT EVER PUT THE COLUMN BESIDE THE CODE.** `dbg_goto`'s own catalog section states, in prose, that *no Godot build advertises `supportsGotoTargetsRequest`, so the capability check returns first* — the tool cannot run on any shipped engine — and the C# plane's section states that netcoredbg advertises neither that nor `supportsDataBreakpoints`, which is given as the reason those two are deliberately NOT mirrored to the C# plane. The GDScript plane ships both anyway. `dbg_set_exception_breakpoints` is the third: Godot 4.3 advertises no filters, per the guard's own comment. 🔴 **THE ROW IS NOT A REMOVAL PROPOSAL, IT IS A MEASUREMENT REQUEST.** The integration gate asserts a BICONDITIONAL — advertised implies it answers, unadvertised implies it refuses by reason — which holds identically whether the capability is present or absent, so a green CI run says nothing about which side of it Godot is on. Nothing in this tree RECORDS the observed capability set of a real adapter. The work: read `caps` out of the live 4.3 and 4.7 integration runs, write the observed set down where a reader can find it, and only then decide per tool between keeping it documented as engine-gated, and saying so in the tool's own description where a caller sees it before choosing. 🔵 Opened `user` because the atom is shipped tool surface an operator can call and be refused by, and **SCHEDULED rather than OPEN** because the measurement is a reading of the 4.3 and 4.7 integration runs and the decision after it is a product question for the maintainer, not a call this apparatus should make on its own. Targeted at the same session as `sdk-v2-migration` on purpose: both are read-then-decide rows and the pair is a set one session can honestly carry, which is the question `schedule-set-unread` says nobody asks 🔴 **MEASURED AT 278 OFF THE POST-MERGE INTEGRATION RUN AT `0be54af`, BOTH ARMS GREEN, AND THE ANSWER IS WIDER THAN THIS ROW.** Godot 4.3-stable and 4.7-stable advertise an IDENTICAL set: `supportsRestartRequest`, `supportsSetVariable` and `supportsTerminateRequest` true, the other six ABSENT — and `absent` is a different fact from `false`, which two shipped paragraphs in `dap.ts` had said for as long as they existed. So the dead surface is SIX callable things and not the three named here: the tools `dbg_goto`, `dbg_data_breakpoints` and `dbg_set_exception_breakpoints`, plus the `conditions` / `hit_conditions` / `log_messages` modifiers on `dbg_set_breakpoints`, which the same run reports dropped on both arms. 🔴 **AND THE TYPED LIST THAT STOOD IN FOR A LEDGER WAS WRONG IN BOTH DIRECTIONS.** `gdscript-dap-plane.integration.mjs` carried a hand-written list describing itself as *the gates the host reads*: it named `supportsTerminateRequest`, which `host/src` gates on nothing and which is one of the three ADVERTISED, and omitted `supportsConfigurationDoneRequest`, which it does gate on — so the live claim printed *3 gate(s) the host reads* about two gates and one bystander, and the one capability nobody had ever observed was the one nobody could see was missing. 276's finding-to-carry, in the row 276 opened. 🟢 **WHAT SHIPPED, AND THE PER-TOOL DECISION WAS HIS.** Offered record-only, record-plus-say-so, retire-the-tools, and record-plus-USER_GUIDE; he chose record-plus-say-so. `docs/dap_capability_ledger.json` carries the observed set per arm with the run and commit it was read from and a `unread` value that is NOT `absent` (271); check 33 joins `gated_on` to `host/src` in both directions, refuses a surface called dead on an `unread` capability, refuses one whose capability came ALIVE, and refuses a dead surface whose own description does not say so; and the live gate asserts the observed set against the ledger, which is the only falsifier this record has and it runs on CI because that is where the evidence is. Six descriptions amended, including the one that named 4.3 as *the example* of a build without exception filters — 4.7 has none either. 🔵 The tools are KEPT and kept guarded: a capability is not a boundary and `dbg_goto` is the destructive tool on this plane. 🔵 The bytes are the honest cost — `BYTES_CEILING` 367,616 → 369,171, raised with the note it demands, because a description IS the tool surface |
| main-red-between-sessions-unread | DONE | internal | scripts/handoff_gate.py | 276 | 277 | — | The post-merge verdict reader fires only at a CI-measured close, so `main` going red between sessions is still visible only to a pickup that happens to look | 🔴 **NAMED IN PROSE AT 275 §8.7 AND DELIBERATELY NOT BUILT, WHICH IS THE HALF THAT NEEDED A ROW.** 273 found `main` red on its own twice, both times sitting in run history where nothing in the ritual looks. 275 answered it for a session that CLOSES: `MEASURED_RUN_NOT_GREEN` asks GitHub what the run that produced the block's counters concluded. A session that closes against a local log never asks, and a session that runs out of context and does not close at all asks nothing — which is precisely the session after which a red `main` sits longest. 🔵 The shape is written down: `--gh-open` grows a FOURTH reading — *is `main` green at HEAD?* — over the same one-command route the other three already take, and the open gate prints it at PICKUP rather than at close. 275 left it unbuilt on purpose so that its own binding half could be measured alone; this is the row that stops that decision becoming eight handoffs of prose, which is the class 275 §7 had just paid for one document over  🟢 **CLOSED AT 277, AND IT PAID ON THE PICKUP THAT BUILT IT.** `--gh-open` prints a fourth line, `MAIN_AT_HEAD <sha> <conclusion>`, and `open_tier` refuses TIER0 on a verdict that was READ and is not `success`: the cheap tier's whole premise is that the tree here is the tree the previous block was verified against, and `tier_trigger` proves that tree's IDENTITY and nothing about its health. 🔴 **THE ATOM IS THE COMMIT, NOT THE RUN.** `gh_run_verdict` (275) asks whether ONE run concluded; a branch has several workflows and a session inherits the whole commit, so the population is every run at `main`'s newest sha and the verdict is the worst of them — a reader keyed on one run id answers green for a commit whose integration workflow failed. 🔵 **UNREAD IS A NOTE AND NEVER A REFUSAL**, which is 271 §1's rule for the three readings beside it: a pickup on a machine that cannot reach the forge must still be able to open, and refusing on connectivity would make the tier a statement about the network. Three answers, not two — a run still going is neither, and a pickup minutes after a merge is exactly when that happens |
| dap-late-rejection-flake | OPEN | internal | host/test/dap.test.ts | 275 | — | — | `dap.test.ts`'s *a launch rejection arriving AFTER a stop still ends the session* failed once on a loaded `node 22` runner and passes everywhere else | 🔴 **MEASURED, NOT SUSPECTED, AND THE MEASUREMENT IS WHAT MAKES THE ROW SMALL.** Job `96349117942`, PR #337, a diff of one Python file and one changelog entry: `Cannot read properties of undefined (reading 'state')` at `dap.test.js:1650`. `dbg_launch` had returned an ERROR result, so `structuredContent` was absent — the tool refused rather than reaching `stopped`. Re-run in isolation twelve times on the same node version: **zero** failures, so it is load-shaped rather than logic-shaped, which is 273's timing family in a different disguise. 🟢 **WHAT SHIPPED WITH THIS ROW IS THE DIAGNOSTIC, NOT THE FIX**: the assertion asserts the SHAPE before reading the field, so the next occurrence prints the error the tool actually returned instead of a TypeError that carries none of its own diagnosis. Fixing the flake needs that message, and nothing in the tree can produce it until it happens again. 🔵 **DO NOT GUESS AT IT FROM HERE.** The handshake has a fixed timeout and a loaded runner runs three matrix legs beside the integration workflow; that is a hypothesis, and 273's own session is the argument for measuring one rather than acting on it |
| scheduled-workflow-red-unread | DONE | internal | scripts/handoff_gate.py | 279 | 279 | — | `MAIN_AT_HEAD` takes every run at `main`'s NEWEST sha, so a workflow triggered by `schedule:` is inside the population only between its own run and the next merge — and outside it, unread, for the rest of the week | 🔴 **FOUND ON 279's PICKUP, BY THE READER REFUSING FOR THE FIRST TIME.** `MAIN_RED_AT_HEAD` fired at `e65ed07`: `sdk-drift`'s `landscape-drift` job had failed on 2026-08-17 and again on 2026-08-24. 🔴 **AND IT WAS VISIBLE ONLY BY AN ACCIDENT OF TIMING** — `main` had not moved since Friday, so that morning's cron run happened to sit at `main`'s newest commit. On 277's and 278's pickups the same failing workflow was one merge behind the head and the reader returned a clean `success`; both sessions closed their blocks 🟢 *nothing owed* over a job that had been red for a week. 277 wrote *the atom is the commit and not the run*, and it is — for the merge path, where every workflow is triggered by the push that made the commit. A cron is not. 🟢 **CLOSED AT 279 WITH A SECOND READING BESIDE THE FIRST, NOT INSTEAD OF IT.** `elsewhere_red_of` reports the newest run of every workflow that did NOT land on `main`'s newest sha and did not pass; `--gh-open` prints it as `WORKFLOW_RED_ELSEWHERE <workflow> <sha>` and `main_head_problems` turns it into a named, 🔴-marked NOTE and never a refusal, because a scheduled workflow's subject is the world and not the health of the tree being inherited (271 §1). The commit verdict is unchanged and still refuses. 🔴 **AND THE REFUSAL COULD NOT SAY WHAT FAILED**: `--gh-open` has printed the names since 277 and `MAIN_AT_HEAD_RE` captured two groups, so through the measured-log route the first refusal this reader ever issued read *`main` is failure at e65ed07* and stopped — 273's *a reader that cannot show its own refusal*, found in its own first firing. The printed line is unchanged; the pattern has a third group |
| assetlib-source-leg-unimplemented | DONE | internal | scripts/assetlib_sweep.py;docs/alternative_mcp_roster.json;docs/LANDSCAPE_TRACKING_POLICY.md | 279 | 279 | — | Rule 2 defines *no change* with two clauses — the Asset Library card AND the repository head — and `assetlib_sweep.py` has only ever read the first | 🔴 **IT COST EXACTLY WHAT RULE 2 WAS WRITTEN TO STOP, TO THE SAME PROJECT, IN THE SAME WEEK.** That rule's own *Why* records that the 2026-08-09 sweep found a carried project had shipped a debugger 48 hours after the document saying the category could not. `godot-mcp-enhanced` shipped a complete interactive debugger at `6394ddb` on **2026-08-09** — an `EditorDebuggerPlugin` subclass driven by ten handlers: set, clear and list breakpoint, stack trace, inspect frame, evaluate, step, continue, pause, reload scripts — and its Asset Library card did not move until 2026-08-22. For thirteen days this sweep reported it as `no change`, with the evidence, and the evidence was about the card. 🔴 **AND THE ROSTER'S OWN TWO FIELDS DISAGREED ABOUT WHEN THE PASS HAPPENED**: `last_analysed` read 2026-08-09 and `last_analysed_commit` pinned `8b56302`, which is dated 2026-08-08. A date and a sha describing one event with nothing joining them, and the gap between them is the commit that overturns the ruling. 🟢 **CLOSED AT 279.** `source_state` is pure and answers three ways — `held`, `moved`, `unread`, because a forge that will not answer is not a green (271 §1) and an entry nobody has ever analysed cannot have moved since a pass that did not happen. `repo_heads` dials the forge once per entry that names a repository, the printout marks every row the card leg alone calls *no change*, and `--check` refuses on it. The roster ruling for `godot-mcp-enhanced` is corrected from `breakpoint-gutter-marking-only` to `godot-internal-editor-debugger` |
| roster-new-ai-unrefused | DONE | internal | scripts/assetlib_sweep.py;docs/alternative_mcp_roster.json | 279 | 279 | — | The discovery leg prints `<- record on the roster` beside every never-tracked in-editor AI addon and `--check` refuses only on `moved or new_mcp`, so nothing has ever refused when nobody recorded them | 🔴 **FOURTEEN OF THEM, AND THE POPULATION THAT MUST BE ON THE ROSTER WAS ALREADY DEFINED IN THE ROSTER.** Its first line of `_comment` says it holds *every product that has appeared in any prior sweep* and that *an entry is never dropped*; an addon the discovery leg has surfaced HAS appeared in a sweep. So the check read a narrower population than the file it reads — 278's *a claim in a comment is still a claim*, with the reader arriving one file later. 🟢 **CLOSED AT 279 BY DERIVING THE REFUSAL FROM THE ROSTER'S OWN CONTRACT RATHER THAN BY ADDING A SEVERITY.** Recording an in-editor AI addon costs one row and no source-level pass — Rule 1: it opens no socket and cannot be driven by an MCP client — so the refusal asks for the cheap thing the roster already promises rather than the expensive thing only an MCP-shaped entry owes. All fourteen recorded at the 2026-08-24 reading, `last_analysed` null and staying null |
| sdk-upstream-reader-one-name | DONE | internal | scripts/registry_lag.py;.github/workflows/sdk-drift.yml | 279 | 279 | — | `sdk-v2-migration`'s five re-reads each ran one `npm view` against one package name, and the reader that would have answered them lived nowhere in the tree | 🔴 **THE ROW'S SCHEDULE CHECK WAS A SENTENCE, EXECUTED BY HAND, FIVE TIMES, CORRECTLY, AGAINST A PACKAGE THAT COULD NOT ANSWER IT.** See `sdk-v2-migration` for what it cost. 🟢 **CLOSED AT 279 WITH A DERIVED POPULATION**: `upstream_problems` compares the major `host/package.json` pins against every package the registry serves from the same repository as the dependency it actually pins, so a rename, a split or a sixth sibling joins the reading by itself. The join is the REPOSITORY and not the scope — `@modelcontextprotocol/inspector` carries the same scope and is a different project — and a fixture row proves it rejects exactly that. 🔴 **AND THE READING GOES FIRST IN `main()` ON PURPOSE**: every refusal below it returns immediately, and this tree is eleven commits past its newest tag today, so a reading placed after them is one that only ever prints on a healthy tree. 199 §9.2 and 277 §1.2, avoided rather than paid |
| untagged-count-unbound | DONE | internal | scripts/handoff_gate.py;scripts/registry_lag.py | 279 | 280 | — | `registry_lag.py`'s `untagged` count is the reading that says a release is owed, and no header atom binds it, so a block can carry a truthful `lag 0` beside a stale one | 🔴 **278's BLOCK SAID NINE AND THE TREE WAS ELEVEN.** 278 read the count during the session and never re-read it after its own two PRs merged — which is precisely what 278 caught 277 doing, wrote up as a finding, and then did. Third session running for this class. 🔵 `npm.lag` IS a header atom and `HEADER_UNREAD_CLAIMED` governs it; `untagged` is not one, so the number that actually decides whether a release is owed is the one nothing binds to a commit. The work: give it an atom, or say in the header why the other reading is the one that counts 🟢 **CLOSED AT 280, AND WHAT IT COST TO CLOSE IS THE FINDING.** `npm.untagged` is a header atom now, read from the measured log first and derived second, exactly as `npm.lag` is — so a block may state the number and the gate compares it. 🔴 **AND ADDING THE ROW TO `PROVENANCE` IS WHAT NAMED THE POPULATION NOBODY HAD.** That table asks one question — *what is this counter a fact ABOUT* — and it cannot be answered for this one without noticing that `registry_lag.untagged()` is fed `git_tags()`, THIS CHECKOUT's list, while `npm.tags` was declared REMOTE at 234 precisely because the clone's tag list is a fact about the disk (six tags on one laptop, 234 §4.8). The header's reader takes ORIGIN's names and REFUSES rather than measuring when origin's newest tag is not in this checkout. The two lists agree today at 134; the hazard was latent, not live, and it is declared now instead of true by luck. 🔵 It also needed `HEADER_ALIAS_PENDING` — 246 built the self-clearing table for `COUNTER_READERS` and the header half never got one, so a new header reader could be shipped only by editing a block that had already shipped. Expires at 281 when 280's block enters `BLOCK_POPULATION`. |
| gate-input-requirements-untabled | DONE | internal | scripts/handoff_gate.py;.github/workflows/ci.yml | 279 | 280 | — | Every gate depends on some subset of {a full object store, a network, a compiled `dist/`, a staged addon, a live engine}, per job, and `DEPTH_REQUIRED` is one column of a table with no other columns | 🔴 **278's FINDING-TO-CARRY, WRITTEN INTO A ROW AT 279 BECAUSE 278 LEFT IT IN PROSE** — under 278's own new rule that a NEXT item naming no row is prose, and that this includes the rows a session opens for its successor. The rule was written in the same document that then broke it. 🔵 279 did not take it: the pickup found `main` red, an owed release two commits understated, and a Tier 1 row whose fifty-three-session premise was false, and all three outranked a table nothing was yet refusing over 🟢 **CLOSED AT 280 — `DEPTH_REQUIRED` IS `GATE_INPUTS`, FIVE COLUMNS AND SIX ROWS.** `objects`, `dist`, `addon`, `engine`, `network`. 🔴 **AND THE ANSWERS WERE ALL ALREADY WRITTEN DOWN, EACH IN THE ONE FILE NOBODY CHECKING A DIFFERENT FILE WOULD READ**: `wire_invisible_gate.mjs` line 50 says *(needs dist/ — run `npm run build`)*; `ci.yml` says the tarball's `addon` root *does not exist on a fresh clone until `stage-addon`*; `integration.yml` says `vcs-plane` is *the one plane that needs NO Godot*. Four true statements and no reader — 278's own sentence about `fetch-depth`, true of four more columns while it was being written. 🔵 **EVERY COLUMN HAS A DERIVED PROVIDER**, which is what stops the table agreeing with itself: `job_provides` reads all five off the job's own text, so moving a step or giving a schedule-only workflow a `push:` trigger changes the answer with no row edited. `network`'s provider is the WORKFLOW — not *does the runner have a socket*, which they all do, but *may this reading fail a merge* — and that claim is the one that would have refused 279 §9's near-miss in advance. 🔴 Two fixtures beat the author on the run that added them: an undefined input crashed the reader it had just refused, and the coverage question, asked inside the per-roster reader, complained about four columns every one-column fixture was not testing. Self-test 407 -> 431. |
| counter-provenance-undeclared | DONE | internal | scripts/handoff_gate.py | 280 | 281 | — | `PROVENANCE` is the only table in this tree that asks a reader what its counter is a fact ABOUT, and it covers the 9 header rows; `COUNTER_READERS` has 35 rows and no such column at all | 🔴 **279 ASKED WHAT FINDS THE FIFTH TYPED POPULATION BEFORE IT COSTS A YEAR. 280's ANSWER IS THAT THIS TABLE ALREADY DOES, TWICE, AND IT IS ASKED OF A QUARTER OF THE READERS.** `npm.untagged`'s population was not found by looking for it — it was found because a row cannot be added to `PROVENANCE` without answering *TREE, CLONE or REMOTE*, and answering it for a commit distance counted from `git_tags()` is the same sentence 234 §4.8 already wrote about `npm.tags`. That is a mechanism, not a habit: **the question gets asked because the table will not accept a row without it.** 🔵 The 35 `COUNTER_READERS` rows are asked nothing of the kind. They carry a cost (`CHEAP`/`MUTATING`) and a `SINCE`, both of which are facts about when and how expensively the reader runs, and nothing about what its number is a fact ABOUT — so `floor_pin 108`, `blast 2602`, `term 311 file(s)` and thirty-two others are numbers whose subject nobody has ever had to name. Some are certainly CLONE facts wearing a green, which is 277's *a green is only ever green on a machine* with a population attached. **The work is a provenance column on `COUNTER_READERS`, asserted in both directions exactly as the header's is — a key with no reader is a stale row, a reader with no key is a counter whose subject nobody declared — and then reading what the 35 answers turn out to be.** Expect the answering to find the defects, not the column. 🟢 **CLOSED AT 281, AND THE HEADER'S OWN THREE VALUES DID NOT TRANSFER.** `TREE / CLONE / REMOTE` asks WHERE a number is read from, and all 35 of these run a command in this checkout — every row would answer TREE and the column would be a predicate with one value, which is 276's finding recreated by copying a good table into the wrong question. The question with a population here is the one TIER0 already turns on: **what could make this number different at THIS COMMIT?** `COUNTER_PROVENANCE` answers it in four values — TRACKED 9, INDEX 24, CLONE 1, MACHINE 1 — and `CLONE` is 234 §4.8's own word given a population at last, since no header row has ever carried it. 🔵 **THE VALUES ARE DERIVED FROM THE READERS' OWN CODE, NOT TYPED**: `subject_of` greps the script a row RUNS for the idioms that make its answer depend on something other than the tracked bytes, exactly as `job_provides` derives a job's supply from its own YAML, and `SUBJECT_UNDERSTATED` refuses any row whose reader reads more than its value admits — re-checked on every close, because a table of measurements nobody re-measures is a table of memories. 🔴 **AND THE DERIVATION WAS WRONG THREE TIMES BEFORE IT WAS RIGHT, IN BOTH DIRECTIONS.** Draft one stripped comments and called four scripts non-TRACKED on prose ABOUT git and clocks — 279's terminology exclusion, one predicate over. Draft two stripped string literals too and returned TRACKED for thirteen of seventeen, because every reader here spells its command as an argv LIST and the signal lives in exactly the tokens a prose-stripper throws away; it called `instrument_gate.py` clean, whose `blast` three sessions have had to correct from a local reading to CI's at the same commit. Draft three matched bare verbs and found `"add"` in `scope_gate.py`'s `_GROWERS` tuple of METHOD names, `"add"` in `wire_diff.mjs`'s `git worktree add` (the object store, not the index), `git config` inside an `instrument_gate.py` exemption REASON, and `hrtime` inside **this table's own regex** — the corpus of a derivation over `scripts/` contains the derivation. 🔴 **AND THE FOURTH CORRECTION CAME FROM THE SELF-TEST, NOT FROM ME**: the live pair pinning `floor_pin_gate.py`'s corpus refused the draft above it, because that file carries *Tracked paths `git ls-files` returns* inside a FLOOR REASON, and a reason is a single-line string, which the stripper keeps because an argv element is one too. An argv element IS the whole string; prose merely contains the words. 🔵 **THE CORPUS IS THE SCRIPT PLUS ITS FIRST-PARTY IMPORTS**, and `_gate_lock.py` is why: six `floor_pin.*` and ten `instrument.*` counters reach `git status --porcelain` only through the shared lock, so a derivation over a gate's own bytes sees none of it — a reader that reaches its input through an import is one an import-blind grep calls clean. 🟢 **THE PAYOFF IS ON THE LINE THAT ASKED FOR IT.** `open_tier` has printed *a counter which is a fact about the machine rather than the tree will disagree* since 240, and for forty-one sessions nothing knew WHICH; the pickup now names them by class as it inherits them, and a close-time disagreement on a TRACKED counter says plainly that nothing about the machine can explain it |
| authoring-plane-bridge-drop-flake | OPEN | internal | .github/workflows/integration.yml;host/test-integration/authoring-plane.integration.mjs | 280 | — | — | `authoring plane / A-G mutators vs real editor` lost the editor bridge mid-probe on the POST-MERGE run and passed on re-run at the same commit | 🔴 **MEASURED ON 280's OWN MERGE.** PR #346 went 26/26 green, and the `integration` run on `main` at the same content came back red on this one job: `AUTH_SUMMARY pass=191/200 fail=9`, with all nine cascading from ONE cause. `AUTH_BACKEND_THREW` reports `bridge_closed` — *Bridge connection closed before a response arrived (read ECONNRESET) — it went away without closing the connection* — and every family after it gets `bridge_unavailable` at `127.0.0.1:9080`. 🔵 **THE TWO POPULATION CLAIMS ARE THE HONEST PART AND THEY ARE WHY THIS IS A ROW RATHER THAN A SHRUG**: `AUTH_POPULATION_SPOKE` named the two families that made NO claim and `AUTH_POPULATION_COMPLETE` named the four that threw AFTER claiming — *claims were dropped, not failed*. A probe that reported 191 passes without those two readers would have read as a mostly-green run. Re-run at the same commit: green in 1m09s. 🔴 **WHAT IS NOT KNOWN IS WHY THE EDITOR WENT AWAY**, and the log carries a candidate the sweep should either implicate or clear: `SCRIPT ERROR: Compile Error: Identifier not found: SilentWolf` on `_auth_probe_backend_leaderboard.gd` and `_auth_probe_backend_save.gd`, beside `Task 'update_scripts_classes' already exists`. Same family as `dap-late-rejection-flake` (275) — a real-engine plane that fails once and passes everywhere else — and the same rule applies: **a flake nobody has measured twice is a defect nobody has looked at.** Do not silence the job; measure whether the compile error precedes the drop 🔵 **MEASURED A SECOND TIME AT 282, ON THE PR THAT WAS NOT EVEN ABOUT IT.** 280 handed this row over saying it had happened once and had never been measured twice. It happened again on `session282-the-block-that-was-not-registered` (PR #350, job 97779170000): `AUTH_MP_THREW FAIL resource_load: Bridge error [bridge_closed]: Bridge connection closed before a response arrived (read ECONNRESET)`, and every family after it cascaded to `bridge_unavailable` at `127.0.0.1:9080` — `AUTH_SUMMARY pass=182/193 fail=11`, with `AUTH_POPULATION_COMPLETE` naming four families that made claims and THEN threw. 🔵 **THE SHAPE IS IDENTICAL TO 280's AND THE NUMBERS ARE NOT**: 280 read `pass=191/200 fail=9` from one cause, 282 reads `182/193 fail=11` from the same one. Two measurements, same first cause (`bridge_closed`, `ECONNRESET` on the editor bridge), different cascade sizes — which is what a DROP looks like and not what a logic defect looks like. 🔴 **AND THE PR IT HAPPENED ON TOUCHED ONE PYTHON FILE**, so it is not caused by anything in the change under test. Still not root-caused; now measured twice, which is what the row asked for |
| subject-derivation-reads-a-file-not-an-invocation | OPEN | internal | scripts/handoff_gate.py | 281 | — | — | `subject_of` answers about a FILE and a `COUNTER_READERS` row invokes a MODE, so a multi-mode script gets one subject for all of them | 🔴 **`handoff.claims` IS THE MEASURED INSTANCE AND IT IS IN `SUBJECT_UNDERIVABLE` FOR IT.** `handoff_gate.py --selftest` is fixture-driven and reads nothing but this file's own tables; the same file's `--gh-open` dials GitHub and npm. Derived over the file it comes back MACHINE — on a `cpu_count` that is inside `SUBJECT_SIGNALS`' own regex — and the row is declared TRACKED by the MODE, on a reason typed by a human. That is an exemption resting on a judgement no reader re-takes, which is 279 §9's shape: an exemption whose reason has expired reads exactly like one whose reason holds. 🔵 The repair is to derive over the CALL GRAPH REACHED BY THE INVOCATION rather than over the file's bytes — the argv already names the entry point, and `reader_corpus` already walks one import hop, so the machinery is half built. 🔵 It would also retire the second exemption's premise: `ci.checks` is `cmd=None` and derived in-process by `ci_check_runs`, which is a function this walk could reach directly rather than a row that opts out 🔴 **RE-PRICED AT 282, AND THE REPAIR THE ROW NAMES DOES NOT FIX THE INSTANCE IT NAMES — MEASURED, NOT SUSPECTED.** A call-graph walk over the invocation was prototyped: `main`'s dispatch is read for the row's own flags (`if "--selftest" in argv: return selftest()`), the reachable top-level defs are closed over, and the corpus becomes those bodies plus the module-level names they use. It works — `mutlock.guarded` moves INDEX → TRACKED and `tree_quiet.cases` CLONE → TRACKED, both correctly, because those modes genuinely do not reach the index or `git config`. 🔴 **AND `handoff.claims` COMES BACK `CLONE` ON `"git" , "remote"` AND `get-url`, BECAUSE A SELF-TEST'S CALL GRAPH IS THE WHOLE MODULE BY CONSTRUCTION.** That is what a self-test is for. Narrowing to the invocation cannot separate `--selftest` from the file it tests, so the repair as written buys the general case and not the measured one. 🔵 **AND THE SECOND MEASUREMENT IS SHARPER THAN THE ROW'S OWN DIAGNOSIS.** The row says the derivation returns MACHINE on `cpu_count` inside `SUBJECT_SIGNALS`' regex. It is not only there: `selftest()` carries `{"a": (MACHINE, ["cpu_count"])}` as a FIXTURE VALUE — the derivation matching its own test data. `cpu_count` is also the ONE bare token in the MACHINE pattern; every sibling idiom is call-shaped or module-qualified (`time.time(`, `Date.now(`, `os.cpu`, `process.hrtime`). Making it call-shaped removes both hits in one edit and costs nothing, and the whole-file answer then falls to CLONE, which is the network path `--selftest` does not reach. 🔴 **SO THE ROW IS TWO ROWS WEARING ONE TITLE**: derive per-invocation (built and measured here, and worth having), and *a derivation whose corpus contains its own fixtures is green about its own test data* — which is 281's finding-to-carry with the corpus one hop wider than 281 drew it. Neither is closed; both are now priced  🆕 **289 — THE CHEAP HALF SHIPPED, AS ITS OWN ROW, AND THIS ROW IS NOW ONLY THE EXPENSIVE ONE.** 282 measured that the row was two rows wearing one title. The second — *a derivation whose corpus contains its own fixtures* — closed at 289 as `subject-derivation-matched-its-own-source`: the single bare token in the MACHINE pattern is call-shaped now, the whole-file answer for `handoff_gate.py` fell from MACHINE to CLONE on `"git", "config"` / `"git", "remote"` / `core.hooksPath` / `get-url`, and the 476-claim self-test held. **What is left is the per-INVOCATION derivation**, which 282 prototyped and which does not fix the instance in this row's own title, because a self-test's call graph is the whole module by construction. 🔵 It is still worth having — `mutlock.guarded` and `tree_quiet.cases` both move to TRACKED correctly under it — and it is no longer entangled with a one-line edit |
| node-add-discards-the-name-it-was-given | DONE | user | addons/breakpoint_mcp/operations.gd;host/src/schemas.ts;host/src/tools/editor/node.ts;scripts/contract_check.py | 283 | 283 | — | All 22 authoring tools accept a `name`, hand it to `add_child` with `force_readable_name` defaulted false, and answer any collision with the machine form `@Type@N` — no error, no flag 🟢 **CLOSED AT 283 BY COLLAPSING THE POPULATION TO ONE.** `_commit_add` is now the only place this file registers an `add_child` into an undo action, and it passes `true`: a taken name comes back `SFX2`, the way the editor's own Add Node answers it. The rename is REPORTED with `coerced: true` and `requested` — the shape `node_set_property` has used since 1.82.0 — declared on all 22 output schemas by a spread rather than 22 hand-edits, and the caller is warned before it picks a name by one shared `NAME_TAKEN_CLAUSE` literal with 22 readers. Check 34 refuses a second registration site | 🔴 **MEASURED BY BEING THE USER, ON A LIVE GODOT 4.7.** The guide's own §7 step 3 verbatim, twice: `"SFX"`, then `"@AudioStreamPlayer3D@19825"`. The next call addressing `SFX` gets `bad_path`. 🔴 **AND `node_duplicate` HAD NO NON-COLLIDING CASE AT ALL** — a duplicate always collides with its source, so `@Label@19826` was the tool's ONLY behaviour where the editor's Duplicate gives `Label2`. Engine-measured both ways: `add_child(n)` → `@AudioStreamPlayer3D@2` / `@AudioStreamPlayer3D@4`; `add_child(n, true)` → `SFX2` / `AudioStreamPlayer3D`. That second column is also the first time the shipped schema sentence *"defaults to the class name"* is true. 🔵 **A ROSTER WOULD HAVE BEEN THE DEFECT AGAIN** (282 §2.3): not one of the addon's 49 `add_child` sites passed the flag, because nobody knew to, and a list of sites-to-fix is a list somebody has to keep true |
| runtime-port-held-by-a-stranger-reads-as-a-shutdown | DONE | user | host/src/bridge.ts;host/test/bridge.test.ts | 283 | 283 | — | Four readers of one fact and two were wrong: with a foreign process on the runtime port, `runtime_*` answered `bridge_closed` — *"what a shutdown looks like from here"* — when no game of ours had ever run 🟢 **CLOSED AT 283, AND THE HOST ALREADY HELD BOTH ANSWERS.** A peer whose key is wrong sends exactly one frame — `{"error":{"code":"unauthorized"}}` — and closes; `onMessage` dropped it for carrying no request id. That frame is now recorded and names the real remedy. A peer that never sends any frame is recorded too, but the two signals are NOT equal in strength: the auth denial is DEFINITIVE and overrules the close cause, while *never spoke* is an INFERENCE and fires only past a full deadline — because a bridge that IS ours also sends nothing when it is killed mid-request, so asserting it on the close path would throw away 264's reset-vs-clean-shutdown distinction to say something weaker. Codes unchanged; only what a human reads moved | 🔴 **FOUND HONESTLY: a headless `example/` game leaked from a probe run SIX DAYS earlier was still holding his 9081.** Three diagnoses of one fact, measured separately: nothing listening → `bridge_unavailable`, ECONNREFUSED, names the next action 🟢; a foreign non-speaking listener → **hangs the full 15 s** then a bare `timeout` 🔴; a foreign Godot → `bridge_closed` and the shutdown story 🔴. Beside them `doctor` said *"open, but no Breakpoint bridge answered … something holds that port"* and `dbg_launch` refused with the conflict spelled out — both right. 🔵 203 §2's rule about two opinions on one fact, at four readers |
| quick-start-withheld-list-short-by-one | DONE | user | docs/USER_GUIDE.md;scripts/contract_check.py | 283 | 283 | — | §7's box names three tools a default install cannot run; step 5's `godot_run_managed` is a fourth and is not named, so the quick start hits an unannounced wall 🟢 **CLOSED AT 283 BY GIVING CHECK 31 THE SECTION IT WAS WRITTEN ABOUT.** The check's own header diagnoses this box by name — *"written before the recipes existed, never joined to them, and therefore stale the day recipe C was added"* — and then joined §10/§11 and left §7 governed by nobody. Its window now includes §7: same four directions, one more section. It fired on all four withheld tools on the first run. §7's steps are marked **(higher-trust)** and the box names all four, plus the fact that `godot_run_project` beside `godot_run_managed` is NOT privileged and does work as written | 🔵 **AND THE EXTENSION NEEDED ONE NEW DISCRIMINATOR, WHICH IS ITS OWN SMALL LESSON.** §10's only blockquote IS the higher-trust declaration, so *"the line starts with >"* was sufficient while §10 was the whole population. §7 also carries a blockquote about tagging rich values, naming `node_set_property` and `runtime_set_property` — neither privileged — and reading it as a declaration accused the guide of withholding two tools nobody withholds. A blockquote is now classified by its SUBJECT, and the withholding vocabulary is read out of `CAPABILITY_GROUPS` rather than typed |
| doctor-advises-the-flag-you-just-used | DONE | user | host/src/cli/doctor.ts;host/test/cli_doctor.test.ts | 283 | 283 | — | `doctor --require-live`'s footer ends *re-run with --require-live* — advice about the invocation, printed without ever asking what the invocation was 🟢 **CLOSED AT 283.** `DoctorReport` carries the `liveLevel` the run was given and each bridge row declares its `tier`, so a row that is STILL informational names the level that would actually promote it (`--require-live=all` for the runtime bridge) and says it needs the game running, not just the editor | 🔵 **THE SAME DEFECT `liveness` WAS ADDED TO FIX, ONE AXIS OVER.** That field's own comment says the summary *"stopped asserting one explanation over a population, and let each member declare which explanation is its own"* — and the sentence it fixed still ended with one confident claim about the INVOCATION that no member declared and nothing read |
| block-registration-unenforced | DONE | internal | scripts/handoff_gate.py;QUEUE.md | 283 | 284 | — | 253's rule — the previous session's block goes into `BLOCK_POPULATION` in the FIRST PR — is enforced by nothing that RUNS, so three sessions running have discovered it at the close and paid a second PR each | 🔴 **241, 282 AND 283 HAVE EACH PAID IT, AND EACH WROTE A LOUDER WARNING FOR THE NEXT SESSION.** 282's handoff says it in capitals, having paid 241's price one session earlier; 283 read that handoff at pickup and missed it anyway. **The warning is not what is failing.** `MOVED +N` is measured from the previous block's `main` row read out of `BLOCK_POPULATION`, so an unregistered block makes the close refuse with `git.moved` — at the END of a session, when the price is a second branch, a second PR and a re-close against a moved HEAD. 🔵 **AND THE READER IS ALREADY THERE.** `handoff_gate.py --open` READS the previous block at pickup — it must, to inherit thirty-five counters from it — so it is one comparison away from also asking whether that block is in `BLOCK_POPULATION`. A refusal at pickup costs one commit at the start of a session; the same refusal at the close costs a PR. 🔴 **THIS ROW IS THE ARGUMENT AGAINST ITSELF AS PROSE**: 283 first wrote it into the handoff's §7 and nowhere else, which is precisely the mechanism that has failed three times 🟢 **CLOSED AT 284, AND THE TRIGGER IS A NUMBER THE SESSION WAS GOING TO CHANGE ANYWAY.** The row proposed a refusal at pickup and that turned out to be the one place it cannot go: at a pristine pickup the block being opened against is CORRECTLY absent, because a session does not register its own, so a gate that refused there would refuse every opening this apparatus has. 🔵 **`QUEUE.md` ALREADY KNEW WHICH SESSION THE TREE WAS.** `<!-- QUEUE_HEAD N -->` exists because 240 deleted the age column so the number could not be typed twice, which makes it the one tracked, gate-read fact naming the session — where a handoff is gitignored and unreadable from CI by construction (269). `POPULATION_CURRENCY` requires every block from `POPULATION_SHAPE_FROM` to `head - 1`, so the instant a session bumps the head to 284 it must have registered 283. It runs in `--selftest`, which means CI, which means the first commit that touches the queue. 🔴 **AND IT DEMANDS A CONTIGUOUS RUN, NOT JUST THE NEWEST** — `previous_main` and `version_interval` both resolve by scanning BACKWARD, so a hole in the middle is a wrong answer delivered confidently rather than an absence. 🔵 `--open` still says the sentence at pickup, but it is a reminder with a gate behind it now rather than the fourth louder warning in a document |
| shortfall-cause-reads-prose | DONE | internal | scripts/floor_pin_gate.py | 283 | 284 | — | `FLOOR_PIN_SHORTFALL_CAUSE` reads COMMENTS, so a comment that quotes a refusal sentence is judged as a refusal site that asserts one cause | 🔴 **MEASURED AT 283, ON THIS SESSION'S OWN COMMENT.** After teaching `boundary_gate.mjs` a reply spelling it could not read, 283 wrote a comment explaining the episode and quoted the refusal verbatim — `BOUNDARY_CONSTS_COLLAPSE 15 < 20 — the addon stopped yielding hard-wired fields`. `floor_pin_gate.py` then refused `boundary_gate.mjs:180` for asserting ONE cause over a derived population, pointing at a line that is prose about a refusal rather than a refusal. Rewording the comment cleared it. 🔵 **281's LESSON ONE FILE OVER**: *prose merely CONTAINS the words*, and `COUNTER_PROVENANCE` was given comment-stripping for exactly this. This reader never was. 🟡 **THE DIRECTION IS THE MILD ONE** — a false refusal is loud and costs a cycle, where the silent direction would be a real single-cause refusal hidden inside a string. Both are worth closing, and the fix is the one 281 already built: strip comments before judging, and keep the shortfall reader's population to lines that actually raise 🟢 **CLOSED AT 284, AND THE FIRST DRAFT OF THE FIX WAS WORSE THAN THE DEFECT.** Blanking comments out of the text before running `JS_LITERAL` means the finder runs over text it has never seen, and a JS REGEX literal — `/["\']/`, in two of this tree's own gates — desynchronises the walk that decides what is a string. Measured over the live tree: 6,717 literals dropped and **24 GAINED**, several of them multi-line slabs of code read as one quotation. A reader whose only failure mode was going QUIET had been given a second one of INVENTING, which no floor underneath it can see. 🔵 **SO THE FILTER ONLY SUBTRACTS.** `JS_LITERAL` runs over the ORIGINAL text, unchanged, and a left-to-right pass drops the spans that START inside a comment — nothing can be added by construction, and every surviving body and line number is bit-identical to what shipped. The ambiguity (`"https://x"` is not a comment; ``// see `q` `` is not a literal) is resolved by taking whichever opens FIRST, which is the only reading that cannot disagree with the finder it filters. 🔵 Live population 100 → **98** (68 literal against a floor of 55). The self-check drives 283's own episode as a fixture — the same sentence quoted in a comment and raised in code, in one file, in all three comment dialects — plus the URL case that killed the first draft, and asserts over the whole tree that the filter has invented nothing |
| cs-dap-launch-race-unrostered | DONE | internal | host/test/csdap.test.ts | 284 | 285 | — | `cs_dbg_launch WITHOUT stop_on_entry does not wait for a stop` failed on the node-18 leg of a post-merge run with `undefined` where it expects a session object, and it has no row | 🔴 **MEASURED AT 283 AND DELIBERATELY NOT FILED THERE, WHICH IS THE FIRST THING TO SAY.** The post-merge run at `1a5eb13` needed a cancel-and-re-run because this fired alongside the stuck `host tests` leg (248). 283 saw it, wrote it into its handoff's *where to look next*, and did not open a row — because filing one would have meant a FIFTH PR after the close, which is exactly what §8.4 had just been written about. That is an honest trade and it is also how a finding becomes prose in a document nothing reads, so the row is opened here at 284's first PR instead. 🔵 **IT IS NOT `dap-late-rejection-flake` (275).** That row is the GDScript adapter; this is the C# one, in `csdap.test.js`, and the assertion got `undefined` where it expects `{initialized_seen, session_id: 'csharp', state: 'running'}`. It PASSED on the same commit's PR run and on all three legs of the previous post-merge run. 🔴 **AND THE SUBJECT OF THE TEST IS THE RACE**: it asserts that a launch WITHOUT `stop_on_entry` does not wait, so the thing being measured and the thing that can go wrong under load are the same thing. 🔵 **DO NOT GUESS AT IT FROM HERE** — 275's rule, and 273's session is the argument for it. What this row asks for is the same diagnostic 275 shipped: assert the SHAPE before reading the field, so the next occurrence prints what the tool actually returned instead of a TypeError carrying none of its own diagnosis |
| shape-before-field-uncounted | DONE | internal | host/test/cli.test.ts;host/test/processes.test.ts;host/test/csdap.test.ts;host/test/dap.test.ts;host/test/vcs.test.ts;host/test/knowledge.test.ts;host/test/runtime.test.ts;host/test/initialized_wait.test.ts | 285 | 287 | — | 275 shipped `assert the SHAPE before reading the field` at the one site its flake happened and nobody derived the population it belongs to; 98 sites read a field off a cast `structuredContent` and one of them asserts the shape first | 🔴 **282 §2.3 FOR THE THIRD SESSION RUNNING, ON THE THIRD UNCOUNTED POPULATION.** 283 found 49 `add_child` sites nobody had counted; 284 found 24 `ResourceSaver.save` sites nobody had counted; this is 98. Measured at 285 across `host/test/*.ts`: **103 casts of `structuredContent`, 98 of which then read a field — 28 dereferenced inline as `(res.structuredContent as X).field` and 70 bound to a const first — and exactly ONE asserts the reply is there before reading it.** That one is `dap.test.ts:1487`, written by 275 after `Cannot read properties of undefined (reading 'state')` cost a CI round-trip carrying none of its own diagnosis. 🔵 **THE READING'S OWN LIMIT IS PART OF IT**: the two spellings were counted by two greps, one of the 70 is a HELPER used many times (`cli.test.ts:90`), and a site that casts and never dereferences is correctly out of scope — so 98 is a floor on the class, not a census of it. 🔴 **AND A DEEP-EQUAL IS NOT SAFE JUST BECAUSE IT DOES NOT THROW.** The row this one came out of failed with `undefined` from a deep-equal, not a TypeError, and `undefined` still could not say whether the tool refused or what it refused with — so the class is *an assertion that reads a reply it has not established arrived*, not *an assertion that throws*. 🔵 **WHAT IT ASKS FOR IS A GATE, NOT A SWEEP.** Editing 98 sites is the third time this population gets fixed by hand; a reader over `test/*.ts` that refuses a dereference of `structuredContent` with no `assert.ok` above it in the same block is the thing that makes the guarantee true after the next test is written. `positive_control_gate.mjs` already reads assertion units in this tree and is the natural host 🔴 **AND CI PRODUCED THE ROW'S FIRST INSTANCE WITHIN THE HOUR, IN A FILE THIS ROW NAMED AND 285 HAD NOT TOUCHED.** PR #360 at `7e440df`, `host tests (node 20)`: `not ok 100 - godot_run_project tells NOT WAITED apart from WAITED AND LOST` with `error: "Cannot read properties of undefined (reading 'bridge_ready')"`, a TypeError at `cli.test.ts:164` — one line after the `sc()` helper at `cli.test.ts:90` handed back `undefined`. **That helper is the one this row's own reading singled out** as *one of the 70 is a HELPER used many times*. 934/935 passed; node 18 and node 22 were green on the same commit and the same step, so it is load-shaped, like 275 and 280. 🔴 **AND THE LOG SAYS WHAT JAVASCRIPT DID RATHER THAN WHAT THE TOOL RETURNED**, which is the entire defect: the reply is gone, the cause is unrecoverable, and the only way to learn it is to wait for the flake again. 🔵 **THE REPAIR IS TWO LINES, NOT NINETY-EIGHT, AND THAT SHARPENS THE ROW.** The population has two spellings with very different costs: an inline `(res.structuredContent as X).field` must be guarded per site, but a HELPER guards every call site in its file at once — `cli.test.ts:90` covers **20** and `processes.test.ts:98` covers **12**, so 32 of the 98 were closed by two edits at 285's close, each driven by a positive control first (`the CLI tool returned no structuredContent — {"isError":true,…"godot binary not found…"}`). **The reader this row asks for should classify by SPELLING as well as count**, because the cheap half is worth doing first and the expensive half is the one that needs the gate. 🔴 **THE FLAKE ITSELF IS NOT DIAGNOSED AND IS NOT GUESSED AT** (275's rule): the guard is what makes the next occurrence legible, and nothing in this tree can produce that sentence until it happens again 🔴 **AND THE CHEAP REPAIR COST TWO ORPHANS, WHICH IS A TENSION BETWEEN TWO OF THIS TREE'S RULES AND IS WORTH MORE THAN EITHER.** The first version of the two helper guards used `assert.ok(r.structuredContent, …)`. That is a claim site at MODULE SCOPE — inside no `test()` block and under no section marker — so `tautology_gate` counted two new orphans and refused on ALL THREE legs: `TAUT_ORPHAN_RISE 46 > 44 — 2 claim site(s) reached`. **The ceiling that caught it was pinned at the live value by `orphan-ceiling-headroom` hours earlier, for exactly this reason**, and the next rise it caught was this session's own. 🔵 **RAISING IT WOULD HAVE BEEN THE WRONG FIX EVEN THOUGH THE GATE OFFERS IT.** A guard in a helper asserts nothing about the tree; it refuses to hand back garbage, and the CLAIMS are the `assert.equal(sc(r).field, …)` lines at the call sites, already attributed inside their own blocks. **A `throw` says that; an `assert` misfiles a precondition as a claim and then asks for headroom to hold it.** Both helpers throw now, the diagnostic is byte-identical because `node:test` reports a thrown Error's message in the same `error:` field, and orphan is back to 44/44 with the ceiling unmoved. **The reader this row asks for should emit preconditions as throws, not asserts**, or the cheap half of the repair buys itself a ceiling refusal every time 🟢 **CLOSED AT 287, AND THE UNIT SURVIVED THE REPAIR THIS TIME.** `positive_control_gate.mjs` derives the population from the TypeScript AST — 101 reads of `structuredContent`, each either a raw cast or a call to the guarded `structured<T>()` helper — with `SHAPE_POPULATION_FLOOR` 80 under it and `SHAPE_UNGUARDED_CEILING` 0 over the unguarded half, both swept by `floor_pin_gate`. The sweep was driven FROM the reader rather than by hand: the 94 unguarded casts across nine files became calls to one guarded helper, and `processes.test.ts`'s local copy of that guard became the same helper, so there is one precondition rather than ten. 🔴 **AND FLOORING ON CASTS ALONE WOULD HAVE READ THE REPAIR AS A COLLAPSE.** The first draft counted casts; the sweep took them from 103 to 9; and the floor written to catch a reader that stopped matching fired on the reader working. The population is every READ, which is the unit that does not change when a cast is collapsed into a helper — the unit 285 §6.1 and §9.3 used two of in one row. 🔵 **THE SWEEP FOUND ONE MORE CLASS AND THE SUITE NAMED IT WITHIN THE MINUTE.** `csdap.test.ts` asserts that a REJECTED launch does not report `state: running`, written as an optional-chained read of a cast that may be undefined — the absent envelope is the EXPECTED answer there, and guarding it turned a passing claim into a thrown precondition. An optional-chained read cannot throw, so it is not this rule's population; three sites are classified `optional-chained` and left alone |
| queue-row-drop-is-silent | DONE | internal | scripts/queue_gate.py | 285 | 286 | — | a row missing its trailing pipe removes itself AND eleven others from the population, and `queue_gate.py` prints `🟢 every row parses` and exits 0 over the smaller table | 🔴 **MEASURED AT 285, BY DOING IT — TWICE, WITH THE FILE RESTORED BETWEEN.** Deleting the final pipe from ONE row takes the verdict line from `QUEUE_GATE 138 row(s) at head 285 · 9 OPEN` to `QUEUE_GATE 126 row(s) at head 285 · 8 OPEN`, exit 0, with `🟢 QUEUE_GATE ok — every row parses` printed both times. **Twelve rows and one OPEN row leave the population and nothing says so.** 🔴 **AND THE SENTENCE IS TRUE, WHICH IS WHY IT IS DANGEROUS**: every row the parser could READ did parse. The claim is quantified over the surviving set, so the one shape it cannot see is the shape that shrinks the set — 172 §10.21's floor-at-nothing with a table instead of a count, and 282 §2.3's *a guarantee is false until something derives its population* pointed at the file that holds the population. 🔵 **AND THE ASYMMETRY IS THE WHOLE FINDING, MEASURED ON THIS ROW'S OWN FIRST DRAFT.** A row with TOO MANY cells is refused loudly and by line number — `QUEUE_PARSE line 223: 12 cell(s), 9 column(s)` fired on the draft above, because it quoted a pipe character in its prose. A row with too FEW is not refused at all; it is dropped. **The same parser sees one malformation and cannot see the other**, and only the invisible one changes what the gate is quantified over. 🔵 **WHAT IT ASKS FOR** is the shape `_population.mjs` already uses everywhere else here: derive the population a second way — every line in the table region that begins with a pipe and a space — and refuse when the two derivations disagree, naming the line that could not be read. A floor on the count alone is weaker and needs editing every time a row is added. 🔴 **THE PROVENANCE IS THE ARGUMENT.** This was not found by reading `queue_gate.py`. It was found because 285's own last edit to this file stripped a trailing pipe by accident and the gate stayed green — the session that spent itself on uncounted populations was silently dropped from one, under a gate that said ok 🟢 **CLOSED AT 286, AND THE ROW UNDER-COUNTED ITS OWN DEFECT.** Measured across all 139 positions of the live table: a missing trailing pipe does not cost twelve rows, it costs EVERY row below it. `_cells` answers `[]` and `parse` read that `[]` as *the table ended*, so the loss is POSITIONAL — a malformation at row 0 leaves ZERO rows, at row 50 leaves 50, at row 138 leaves 138, and all 139 report zero problems. The row's `eleven others` was the count at the one position 285 happened to break. 🔴 **AND THAT IS WHY A BIGGER FLOOR IS NOT THE REPAIR**, which the row half-guessed: `QUEUE_ROW_FLOOR` is 20 against 139 rows, so it sees a malformation only in the first 20 positions — 14% of them today and a smaller share every session the table grows. A fixed floor against a growing population decays. 🟢 **WHAT SHIPPED** is the second derivation the row asked for and one it did not: the block now ends at a line with NO PIPE IN IT rather than at a line the reader could not parse, so an undelimited row is refused by line number and costs ONE row; and `block_shape` counts PIPE CHARACTERS where `parse` splits on them, so the two cannot lose a row for the same reason. 🔴 **THE FIRST DRAFT OF THAT SECOND READER SHARED `parse`'s SEPARATOR RULE AND WAS MEASURED AGREEING WITH IT** on a row of dashes — two readers with one blind spot are one reader (161). Making the separator POSITIONAL rather than a character class exposed a SECOND silent drop this row never named: a dash-only row is read as the markdown header separator ANYWHERE in the block and skipped without a word. 🔵 **AND THIS CLOSING NOTE BROKE THE TABLE WHILE BEING WRITTEN, AT LINE 223, WHICH IS THE LINE 285's DRAFT BROKE IT AT** — it quoted the separator literally, three pipe characters landed in a prose cell, and BOTH new derivations named it independently: `QUEUE_PARSE line 223: 12 cell(s), 9 column(s)` and `QUEUE_BLOCK_SHAPE line 223: 13 pipe character(s), 10 required`. The row's own subject, third time, caught twice. 🔴 **AND THREE OF THIS SESSION'S OWN INSTRUMENTS REFUSED THE WORK.** `QUEUE_BLOCK_VACUOUS` exists because blinding the new derivation to `(0, [])` still made `n_block != len(rows)` true, so the disagreement claim PASSED under the blind — a reader that counted nothing satisfying the claim that it disagreed (172 §10.21, inside the instrument written to refuse that). `instrument_gate` then refused twice more: `{SIG:_table}` and `{SIG:_pad_paths}` CRASHED the self-test rather than failing it, because the fixture builder indexed a list the blind had emptied — 285 §5.7's rule one step earlier, a self-test that dies before its first assertion — and the late-live axis found `block_shape` is called TWICE on the live path, so a reader answering honestly once and empty afterwards left `check()` silent and printed *a second derivation counted the same 0 row(s)* under a green gate. That is 245 §3 verbatim, one reader over, found by the harness rather than by its author. 🔵 **THE OK LINE NO LONGER SAYS ONLY `every row parses`** — it says a second derivation counted the same number off the same block. `queue_gate --selftest` 75 → 83 claims; five blinds redden, zero crash |
| block-counters-without-readers | DONE | internal | scripts/handoff_gate.py | 286 | 287 | — | 285's status block claimed `difference_field 28 population / 5 unreachable / 5 declared` and `orphan 44/44`, and neither binds to a `COUNTER_READERS` row — two numbers the block asserted that nothing in this tree can check | 🔴 **FOUND BY THE CLOSE, NOT BY READING.** `UNREADABLE CLAIM` fired on both when 285 was closed at 286: *a claim in the status block that nothing in this tree can check*. Both are real measurements from real instruments 285 shipped or pinned — `difference_field_gate.mjs` prints its population and `ORPHAN_CEILING` was deliberately pinned at the live value — so the counters deserve readers rather than deletion. They were dropped from the block to get the close green, which is the weaker of the two answers the gate offers. 🔵 **IT IS §5.6's CLASS ON THE COUNTER AXIS.** That refusal accepts a roster entry or a declared exemption and nothing else; this one accepts a reader or silence, and 285 took a third option the gate does not have — restating a number with no instrument behind it. A session that ships a gate owes its counters a `COUNTER_READERS` row in the same PR, for the same reason it owes an `INSTRUMENTS` entry 🟢 **CLOSED AT 287, AND 246's TABLE IS WHY IT COST ONE SESSION RATHER THAN TWO.** Both counters have `COUNTER_READERS` rows now — `taut.orphan` on the `TAUT_ATTRIBUTED … orphan=44/44` line `tautology_gate.mjs` has printed since 258, and `difference_field.population` on that gate's own three-number line. 🔴 **THE ROW'S SUBJECT WAS AN ORDERING, NOT TWO NUMBERS.** `ALIAS_UNUSED` refuses a reader no real block reaches, and a block only becomes real when the NEXT session registers it, so a counter and its reader can never land in the same PR — which is why striking both counters looked like the cheaper answer to 285, and why two numbers an instrument was already printing stopped being reported at all. `ALIAS_PENDING`, built at 246 for exactly this one-session gap, carries both keys until 288 registers 287's block and `ALIAS_PENDING_STALE` deletes them by refusing rather than by anyone remembering. Both rows are `SINCE(287)`: 287's block is the first that can carry them |
| task-tool-confirmation-crashes | DONE | user | host/src/mutation-guard.ts;host/src/tasks.ts;host/test/mutation_guard.test.ts | 284 | 284 | — | `godot_run_headless_script` called without `confirm` answered `Cannot read properties of undefined (reading 'taskId')` — a raw JS TypeError where the confirmation refusal should be | 🔴 **MEASURED AT 284 AGAINST A LIVE GODOT 4.7**, driving the packed 1.83.0 tarball over a real MCP handshake the way the guide tells a user to. No prompt, no remedy, nothing naming the tool. With `confirm: true` the identical call ran fine, which is what made the shape obvious. 🔵 **282 BUILT THE BRANCH AND COULD ONLY DRIVE HALF OF IT.** `typeof handler !== "function"` is FAIL-OPEN and `registerTaskTool` hands over a `{createTask,…}` object, so this tool — the one on this surface that EXECUTES a GDScript — had fallen through both guards with no confirmation at all. 282 gave both guards a task-handler branch and was right to. What a container cannot call is the guard actually BLOCKING: `createTask` has a return type, `{ task }`, the SDK reads `.task.taskId` straight off it, and the gate was returning a ToolResult there. 🔴 **THE POPULATION IS ONE TOOL, WHICH ARGUES FOR THE SEAM AND NOT AGAINST IT** — destructive ∩ task-registered. The moment `godot_export` is annotated destructive, and it writes a caller-supplied path, it joins, and nothing would have said so. 🟢 **CLOSED BY ANSWERING IN THE TASK'S OWN CURRENCY**: the task is created and its result stored `failed` in the same tick, so a task-aware client sees an ordinary failed task and a plain client's auto-poll returns refusal text byte-identical to the other 73 gated tools. Throwing would have been fewer lines and would have made one tool answer a confirmation refusal in a different dialect. `wrapEntry` now owns the shape difference in ONE place — callers hand it a `check` — so the pause latch, which had the identical defect on the identical tool, is repaired by the same seam rather than a second copy of the reasoning. Verified live after the change |
| destination-exists-silently-destroys | DONE | user | addons/breakpoint_mcp/operations.gd;host/src/schemas.ts;scripts/contract_check.py | 284 | 284 | — | Nineteen of the twenty-six tools that take a destination path have no `overwrite` and no `existed` report; eleven were measured DESTROYING an existing file and answering exactly as they answer a fresh create | 🔴 **MEASURED AT 284 AGAINST A LIVE GODOT 4.7, NINE FOR NINE ON ONE BATCH.** Nine resources were created, a sentinel line appended to each on disk, and each tool called a second time with identical arguments: every sentinel was gone and every reply read `created` / `saved` / `packed`, the same words as the first call. `resource_create` overwrote an `Environment` a property had been set on, resetting it — and then, asked for a `StandardMaterial3D` at the same path, silently turned that resource into a material, so anything referencing it as an Environment now holds the wrong type. `scene_new` replaced a saved scene on disk while the editor's open copy still showed the node that had been added, so the file and the editor disagree and nothing says so. 🔴 **THIS IS 283 §1 ONE AXIS OVER AND WORSE.** 283 found `node_add` answering a name collision by inventing `@Type@N`; the collision here is a FILE and the answer is deletion. 283's finding-to-carry was that *no instrument calls the tool twice* — this is the same blind spot, found the same way, on a population nobody had counted. 🔵 **THE POPULATION IS DERIVED, NOT GUESSED**: 26 tools take a destination path, 7 have `overwrite`, 19 do not; and underneath them are **48 `ResourceSaver.save` call sites** in `operations.gd`, none of which asks whether anything is there — the same roster shape as 283's 49 `add_child` sites, which is the argument for one `_commit_save` seam rather than nineteen edits. 🔴 **AND THE PRODUCT GIVES FOUR DIFFERENT ANSWERS TO ONE QUESTION**: `filesystem_move` refuses `exists` with no way to force; the 7 scaffold tools refuse `exists` unless `overwrite`; `filesystem_create_dir` succeeds and reports `existed: true`; the other 19 destroy silently. 🟡 **SCHEDULED RATHER THAN OPEN, DELIBERATELY.** This changes shipped behaviour on 19 tools and is his to steer, and it was priced to him at 284 in four options — refuse-by-default with `overwrite`; report `existed`/`replaced` and keep overwriting; both; or split by blast radius. 284's recommendation is BOTH: the refusal is the half that matters and the field is nearly free once the seam exists, because *I created a file* and *I overwrote your file because you asked* should not be the same sentence. Parked on 285 rather than left OPEN so that `QUEUE_TIER_ORDER` is not made to refuse three internal rows already closed at 284 — a live user row would do exactly that, and the ordering rule is about what a session SPENDS itself on, not about what it records 🟢 **CLOSED AT 284 — HE PICKED OPTION C: REFUSE BY DEFAULT, `overwrite` TO FORCE, AND REPORT `replaced` WHEN IT DID.** The twenty-four `ResourceSaver.save` sites collapse to one seam, `_commit_save`, with `contract_check` check 35 refusing a second — 283's `_commit_add` repair applied to the population found one session later. 🔵 **THE TWO KINDS ARE A PROPERTY OF THE CALL SITE, NOT OF THE ARGUMENTS**: `SAVE_NEW` is a destination the caller named and a collision there is somebody else's file; `SAVE_BACK` is the resource's own home, loaded a few lines above, where overwriting IS the operation. A flag the caller could set would let a caller turn `theme_set_color` into a refusal; a constant at the site cannot. Check 35 asserts BOTH kinds stay live, because an all-`SAVE_BACK` tree passes a singularity test while gating nothing. 🔴 **AND THE POPULATION TEST FOUND ELEVEN MORE THE SESSION HAD NOT COUNTED.** Derived from the registered surface rather than typed, it showed that the seven scaffolding writers and four tabletop template writers already REFUSED a collision correctly and had no way to say when they had ACCEPTED one — a fifth spelling of the same question, `created: false`. They report `replaced` now too, and `mp_wire_rpc` split out of its shared output shape because it edits an annotation inside an existing script and has no destination at all: one shape for two jobs is how a symmetry claim becomes unfalsifiable. 🔴 **`token-cost` REFUSED THE PROSE BEFORE IT REFUSED THE FEATURE, ON THE SAME DIGIT AS 283** — the first `overwrite` description put schema-per-tool at 492 B against 490, exactly where 283's `NAME_TAKEN_CLAUSE` draft landed. Cut to four words behind one shared literal with the essay in TOOL_CATALOG's Conventions; `BYTES_CEILING` then raised 374,208 → 376,320 with the note its own refusal text demands. 🟢 **VERIFIED LIVE IN FOUR DIRECTIONS** against Godot 4.7: nine sentinels SURVIVED a second identical call where nine had been lost; `overwrite: true` succeeds and reports `replaced: true`; `resource_set_property` and `theme_set_color` still work repeatedly; and `resource_save` with no `to_path` is still a save-back to itself |
| ledger-population-with-two-producers | DONE | internal | scripts/scope_gate.py | 282 | 284 | — | `SCOPE_LEDGER` declares each population collapsible by blinding ONE reader, and `xlang.remedy_cli_refs` now has two producers | 🔴 **MEASURED AT 282, BY A ONE-SENTENCE EDIT.** `_remedy_grammar` harvests every `` `breakpoint-mcp …` `` span out of every remedy into `xlang.remedy_cli_refs`, and it is called for BOTH the addon table (`remedy_tables`) and the host's `*Remedy` exports (`host_remedy_sentences`). Writing a `doctor` pointer into a host remedy therefore gave that population a second producer, and blinding `remedy_tables` stopped collapsing it: `SCOPE_GATE_BLAST remedy_tables: declared 71 … observed 70` and `SCOPE_GATE_LEDGER remedy_tables: … did NOT: ['xlang.remedy_cli_refs']`. 🔵 **THE GATE IS RIGHT AND THE LEDGER'S SHAPE IS WHAT CANNOT SAY IT.** `LEDGER: dict[str, tuple[str, ...]]` maps ONE blind to the populations it must empty; there is no way to write *these two blinds together empty this one*, and no single existing blind empties it. The natural repair is to make the shared producer blindable — but `targets()` skips any `def` whose name starts with `_`, and `_remedy_grammar` returns `None`, which `EMPTY` has no row for. So the repair is two edits in `scope_gate.py` (admit a leading-underscore shared producer, or spell `None`) plus a signature change in `contract_check.py`, and it is worth doing deliberately rather than under a session that only wanted to add a sentence. 🔴 **AND THE COST OF NOT DOING IT IS ALREADY PAID ONCE**: 282 moved the pointer out of the remedy into the failure sentence beside it, which keeps the user-facing text identical and takes that span OUT of check 28's join — the reachability of `breakpoint-mcp doctor` is no longer checked by anything. That is a real, small loss of coverage, taken knowingly, and written down here so it is a decision rather than a drift 🟢 **CLOSED AT 284, AND THE ROSTER OF ONE WAS THE REAL DEFECT.** `targets()` read `if name.startswith("_") and name != "_tracked_modes"` — one private helper admitted BY NAME, with nothing anywhere saying what made it different from every other underscore `def` in the file. Admitting a second meant either typing a second name or answering the question the first never had to. 🔵 **THE ANSWER WAS ALREADY IN THIS GATE**: `LEDGER` declares, per blind, the populations that blind must empty, so a private helper is blindable exactly when the ledger names it — derived, and justified where `roster_problems` already compares it to the tree. `EMPTY` learned `"None": "None"`, which is the sixth session running to add a shape it could not spell and the first that is a SHAPE rather than a type: a producer that returns nothing and appends into module-level accumulators is still an enumerator, and its empty answer is the one where the loop never runs. 🔴 **AND `xlang.remedy_tool_refs` WAS IN THE SAME POSITION AND HAPPENED TO STILL COLLAPSE** — no host remedy names a tool in backticks today. That is a fact about the current text of two strings, not a property of the code; both populations moved to `_remedy_grammar`, which actually owns them, so the declaration stops resting on luck. 🔵 **THE COVERAGE 282 GAVE UP IS BACK**: the `` `breakpoint-mcp doctor` `` pointer returns to `godotSpawnRemedy`, so its reachability is joined to the CLI surface again, and the user-facing sentence is byte-identical to what shipped. 🔵 251's three rows were charged in full — a LEDGER entry, a BLAST of 2 for the new blind, and the raise a session forgets: `cli_surface` 7 → 8, widened by a population the edit does not look like it touches |
| spawn-failure-kills-the-server | DONE | user | host/src/spawn-guard.ts;host/src/tools/cli.ts;host/src/tools/processes.ts;host/src/peers.ts | 282 | 282 | — | A `GODOT_BIN` that does not exist takes the whole MCP server down with an unhandled `'error'` event, after the tool has already answered `launched: true` | 🔴 **MEASURED AT 282 BY INSTALLING THE PUBLISHED 1.82.1 TARBALL AND FOLLOWING `USER_GUIDE.md`** — the first-run misconfiguration the README itself anticipates (*Set `GODOT_BIN` if `godot` isn't on your `PATH`*), and the one every macOS user whose Godot is an `.app` starts with. `godot_launch_editor` answered `{"launched": true, "pid": null}` and then the process died on `Error: spawn … ENOENT`; the next request never got a reply. Two defects compounding: `spawn` reports a failure to START asynchronously on the child's `'error'` event, so a caller that reads `child.pid` and returns has answered before the failure exists — and a child with no `'error'` listener re-throws on the process. Three call sites had grown it independently (`launchDetached`, `ProcessRegistry.run`, and the peer spawner through it) because nothing said where a Godot spawn is allowed to live. 🔴 **AND `godot_version` RENDERED THE SAME FAILURE AS AN EMPTY SUCCESS**: `execFile` reports ENOENT with `code` set to the STRING `"ENOENT"`, so `typeof e.code === "number"` gave `code: null` — the value a signal-killed child also produces — and `e.stderr ?? e.message` never reached the message because the promise wrapper attaches `stderr: ""`, which is not nullish. An assistant relaying it told the user the version was EMPTY rather than that Godot was not installed. 🟢 **CLOSED WITH A SEAM AND A DERIVED POPULATION**: `spawnGuarded` attaches both outcome listeners in the statement that creates the child and resolves only once the outcome is known; `spawn_failed` is the third state `runCaptured` was missing; and `spawn_guard.test.ts` walks every `.ts` under `src/` and refuses any `spawn(cfg.godotBin` outside that one file, so the fourth call site is refused before it ships. 🔵 The refusal names the next action (254) and check 28's grammar arm refused the first draft of it for opening with *Cannot* — the description is not the remedy |
| doctor-green-on-what-it-did-not-check | DONE | user | host/src/cli/doctor.ts | 282 | 282 | — | `doctor` prints *All required checks passed* and exits 0 over a run in which the addon checks were never produced, and passes any binary that spawns | 🔴 **TWO FALSE GREENS IN THE COMMAND THE README TELLS USERS TO RUN TO VERIFY THE SETUP.** Measured on the published 1.82.1. (1) Run from the wrong directory — which is the documented invocation, a bare `breakpoint-mcp doctor` — `project` skips, `checkAddon` returns that one row and `addon-installed`, `addon-version` and `addon-enabled` are never created; `checks.every(..)` is vacuously true about members that are not in the population, so the summary and the exit code came back byte-identical to a correctly-installed project. It is `glyph()`'s own defect one level up, in the file that fixed `glyph()`: a check that was never RUN is the same contradiction with nothing on screen to contradict. (2) `checkGodotBinary` treated any non-spawn-error as a pass without reading the output — `GODOT_BIN=/bin/ls` produced `✓ godot-binary  /bin/ls → ls (GNU coreutils) 9.4`, and a real Godot 3.5 passed a check whose own failure hint says *Install Godot 4.2+*. 🟢 **CLOSED**: a check DECLARES what its non-pass withholds, `withheldChecks` derives the report's list by subtracting what the report actually carries (so a gate that PASSED withholds nothing without anybody editing a list), `ok` and the exit code turn on it, and the summary names the withheld set BEFORE the failed set because *never ran* is the stronger statement than *ran and failed*. The binary check parses the version and enforces `MIN_GODOT`, which is the one literal the guide's sentence and the branch both read |
| doctor-hints-the-text-renderer-drops | DONE | user | host/src/cli/doctor.ts | 282 | 282 | — | `doctor` renders hints only on `fail` rows, under a footer that says *see the ↳ hints above*, and the in-session hint names a flag that surface does not have | 🔴 **THE REMEDY FOR THE MOST COMMON FIRST-RUN MISTAKE EXISTED AND WAS FILTERED OUT OF THE ONLY SURFACE A FIRST-RUN USER READS.** `if (c.status === "fail" && c.hint)`: the `skip` on `project` carries *Pass --project …*, the `ok` on `addon-version` carries *the addon is ahead of this host — upgrade the host rather than overwriting*, and both were reachable only through `--json` or the `breakpoint_doctor` tool. 🔴 **AND THAT HINT NAMED A FLAG THAT DOES NOT EXIST ON ONE OF ITS TWO SURFACES**: the same `Check` is returned by the MCP tool, whose input schema is `additionalProperties: false` over `require_live`, `live_level`, `include_csharp`, `timeout_ms` — an assistant reading the remedy would try an argument that cannot be accepted. 🟢 **CLOSED**: hints render on every status that has one, and the hint names both remedies because the reader is sometimes a terminal and sometimes not |
| cli-flag-vocabulary-unenforced | DONE | user | host/src/cli/usage.ts;host/src/cli/init.ts;host/src/cli/doctor.ts | 282 | 282 | — | `--help` documents `--trust secure`, the parser's alias map spells it `safe`, and the mismatch exits 0 — on the flag that sets the security posture | 🔴 **THE TOOL REJECTED THE VALUE ITS OWN HELP NAMES FIRST, AND NAMES AS THE DEFAULT.** `usage.ts` said `--trust <level>  secure or full`; `init.ts`'s map had `safe`, `none`, `off`, `full`, `all` and no `secure`. So `breakpoint-mcp init --trust secure` fell through to the `--privileged-groups` parser and printed *ignoring unknown trust group(s): secure (valid: code-execution, all)* — the wrong next action, in a DIFFERENT FLAG'S vocabulary — and exited 0, while `--client bogus` exits 1 and `--surface bogus` and `--require-live=yes` exit 2. 🟢 **CLOSED BY MAKING THE HELP LINE A RENDERING OF THE PARSER'S OWN MAP**, which is what `usage.ts`'s header already argues for in as many words: *the rosters below are not documentation — they are the parser's input*. A documented value the parser rejects is now impossible rather than merely untested, and the test reads the shipped help text back and drives every token it offers. An unknown level is refused in its own vocabulary and exits 2. 🔵 Folded in, same class: `doctor --timeout abc`, a bare `--timeout` and a bare `--project` all fell back to a default in silence — a user who mistypes the timeout read the result as a measurement taken the way they asked for |
| mutation-guards-narrower-than-their-docs | DONE | user | host/src/mutation-guard.ts;host/src/confirm.ts;host/src/index.ts;host/src/tools/vcs.ts;docs/USER_GUIDE.md;docs/TOOL_CATALOG.md | 282 | 282 | — | Two shipped safety guarantees are measurably false: the pause latch holds only the gated tools, and 23 of 89 destructive tools take no `confirm` at all | 🔴 **BOTH MEASURED FALSE ON THE PUBLISHED 1.82.1, BY DRIVING THE TARBALL OVER STDIO.** `USER_GUIDE.md` §9 promises the SIGUSR1 latch holds mutating actions *across the **whole** tool surface*; it lived inside `gate()`, which runs only for confirmation-gated tools. Timed run from `BREAKPOINT_START_PAUSED=1`: `node_add` DISPATCHED at t=1.00s, `scene_save` at t=1.50s, while `node_delete` was correctly HELD until SIGUSR2 at t=7.00s — 111 of the 279 secure-default tools are neither read-only nor gated. `docs/TOOL_CATALOG.md` promises *every tool flagged destructive accepts an optional `confirm` … so a destructive op is never executed silently*; `tilemap_clear`'s input properties on the wire were `['path']`, with no `confirm` for any caller to pass. 🔵 **HIS DECISION, PRICED AT 282 IN PLAIN LANGUAGE AND TAKEN: honour both docs** rather than gate-only, withdraw-both, or latch-only. 🟢 **CLOSED WITH TWO SEAMS AND NO ROSTER**, because a roster of tools-to-gate is a population somebody has to keep true and *nobody added the parameter* is exactly what a roster fails to prevent: `applyPauseLatch` holds every tool whose published `readOnlyHint` is false (fail-safe — an unrostered name is held, never let through) and `applyDestructiveGate` gives every `destructiveHint` tool that declares no `confirm` both the parameter and the gate. The population is `annotationsFor`, the table `tools/list` already publishes, so the surface a client is TOLD is destructive and the surface that gates are one set. 🔴 **AND THE FIRST DRAFT OF BOTH GUARDS DECLINED TO COVER THE MOST DANGEROUS TOOL ON THE SURFACE AND SAID NOTHING** — `typeof handler !== "function"` is FAIL-OPEN, and `registerTaskTool` hands a `{createTask, getTask, getTaskResult}` bag, so the three D2 task tools fell through and `godot_run_headless_script`, which EXECUTES a GDScript, kept no confirmation. Found by the new test on its first run. 🔴 **`vcs_stash op=push` MOVED FROM UNGATED TO GATED ON THIS SESSION'S OWN EVIDENCE**: the probe that proves the gates called it unattended against this repository and reverted a day of uncommitted work out of the working tree, recovered only because somebody knew to look in `refs/stash`. Reversible-with-`pop` is not the same predicate as *never executed silently* |
| tarball-references-files-it-does-not-ship | DONE | user | host/package.json | 282 | 282 | — | Every `dist/**/*.js` ends in a `sourceMappingURL` comment and `files` ships no `.js.map`, so every published file points at something that is not there | 🔵 **SMALL, MEASURED, AND REAL.** `npm pack breakpoint-mcp@1.82.1` lists 93 files and zero `.map`; `tsconfig.json` sets `sourceMap: true`, so every emitted file carries the reference. Harmless at runtime and not harmless in a bug report: a user's stack trace resolves to compiled output nobody can read back to a line. Closed by shipping the maps the emitted files already name — 728K, against a package whose point is being debuggable |
| counter-provenance-unreached-by-the-pickup | DONE | internal | scripts/handoff_gate.py | 282 | 282 | — | The pickup's provenance sentence looks up an atom's TEXT in a table keyed by reader name, so the intersection is zero and `.get(k, TRACKED)` answers with the safest class every time | 🔴 **FOUND AT 282's PICKUP, BY THE PICKUP, ONE SESSION AFTER IT SHIPPED — AND IT HAS NEVER ONCE BEEN RIGHT.** 281 closed `counter-provenance-undeclared` by deriving what each of the thirty-five counters is a fact ABOUT, and the payoff (§2.4) was that the inheriting session is told at the moment it inherits which of them can honestly disagree at the close. The line printed *35 of the 35 are declared TRACKED and cannot* over a table declaring 24 INDEX, 1 CLONE and 1 MACHINE. `atoms` holds the block's atom TEXT — `'904/904'`, `'contract 30/30'`, `'26 CI jobs'` — and `COUNTER_PROVENANCE` is keyed by READER NAME; `keys`, built ten lines above by `bind()`, is the population the table is written in, and the intersection of `atoms` with the table is EXACTLY ZERO. 🔴 **A DEFAULT IS WHAT MADE IT SILENT.** `.get(k, TRACKED)` turns *this key is not in the table* into *this counter is the safest class there is* — unread reported as green, which is the rule 281 wrote for `UPSTREAM_DEFERRED` two hundred lines away in the same file. A bound key with no row is a problem now, so the table cannot go quiet about a counter again. 🔵 **AND WHAT WOULD HAVE CAUGHT IT IS WHAT 281 ITSELF CONCLUDED** — *a derivation is not more trustworthy because it is derived; only when something compares it to a fact the tree already holds*. The tree held the fact: §2.1's own table. Nothing compared them, so `PROVENANCE_PICKUP_POPULATION` in `--selftest` does. The corrected reading at 282's pickup is 8 of 34 TRACKED and 26 that can honestly disagree |
| terminology-exemption-by-citation | DONE | internal | scripts/terminology_gate.py | 279 | 279 | — | A tracked file is excused from the terminology sweep for containing the string `LANDSCAPE_TRACKING_POLICY.md`, so naming the policy — in a comment, a docstring or a `paths` cell — buys the exemption on its own | 🔴 **FOUND BY THIS SESSION EARNING IT TWICE BY ACCIDENT.** 279 referenced the policy in `QUEUE.md` and in `scripts/assetlib_sweep.py` while working on something else; both left the swept population, 310 files became 308, the tracked total was unchanged, and the gate printed 🟢 on both sides of the change. 🔴 **AND A THIRD FILE HAD BEEN HOLDING THE EXEMPTION FOR FREE** — `docs/mcp_method_ledger.json` names the policy, carries no retired term, and had been out of the sweep for as long as the predicate has existed. 🔴 **THE AGGREGATE IS WHY IT WAS INVISIBLE**: `TERMINOLOGY_EXCLUSION` already refuses an exclusion carrying zero occurrences, but it asks that of the whole SET, so a file excused with nothing in it hides behind the seventeen legitimate hits in the files that really do state the rule. A column with no per-member predicate cannot be wrong about a member (276), and this one was wrong about three. 🟢 **CLOSED AT 279 BY DERIVING THE PREDICATE FROM THE EXEMPTION'S OWN REASON**: a file is excused because it has to SPELL the retired word in order to retire it, so it must do both — name the policy AND carry an occurrence. Three files re-admitted, all clean, sweep 310 -> 311; three fixture claims drive the predicate with each half removed |
| tool-count-cap-blocks-capped-clients | DONE | user | README.md;docs/USER_GUIDE.md;host/src/client-caps.ts;host/src/tool-census.ts;host/src/index.ts;host/test/client_caps.test.ts | 289 | 289 | — | Some AI clients refuse a server whose tool list is longer than a fixed limit, and they refuse the whole server before it runs — so on those clients Breakpoint does not appear, does not warn, and prints nothing the user could search for | 🔴 **A TOTAL FAILURE FOR A WHOLE CLASS OF CLIENT, WITH THE FIX ALREADY SHIPPING AND NOBODY ABLE TO FIND IT.** Google Antigravity caps a server's tool list at 100 and answers `enabled tools would exceed max limit of 100` without loading anything (google-gemini/gemini-cli #26678, filed 2026-05-07); VS Code blocks agent mode once every enabled server's tools together exceed 128 (microsoft/vscode #290356). The default surface is roughly three times the smaller of those. `BREAKPOINT_TOOLSETS` has been the remedy since it shipped — and at 288 the number 100 appeared nowhere in the repository, no client was named, and `docs/USER_GUIDE.md` mentioned the setting zero times. 🟢 **CLOSED BY FOUR THINGS, EACH THE CHEAPEST VERSION OF ITSELF.** (a) A *Tool limits* section in `README.md` and a troubleshooting entry plus a configuration row in `docs/USER_GUIDE.md`, quoting Antigravity's refusal **verbatim** so a search for that string lands here. (b) Two presets measured and published — `d` → **58** (both language servers and both debuggers) and `b,c,d` → **92** (that plus the CLI and runtime planes) — both under every cap in the table, where the only figures published before were `c` → 27 and `a,b` → 155, one below the cap and one over it. (c) A startup line on stderr printing the count actually registered, from `tool-census.ts`, installed INNERMOST so it counts what survived the toolset filter and the capability drops rather than what was attempted — the number a capped client judges the server by, observable from the server's own output for the first time. (d) The pin: `host/test/client_caps.test.ts` re-derives every published preset against `SMALLEST_CLIENT_CAP` on every run and joins the README's table to `client-caps.ts` in both directions, so a tool added to `lsp`, `dap`, `cli` or `runtime` reddens a test here instead of an editor there. 🔵 The caps themselves are somebody else's numbers and the module says so, with the issue each was read from and the date |
| subject-derivation-matched-its-own-source | DONE | internal | scripts/handoff_gate.py | 289 | 289 | — | The MACHINE idiom list carried one bare token, so the subject derivation answered *this number is a fact about the runner* about the very file that defines the derivation | 🔴 **THE SPLIT HALF OF `subject-derivation-reads-a-file-not-an-invocation`, MEASURED AT 282 AND CLOSED HERE.** `cpu_count` was the only bare token in `SUBJECT_SIGNALS[MACHINE]`; every sibling idiom beside it is call-shaped or module-qualified (`time.time(`, `Date.now(`, `os.cpu`, `process.hrtime`). Bare, it matched exactly two things, neither of them a reading of a machine: **the pattern's own source text**, and **the self-test fixture that drives the derivation** — so a derivation over `scripts/` had the derivation in its corpus. 🟢 **MEASURED BEFORE AND AFTER, NOT REASONED ABOUT**: `subject_of(handoff_gate.py)` answered `MACHINE ['cpu_count']` before and `CLONE ['"git" , "config"', '"git" , "remote"', 'core.hooksPath', 'get-url']` after — the network and configuration path `--selftest` does not reach, which is the honest answer for the whole file. `--selftest` held at 476/476 with no fixture edit, because the fixture is a derivation RESULT rather than corpus text. `handoff.claims` stays in `SUBJECT_UNDERIVABLE` and its reason was rewritten to the re-measured answer: a reason that has expired reads exactly like one that holds (279 §9) |
| blast-is-a-fact-about-the-runner | DONE | internal | scripts/instrument_gate.py;scripts/handoff_gate.py | 289 | 292 | — | Four sessions in a row have corrected the `instrument.blast` counter from the number their own machine measured to the number CI measured at the same commit, and nobody has measured why the two disagree | 🔴 **FILED AT 289, OWED SINCE 287.** 287 §6 said this would be opened at 288's pickup or by whoever measured it first; 288 was an analysis session and opened no rows, so the row that records a repeating correction has itself been the thing nobody recorded. 🔵 **WHAT IS KNOWN**: the correction is always in the same direction and always at the same commit, which is the signature of a reader whose subject includes the runner rather than of a tree that moved — `COUNTER_PROVENANCE` declares `instrument.blast` INDEX, and INDEX is the class that is equal on every machine at a committed clean tree. Either the declaration is wrong and the counter is MACHINE-class, or something in the local tree is not what CI checks out. **One measurement decides it**: run the instrument locally at a commit CI has already measured and diff the per-site blast, rather than correcting the total a fifth time. 🟢 **ANSWERED AT 291 AND CLOSED AT 292 — THE ROW'S OWN TITLE WAS THE ANSWER.** 291 §3.1 ran exactly the measurement this cell asked for and took SEVEN readings of ONE commit (`a50d968`): a Cowork container answered **2937** four times — two separate clones, two separate `npm ci` installs, two runs back to back — and GitHub Actions answered **2928** three times, on three runners across three Node majors, one of them a re-run an hour later. 🟢 **ALL TWENTY-THREE PER-INSTRUMENT LINES WERE BYTE-IDENTICAL ACROSS THE FOUR CONTAINER RUNS** — not the total agreeing by luck, every row — and the three CI legs agreed with each other exactly. 🔴 **SO THE COUNTER IS A DETERMINISTIC FUNCTION OF (TREE, MACHINE CLASS) AND OF NOTHING ELSE THIS APPARATUS VARIES.** Every hypothesis the row carried is dead: it does not move across a re-run, a clone, an `npm ci`, a Node major, a runner instance or an hour; it moves across the machine. Of the two branches this cell offered — *the declaration is wrong* or *the local tree is not what CI checks out* — neither is it: the tree is identical and `INDEX` is what the reader's own SOURCE derives, because the machine dependence lives downstream of the source, in how the runner interleaves a test process. A derivation over source text can never see that, which is why six sessions of correcting the total could not separate it from noise. 🔴 **WHAT REPLACES THE CORRECTION IS A SOURCING RULE, WRITTEN WHERE THE COUNTER IS DECLARED**: `instrument.blast` is read out of `ci<N>/ci-measured-host-tests-*/`, never out of the local replay — recorded in the row's own `why` in `COUNTER_READERS`, so the next session finds it at the counter instead of rediscovering it at the close. 🔵 **AND `leg_disagreements` IS NOT THE READER THAT WAS MISSING**: it compares CI artifacts against each other and those three agree, so the disagreement is CI-versus-elsewhere and no comparison between artifacts can reach it. 🔵 What is left over is smaller and is not this row: WHICH difference between the two machine classes moves the count by nine — CPU count and therefore `node:test` concurrency is the first suspect, since the number counts FAILURE LINES and a differently-interleaved run can truncate or repeat them. The row asked whether the disagreement was real and where it lived. **It is, and it lives in the machine.** |
| undeclared-ceiling-stopped-tracking | DONE | internal | scripts/queue_gate.py;scripts/floor_pin_gate.py | 289 | 295 | — | The ceiling on how many live rows may skip the `paths` evidence is thirteen and only one row still skips it, so a number written to fall with its population has stopped following it | 🔴 **MEASURED AT 289, INCLUDING THE PRICE OF FIXING IT, WHICH IS WHY THIS IS A ROW AND NOT AN EDIT.** `UNDECLARED_CEILING` was set to the number live when the `paths` column landed at 271 and lowered at 272 and 274 *by the rows that closed with their paths declared*. 289 closed one and re-pointed another, taking the live count to **one** — `review-charter-p1-p6`, which cannot simply declare, because its work touches the shipped server and declaring derives the `user` tier, and a live `user` row freezes every internal close in this table (`QUEUE_TIER_ORDER`). 🔴 **AND THE LOWERING WAS ATTEMPTED AND REVERTED, WITH THE COST MEASURED RATHER THAN GUESSED**: at a ceiling of one, `queue_gate.py`'s own positive control `QUEUE_CLEAN` reddens, because the clean fixture carries two live rows with no `paths` and about forty claims build on it. That is 184 §7's rule arriving on schedule — *lowering a FLOOR-shaped ceiling can cost ten absolute pins in a self-test; price it before starting* — and the price is fixture surgery, not a digit. 🟢 **THE SHAPE OF THE FIX**: give the fixture rows declared paths that survive the `internal`→`user` rewrite `GOOD_USER` performs, then take the ceiling to the live value in the same commit. 🔵 Do it in a session with no release in it 🟢 **CLOSED AT 295 — THE FIXTURE WAS PAID FIRST AND THEN THE DIGIT MOVED, WHICH IS THE ORDER 289 NAMED.** `_GOOD_ROWS`' two live rows (`alpha`, OPEN; `beta`, SCHEDULED) now declare `paths`, substituted PER TIER rather than shared: `QUEUE_REACH_JOIN` derives the tier FROM the paths, so `scripts/queue_gate.py` (`internal`) serves `GOOD` and `host/src/tools/vcs.ts` (`user`) serves `GOOD_USER`, and a single shared list would have made one of the two variants pass with evidence that contradicted its own typed cell. `UNDECLARED_CEILING` 13 -> 1, the live value, with its `SIZE_LEDGER` reason rewritten in the same commit (293 §5.1). 🔴 **AND ONE CLAIM HAD TO STOP BORROWING THE FIXTURE**: `TABLE_PADS_PATHS` read `GOOD[0]` for its eight-cell row, so a claim about the PADDING HELPER was silently a claim about the shape of a fixture it does not own and refused on a change that had nothing to do with padding. It carries its own eight-cell row now, and drives the leave-a-nine-cell-row-alone arm as well |
| vcs-paths-two-vocabularies | DONE | user | host/src/tools/vcs.ts;docs/TOOL_CATALOG.md;CHANGELOG.md | 295 | 295 | — | The twelve git tools disagreed with each other about what a file is called, and two of them answered a question nobody asked with a confident empty list | 🟢 **WHAT IT DOES.** These tools let an assistant read and change a Godot project's version history — what changed, who wrote a line, discard an edit. Every one of them takes a file path, and every one of them prints file paths back. They did not agree on how a path is spelled. **WHY IT IS WORTH DOING.** The disagreement is invisible when the game project sits at the top of its repository and appears the moment it sits one folder down, which is a completely ordinary way to lay a project out — it is how this project's own example is arranged. **THE BENEFIT TO THE USER.** Asking for a file's history used to answer `no commits` for a file with years of it, with no error, so an assistant reading that answer would conclude the file was new and act on it. Asking to throw away an edit did throw it away and then reported that nothing had been discarded. Both now tell the truth, and a path that names nothing is refused with the reason instead of answered with an empty list. **THE BENEFIT TO THE PRODUCT.** The twelve tools can now be chained: what one prints, another accepts. Before, the output of one was rejected by the next. 🔴 **FOUND BY THE USER PASS** (288 §3 rank 1, ten sessions untouched), driven against the `vcs_*` family from a container. Root cause: `git status --porcelain=v2` prints CWD-relative paths and `git diff`/`git show` print repo-root-relative ones, and `git` is spawned `-C <projectPath>`, so the two spellings are the same string EXACTLY when the project is the repository root. 🔴 **AND THAT IS THE ONLY LAYOUT `mkrepo()` HAS EVER BUILT** — 500 lines of vcs tests, all green, over a population of one. Fixed with `--relative` on diff/show/restore, an `outside_project` field on `vcs_status` so a `../` path never lands in a list of project paths, and a refusal for a path filter that names nothing in the repository. Seven tests on a new `mkrepo_subdir()` fixture; four of them were driven RED against the old behaviour before the fix went in, and the two that stay green either way are the negative controls — a flat layout reports nothing outside the project, and a real empty answer is still an answer |
| tier-predecessor-skips-a-declared-absence | DONE | internal | scripts/handoff_gate.py | 290 | 290 | — | `tier_problems` requires a block to have inherited from `session - 1` and `BLOCK_ABSENT` says a session can ship nothing, so every session that follows a no-ship session is told its honest inheritance skipped a predecessor and pays a full replay it does not owe | 🔴 **THE SAME ASSUMPTION `POPULATION_CURRENCY` SHED AT 289, LEFT STANDING IN THE READER NEXT TO IT.** 289 §2.5 taught the population reader that a session declared in `BLOCK_ABSENT` owes no status block; the tier reader was never asked, and one paragraph later in the same close it refused `TIER0_PREDECESSOR — block 289 inherited from 287` — which was the truth. 288 shipped nothing, published no block and left no verified tree, so 287 IS 289's predecessor and there is nothing between them for an inheritance to have skipped. 🔴 **THE PRICE IS NOT THE REFUSAL, IT IS WHAT THE REFUSAL BUYS INSTEAD.** 289 could have argued with the gate; it ran the full replay and declared `ritual TIER1`, which was the honest answer available and the expensive one. A reader that cannot express *nothing happened in between* charges that price to every session after a no-ship session, and 288 was the first of those in sixty-one sessions rather than the last 🟢 **CLOSED AT 290, IN THE RESOLVER AND NOT IN THE COMPARISON.** `shipped_predecessor()` walks `session - 1` back over the roster and returns the sessions it stepped over, so the reader that refuses and the note that passes both say which ones and why; the comparison itself is unchanged. 🔴 **THE PAIR IS ON ONE FIXTURE AND IT IS 289 §2.5's OWN ARGUMENT REUSED**: 289's real log line, at 287's real SHA, is clean against a roster declaring 288 and refused by name against an empty one — a resolver that skipped every hole and a resolver that refused every hole each satisfy exactly one of those. 🔴 **AND THE OTHER DIRECTION IS A SEPARATE ARM**: a block inheriting *FROM* a session declared absent is refused, because 288 published no block and a tier whose evidence is a block that does not exist is a counter riding on a tree no instrument ever read. 🔵 The walk is floored at `POPULATION_SHAPE_FROM` — a resolver that can leave the table answers a session number nothing below it can look up |
| instrument-green-residue-is-a-population-nobody-reads | DONE | internal | scripts/instrument_gate.py;scripts/lint_ceiling.py;scripts/registry_bytes.py;scripts/spec_conformance.py;scripts/tree_quiet.py | 290 | 307 | — | Four python gates carry a MEASURED green residue — signatures that can be blinded while the gate stays green — and the measurements live as prose inside twelve values of one exemption roster, so the residue has never been a population anything reads, floors or refuses on | 🔴 **FOUND BY CLOSING `sweep-227-15`, WHICH IS THE ONLY REASON IT IS A ROW AND NOT A SENTENCE.** 227 §8's item 15 asked, of each instrument, whether its green line says *this classifier can classify fixtures* or *the thing it classifies is checked*. The JS half answers the second with `0 undeclared` and no residue; the python half answers `py gates 18/6/12`, and four of those twelve unswept gates are unswept because somebody MEASURED that a blind does not catch them: `spec_conformance.py` 3 of 7 caught, `registry_bytes.py` 4 of 10 with `{SIG:tree_shas}` STILL GREEN under `--selftest`, `lint_ceiling.py` 4 of 8, `tree_quiet.py` measured and harness-shaped. 🔴 **A MEASURED RESIDUE THAT IS ONLY A DICT VALUE IS 246's DEFECT IN THE ROSTER THAT EXISTS TO CATCH IT.** `PY_NOT_SWEPT` floors nothing about these numbers: a gate whose caught-fraction FALLS — a new signature added, an old one stops being reached — changes a sentence in a comment and no line goes red, which is precisely what `BLAST_FLOOR` and `SIG` exist to stop happening to the swept half. 🔵 **THE CHEAP SHAPE IS THE ONE THE FILE ALREADY USES**: turn the four prose measurements into declared pairs with a floor, print the census the way `INSTRUMENT_GATE_PY` prints its three numbers, and let the number fall only on purpose. 🔵 **AND THE EXPENSIVE SHAPE IS THE HONEST ONE**: close the residue itself, starting with `{SIG:tree_shas}`, which is the one entry that names a single still-green signature rather than a fraction 🔴 **RE-MEASURED AT 291 WITH THE INJECTOR, AND THE PROSE IS WRONG IN BOTH DIRECTIONS — WHICH IS THE ROW'S OWN CLAIM ARRIVING AS EVIDENCE.** 291 pointed `instrument_gate.sweep` at all four files from a scratch harness outside the tree and counted: `registry_bytes.py` **8 caught of 15** where the roster says *4 of 10*, `lint_ceiling.py` **5 of 9** where it says *4 of 8*, `spec_conformance.py` **1 caught, 6 still green of 8** where it says *3 of 7*, and `tree_quiet.py` **2 of 4 with `hook_state` still green** where it says both members are caught on the global axis. `{SIG:tree_shas}` is still green, as written. **A measured residue that is only a dict value stopped following its population, which is exactly what the row said would happen and had never been demonstrated.** 🔵 AND 291 PRICED THE FIX AND DID NOT TAKE IT: the honest shape is promoting the four files out of `PY_NOT_SWEPT` into `INSTRUMENTS` with a DECLARED `residue` that `sweep()` refuses in BOTH directions — undeclared-still-green as today, and declared-residue-now-CAUGHT, so the number falls with its population instead of being re-typed. That needs a `VERDICT_MARKER` for each file first, and all four print their banner and their tally with the same prefix, so each needs a `<NAME>_SELFTEST_DONE` line printed once, last, on the red path and the green one alike (245 §1 / 247 §2). Measured as four small edits plus four roster entries plus a two-armed reader; not folded into 291's PR because that PR is the landscape roster's population and the two share no file. 🟢 **CLOSED AT 307 AT ZERO, NOT CEILINGED — AND THE CAUSE WAS NEITHER ARM THE ROW OFFERED.** The row asked for an owner or a written ceiling. 304 narrowed it to one line — `tautology_gate.selftest.mjs:385`, *no enclosing test() and no owner* — and named `enclosingTest` as the place to look. 🔴 **`enclosingTest` was never the problem: it was never REACHED.** `analyze` gates the banner fallback on a raw-text regex over the whole source, and that file — the analyzer's own self-test — quotes `test(` inside its fixture strings, so it is judged to have test blocks it does not have and all 175 of its claims lose the fallback at once. 🔴 **AND THE SECTION READER HAD THE SAME DISEASE WITH THE OPPOSITE SIGN.** `populationSections` found its markers by a regex over raw lines: all 33 markers it returned for `seal_order_gate.selftest.mjs` sit inside a template literal, and all 132 of that file's section-owned claims were attributed to text the file merely quotes — counted by `TAUT_SECTION_ATTRIBUTED`, a FLOOR. A gate fooled this way DELETES an attribution on one path and INVENTS one on the other, and the invented half reports as coverage. 🟢 **Both readers now ask the AST.** Measured over all 136 swept files before a line was written: six banner disagreements, three section ones, zero in the direction the code's own comment feared. residue 1 → 0, orphan 44 → 26, banner attributed 98 → 132, section 483 → 467, defect unchanged at 15. 🔵 The ceiling that should follow the reading down is `orphan-ceiling-outruns-its-reading`, priced at eleven pins and deliberately left to its own commit |
| block-sha-that-is-all-digits-reads-as-a-counter | DONE | internal | scripts/handoff_gate.py | 292 | 292 | — | The end-of-session report card prints the commit it was measured at, and the checker that verifies the report card was reading that commit's short identifier as if it were one of the numbers being checked — so roughly one session in twenty-seven cannot file its report at all, and only finds out at the very end when fixing it means shipping again | 🔴 **FOUND BY BEING THE UNLUCKY SESSION.** Every block since 227 prints two or three abbreviated SHAs above `VERIFIED` — the `main` row's, the pre-squash tip on the branch row's continuation, and the one in `CI GREEN … at <sha>` — and for sixty-five blocks running every one of them contained a letter, so `COUNTER_RE` never saw them and no `HEADER_EXEMPT` row was ever needed. 292 merged at **`5155079`**: a real SHA that git resolves, and also seven decimal digits. The header half read it as a counter in two atoms at once — `UNREADABLE HEADER CLAIM` on `squashed to 5155079`, and `git.moved — the block says [5155079, 1], the tree says [1]` on the `main` row, where an atom carrying ONE claim became two. 🔴 **AND IT LANDS AT THE WORST POSSIBLE MOMENT.** A uniformly distributed hex prefix is all digits with probability (10/16)^7 ≈ **3.7%**, about once every twenty-seven sessions — and it strikes at the CLOSE, on the one document a session cannot re-measure without moving the very tree its block describes. 292 paid a second PR for it, which is the cheapest anybody will ever pay. 🟢 **CLOSED BY ONE `HEADER_EXEMPT` ROW, ON A WIDTH TEST WITH NOTHING IN BETWEEN TO ARBITRATE.** Git abbreviates to seven characters or more; the largest counter any block has ever carried is four digits (`taut 4963`); so `\b[0-9a-f]{7,40}\b` separates them cleanly, and it deliberately covers hex-with-letters too — those were never counters and are now exempt for a stated reason rather than by accident of spelling. 🔴 **THE NEGATIVE CONTROL IS THE HALF THAT COULD HAVE BEEN WRONG AND IT IS DRIVEN**: an exemption wide enough to eat a SHA must not eat the counter beside it, so `--selftest` asserts that `26 of 26 required checks at 467fcb4` keeps both 26s and that `taut 4963 claim sites at 5155079` keeps 4963. A row that removed the claim it was written to protect would be worse than the refusal it replaces |
| negative-capability-claims-were-never-sourced | DONE | internal | docs/alternative_mcp_roster.json;docs/LANDSCAPE_TRACKING_POLICY.md | 292 | 293 | — | When we record that a competing product does NOT do something — no C# support, no debugger, no language-server client — nobody has been required to go and check. Three products were re-read at source this session and all three of those "no" answers were wrong, so the comparison a reader draws from this table has been overstating our lead in a way nothing could catch | 🔴 **THREE OF THREE, IN ONE AFTERNOON, ALL IN THE SAME DIRECTION.** 292 re-read `godot-mcp-enhanced`, `fennara-godot-mcp` and `godot-mcp-go` at source because their heads had moved, and every one of them carried `csharp: false` while shipping a real `dotnet build` invocation — `src/tools/script.ts:158 csharpValidateAndRevert()`, `fennara-cpp/src/csharp/build.cpp:run_dotnet_build_if_needed`, and `commands/csharp_commands.gd:run_build()` respectively. None of the three was a NEW capability: they landed 2026-08-06, before 6fb56ed, and at 0.4.0 — every one of them before the pass that recorded `false`. 🔴 **THE RULE EXISTED AND WAS BEING APPLIED TO ONE SIDE ONLY.** 288 §7.4 says *a capability claim is unsourced until somebody reads its source*, and every `true` on this roster carries file-and-symbol evidence in its note. The `false`s carry none, because nobody ever thought of a negative as a claim — and a `false` is the claim *we looked and it is not there*, which is strictly harder to earn than a `true`. 🔵 **AND IT BIASES IN THE ONE DIRECTION THAT MATTERS.** An unsourced `false` on a competitor reads as a gap we have and they do not; forty-eight entries still carry at least one, and `fennara-godot-mcp` additionally had no `debugger` field at all until this session, which is the same hole spelled as an absence. 🟢 **THE SHAPE OF THE FIX, AND IT IS NOT FIFTY SOURCE PASSES.** A negative needs either evidence or an honest `null`: give the four capability fields a third state meaning *nobody has read this*, default every entry that has no evidence line to it, and let the roster's own reader count how many unsourced negatives remain rather than presenting them as measurements. That is one commit and a falling number, against fifty passes that would never be paid. 🔵 Rule 5's own argument, one column over: the cost of a row and the cost of a ruling are different costs, and a blank is not a ruling 🟢 **CLOSED AT 293 — AND THE RULE SHIPPED SYMMETRIC, WHICH IS WIDER THAN THIS ROW ASKED FOR.** The four capability fields (`real_dap_client`, `real_lsp_client`, `csharp`, `debugger`) now take a third state, `"unread"`, meaning *a value was asserted here and nobody can show the reading behind it, so the assertion is withdrawn* — which is a different sentence from an ABSENT field (the roster never claimed anything) and from `null` (no reading is possible, the two closed-source rows). Every definite value owes a `capability_evidence` entry naming what was read, and `--census` refuses a claim with no evidence, evidence with no claim, and a value outside the field's declared vocabulary — merge-blocking, because an unsourced claim is a defect in the TREE and not a fact about a third-party host. 🔴 **THE MIGRATION IS THE FINDING.** Seventy-eight claims were audited against their own notes: 43 could show a reading and 35 could not, and the 35 were withdrawn. It was applied to `true` and `false` alike — a control that only doubted negatives would be the same one-sided reading with the sign flipped — so unevidenced `true`s went too, `Doyunha-Gopeak`'s DAP and LSP pair among them. 🔵 **AND THE CENSUS PRINTS THREE NUMBERS**: `43 claimed / 35 unread / 10 uncited`, where `unread` is the debt that falls one source pass at a time and `uncited` counts claims whose evidence names no file, symbol, port or stated search — a weaker reading and a real one, so a number rather than a refusal. 🔴 **ONE VALUE CONTRADICTED ITS OWN NOTE AND NOBODY HAD READ THEM TOGETHER**: `fennara-godot-mcp` carried `debugger: godot-internal-editor-debugger` while its note said in terms that it is NOT the `EditorDebuggerPlugin` API but a UI scrape of the Debugger dock — corrected to `screen-scrapes-stack-trace-panel`, which also takes one entry back out of the population 292 §2.4's depth claim is measured against. Successor row: `capability-claims-withdrawn-await-a-reading` |
| landscape-entries-unclassified-by-channel | DONE | internal | docs/alternative_mcp_roster.json;scripts/assetlib_sweep.py | 291 | 293 | — | The roster now records which shop each tracked product sells itself through, but forty-six of the fifty-one products recorded before this session have that field blank, so nobody can yet answer "where do the projects we watch actually ship?" — and the answer decides which of the four discovery legs would notice if one of them changed. Filling it in is cheap per row and turns a printed number that currently only goes up into one that goes down; the benefit to the product is that a leg going quiet becomes visible, because a channel with no rows behind it is a leg nobody would miss. No user sees any of it | 🔵 **OPENED AT 291, BY THE COMMIT THAT MADE THE FIELD EXIST.** `channel` records where a product SHIPS and it is deliberately OPTIONAL: forty-six entries predate the channel legs and nobody has recorded where any of them ships. 🔴 **THE ALTERNATIVE WAS THE DEFECT.** Defaulting the blanks to `assetlib` because that is the leg that found most of them would write this file's own finding — a population assumed rather than read — into the census that exists to report it. So `--census` prints `unclassified 46` and the number is a visible intake. 🔵 **THE WORK IS NOT UNIFORM AND THAT IS WHY IT IS A ROW RATHER THAN A CHORE**: an entry with an `asset_id` is `assetlib` by construction and can be filled mechanically; one without needs somebody to look at how the project ships, and `bradypp/godot-mcp` is already recorded as the answer `null` — git clone only, no enumerable channel, and no leg will ever re-read it 🟢 **CLOSED AT 293, AND THE NUMBER IS ZERO RATHER THAN SMALLER.** All forty-six were filled: forty-one `assetlib` by construction from an `asset_id`, exactly as this row predicted; one measured onto `npm` (`godot-mcp (tugcantopaloglu)` — `@tugcantopaloglu/godot-mcp`, mirrored by `@iflow-mcp/…`); and three measured onto NO enumerable channel (`godot-ai`, `GameDev-MCP-Server`, `Doyunha-Gopeak` — queried against npm and the MCP Registry on 2026-09-01, returned by neither, and holding no Asset Library entry). 🔴 **AND THE CENSUS WAS COUNTING AN ANSWER AS A GAP.** `channel: null` is a MEASUREMENT — nothing enumerable surfaces this project, so no leg will ever re-read it — and it was being bucketed with the blanks under `unclassified`. It has its own bucket now: `assetlib 42 · commercial 2 · no-channel 4 · npm 4`. 🟢 **THE NUMBER CANNOT CLIMB BACK BY OMISSION**: `ROSTER_CHANNEL_UNDECLARED` refuses an entry whose `channel` key is ABSENT, while `null` stays legal — 291 shipped the field optional because forty-six rows predated it, and that reason is spent |
| surfaced-population-unread-at-source | OPEN | internal | docs/alternative_mcp_roster.json | 291 | — | — | Forty-seven Godot MCP projects are now on the roster because a search found them, and not one of them has been read. Each row honestly says only what the search knew — name, package, version, date — so nothing claims more than it measured, but the list has no order, and the next person who wants to know "which of these actually matters?" has to re-derive that from scratch. Ranking them once, and reading the top few properly, is what turns a list into intelligence; the benefit to the product is that positioning claims start being made against a field somebody has actually looked at. No user sees it directly | 🔵 **OPENED AT 291 AS THE SUCCESSOR TO ITS OWN FIX, AND IT IS DELIBERATELY NOT A DEMAND FOR FORTY-SEVEN ANALYSES.** Rule 5's split is that a `surfaced` row costs a row and an `entries` row costs a source-level pass, so promotion is ELECTED. 🔴 **WHAT IS MISSING IS THE ORDER, NOT THE WORK.** The rows carry no stars, no download counts and no tool counts, because the two search endpoints serve none of those — so today the only way to rank them is to read all forty-seven. One authenticated `api.github.com` call per slug supplies stars for the thirty-nine that declare a repository, and `registry.npmjs.org/<pkg>` supplies download counts; both are the same shape as the emitter this session shipped and both belong in it rather than in a session's scratch buffer. 🔵 **AND THE PRIZE IS ALREADY VISIBLE**: `Coding-Solo/godot-mcp` was promoted at 291 with 5,399 stars and the roster had never held it, so the ranking is what stops the next one waiting for somebody to chase a name out of a chat transcript |
| capability-claims-withdrawn-await-a-reading | OPEN | internal | docs/alternative_mcp_roster.json | 293 | — | — | The roster used to state, for fifty-two competing products, whether each one supports C#, whether it can drive a real debugger, and whether it talks to a language server. Thirty-five of those statements turned out to have nothing behind them — nobody had ever looked — so 293 withdrew them rather than keep presenting them as findings. Reading them back in, a project at a time, is what turns the table from a list of beliefs into a list of measurements. The benefit to us is that any claim we make about where we lead is made against a field somebody actually checked; the benefit to the product is that the number is now printed on every sweep, so the debt is visible and shrinking instead of invisible and wrong. No user sees it directly | 🔵 **OPENED AT 293 BY THE COMMIT THAT MADE THE DEBT COUNTABLE**, and it is deliberately NOT a demand for thirty-five source passes. `--census` prints `43 claimed / 35 unread / 10 uncited`; the middle number is this row and it falls whenever a session elects a pass. 🔴 **THE POPULATION IS NOT UNIFORM AND THAT IS WHY IT IS A ROW RATHER THAN A CHORE.** Four entries carry no note at all (`godot-mcp-pro`, `Open-Godot-MCP`, `godot-mcp-toolkit` and `Godot-MCP-Native`) and owe a first pass; `coding-solo-godot-mcp` and `satelliteoflove-godot-mcp` have long notes about stars, CI and tool counts and say nothing about any of the four capabilities, which is the most-starred project in the category carrying four withdrawn claims; and single fields on `godot-mcp-enhanced` (LSP), `fennara-godot-mcp` (DAP) and `Doyunha-Gopeak` (C#) are one grep each in a tree somebody has already cloned. 🔵 **AND `uncited` IS A SECOND, CHEAPER LANE**: ten claims rest on evidence naming nothing checkable — nine of them say so in their own text — and upgrading one is re-reading a file rather than re-reading a project |
| sweep-exit-collapses-tree-defects-and-world-movement | DONE | internal | scripts/assetlib_sweep.py;.github/workflows/sdk-drift.yml | 294 | 308 | — | The weekly job that watches fifty competing products reports two completely different kinds of news through one traffic light. One kind is "our own checking code is broken or couldn't reach a registry" — that is our bug and should stop a merge. The other is "somebody else shipped a new version" — that is a reading, not a fault, and it will happen again next week whatever we do. Because both come out as the same red, the light has been red on nearly every run since mid-August and nobody can tell the two apart without opening the log. Splitting them so the job is green when our own machinery is healthy, and prints the news separately, is what makes the colour mean something again. The benefit to us is that a genuinely broken sweep would stand out instead of hiding in a red that is always on; the benefit to the product is that the competitive roster stays trustworthy because its alarm is worth answering. No user sees it directly | 🔵 **294 §2.1 CLASSIFIED THE WORKFLOW AND THIS ROW IS THE LAYER UNDERNEATH IT.** 294 taught the pickup ritual that `sdk-drift`'s subject is the world, which stops the job costing every session a full replay — but it is a judgement about the whole workflow, made because the workflow has only one exit code to judge. 🔴 **THE ROW'S OWN CITATION WAS WRONG IN TWO CHECKABLE PLACES AND FOUR SESSIONS READ IT** (307 §3.2, corrected here at 308): the sentence *an unsourced claim is a defect in the TREE and not a fact about a third-party host* is `census()`'s, not `severity_problems`' — `severity_problems` is about whether a `CHANNELS` row declares what a never-tracked project COSTS — and `capability_problems` is never called by `--check` at all, so the `CAPABILITY_*` family was already quarantined with nothing to split. 🔴 **AND THE TREE-VERSUS-WORLD LINE DOES NOT CUT THE PROBLEM SET**: of the eight conditions feeding `--check`, only two are purely about our own apparatus, because `owes()` makes every world movement owe an action. 🟢 **308 DREW IT SOMEWHERE ELSE AND MEASURED FIRST**: every `schedule:` run in this workflow's history is red and not one was this apparatus failing, so the line is *is there something a person must do that will not undo itself next week*. A project the roster already names having moved is `0` and printed as news; a project with no roster row is `1`; a leg that did not finish is `2`; and a condition nobody classified is `2` rather than a silent green, read off the live call's AST so a ninth one cannot default into the quiet bucket. 🔵 **DO THIS AND 294 §2.1's TABLE GETS SMALLER**, because a job that is green on a healthy tree does not need exempting from the verdict — which is the honest test of whether this row is worth paying |
| capability-paths-declared-for-one-entry | OPEN | internal | docs/alternative_mcp_roster.json;scripts/assetlib_sweep.py | 293 | — | — | The weekly sweep refuses whenever a tracked project's code has moved since we last read it, which is right for a project that ships monthly and useless for one that ships several times a day — the complaint expires before anyone can answer it. 293 gave each entry the option to record which files its ruling actually depends on, so a change that touches none of them is recorded and dismissed instead of demanding a re-read. Exactly one of fifty-two entries records them so far. Filling in more is what stops the sweep crying wolf, and the benefit to the product is that when it does complain, the complaint is worth acting on. No user sees it directly | 🔵 **OPENED AT 293 BY THE COMMIT THAT MADE THE FIELD EXIST**, and the default is deliberately strict: an entry declaring no `capability_paths` is priced exactly as it was before the reader existed, so immateriality is a claim a session makes and never one silence inherits (292 §2.1's argument, one file over). 🔴 **THE FIRST JUDGEMENT IT MADE CAME BACK MATERIAL**, on the entry it was written for: `hybridindie/godot-mcp` moved `037b00f -> 2400578` over twelve files, one of them `mcp_server/tools/undo.py`, which is under the declared paths because the row's tool count rests on that directory. Reading that one file showed the surface unchanged at 180 — so the reader did not retire the refusal, it made it cheap, which is the honest half of 292 §3.2's promise. 🔵 **THE RESIDUAL IS NAMED**: a project committing daily under its tool modules will re-flag every week, and the cheap escape is a DERIVABLE tool count (`^\s*@mcp.tool(` is 180 at both commits) rather than a wider path list — per-project derivations are a bigger idea than this row and should be priced before they are built |
| authoring-plane-default-surface-unswept | OPEN | internal | host/test-integration/authoring-plane.integration.mjs | 308 | — | — | The plugin's editor tools are exercised by an automated suite before every merge, and that suite has always run with every permission switch turned on. About 130 tools have therefore never been tried the way an ordinary person's install would try them, with the default settings, so nobody knows which of them quietly refuse or misbehave for a normal user. This is worth doing because the last time the same question was asked of a much smaller group of tools it found real breakage that had already shipped and that nobody had reported. The benefit to the user is tools that work in the configuration they actually have rather than only in a privileged test harness. The benefit to the product is that the last part of the editor surface nobody has checked stops being a place defects can hide. It needs to be split in two: a pass over the many tools that should work with nothing switched on, and a separate pass over the handful that genuinely require a switch, so that a tool refusing for a good reason is not counted as a defect | 🔴 **CARRIED IN HANDOFF PROSE SINCE 299 AND A TRACKED ROW ONLY AT 308**, which is the decay 307 §5.4 found from the other end: a queue row rots because every session that ranks it re-reads it without re-measuring, and a lead that is not a row at all has the same problem with no pickup ritual to slow it down. Nine handoffs named it and none opened it. 🔵 **REACH IS `internal` BY THE ROSTER, NOT BY JUDGEMENT** — the file this row moves is `host/test-integration/`, which `REACH_PATHS` maps to `host/test` → internal, and 308 typed `user` first on the strength of what the sweep is ABOUT before the reach-versus-evidence join corrected it. 🔴 **ANY DEFECT IT FINDS IS ITS OWN ROW AND THOSE WILL BE `user`**, because the fix would land in `addons/`. 🟢 298 is the precedent and the argument: the same question asked of a smaller family found silent, user-visible breakage. 🔵 It is free on the Godot 4.7 installed on his Mac — 299 §6 has the probe recipe, and a live probe needs the engine and the built `dist` on the same machine |
| capability-boundary-strands-its-consumers | DONE | user | host/src/capabilities.ts;host/src/peers.ts;host/src/tools/processes.ts;host/src/tools/runtime.ts;docs/USER_GUIDE.md | 296 | 296 | — | Four tools a default install keeps could only ever refuse, because the one tool that produces what each of them consumes is withheld by the secure default — and the refusal read like the caller's mistake | 🟢 **WHAT IT DOES.** The server ships with a small group of higher-trust tools switched off — the ones that run arbitrary game code. That is a good default and it is well explained. What nobody had checked is what happens to the tools that STAY. **WHY IT IS WORTH DOING.** Four of them take a ticket that only a switched-off tool can issue: two want a process id that only the managed launcher hands out, two want a peer that only the peer-spawner can start. So on a fresh install those four are listed as available, accept a call, and then say *no process with that id* — for every value anyone could possibly pass. **THE BENEFIT TO THE USER.** The message now says which switch is off, what to set to turn it on, and where to read the full list, appended after the original sentence so a genuinely stale ticket still reads as one. Twenty-five more tools take an optional peer id and used to answer *spawn peers with runtime_spawn_peers* — naming a tool absent from that caller's own list — which is fixed in the one place the whole family passes through. **THE BENEFIT TO THE PRODUCT.** The always-on capabilities resource now publishes the stranded set, so an assistant can plan around it instead of discovering it by failing. 🔴 **FOUND BY THE USER PASS** (288 §3 rank 1's remaining cheap half), driving the runtime and DAP planes over stdio against a live Godot 4.7 game — on the DEFAULT surface, which is the one no test had ever driven end to end. 🔴 **AND THE POPULATION OF ONE IS ONE FILE OVER FROM 295's.** Every existing reader of this boundary asks *is the right tool dropped?* — the tagging is total, the drop is clean, the policy refusal is correct, and all of that is tested on both surfaces. None of them asks whether what is LEFT can still be used, because that is a question about the relation between the two surfaces rather than about either one. 🟢 **`HANDLE_PRODUCERS` is the declared population**: ten consumer/producer pairs, six of which span no privilege boundary and are declared anyway so the table reads as total rather than as a list of today's complaints — tag `dbg_scopes` tomorrow and `orphanedConsumers` says so that day. The suite derives the owed rows from the REGISTERED surface, so a tool cannot join with a required handle field and no row. Sixteen claims, six of them driven RED against the old behaviour first; the arms that must stay silent — the group enabled, a producer that is unprivileged, and every success path — are the negative controls |
| run-project-claims-a-handle-it-does-not-mint | DONE | user | host/src/tools/cli.ts;docs/TOOL_CATALOG.md;docs/RUNBOOK.md | 296 | 296 | — | `godot_run_project` said *Returns the process id* and returned no id, while the same tool's own port-conflict refusal said a detached game is not stoppable by any tool | 🟢 **WHAT IT DOES.** This is the tool that starts the game. **WHY IT IS WORTH DOING.** Its description promised a handle it does not produce: what comes back is an operating-system process number, and the stop and read-output tools accept only the internal ticket issued by the *other* launcher. So the first sentence a caller reads set them up to fail, and the tool's own refusal further down said the opposite — one tool, two shipped sentences, contradicting each other. **THE BENEFIT TO THE USER.** Nobody is now told they can stop a game they cannot stop; where the limit exists it is stated once, in the place they hit it. **THE BENEFIT TO THE PRODUCT.** The catalog's output block for this tool was right all along and the description disagreed with it, so this also closes a disagreement between two shipped documents. 🟢 **THE FALSE SENTENCE WAS DELETED RATHER THAN REPLACED**, which pays the tool surface DOWN by twenty-four bytes on a budget that had ten bytes of room — see `token-surface-ceiling-has-no-room-for-a-correction`. The runbook's Plane C and DAP checklists, which drove the withheld launcher unmarked, now mark every higher-trust row and name the default-surface substitute where one exists |
| token-surface-ceiling-has-no-room-for-a-correction | DONE | user | host/scripts/token-cost.mjs;host/src/tools/dap.ts;host/src/tools/lsp.ts;host/src/tools/runtime.ts;host/src/tools/tabletop.ts;scripts/floor_pin_gate.py | 295 | 297 | — | The tool surface arrived at 295 with 116 bytes under its ceiling, so a correctness fix needing one output field and three descriptions did not fit, and the paydown available inside those three descriptions has been spent | 🟢 **WHAT IT DOES.** Every client that connects downloads a catalogue of all the tools and pays for it in tokens, once per session. A gate caps how big that catalogue may get. **WHY IT IS WORTH DOING.** The cap is now within a few dozen bytes of the real size, so the next session that needs to add an output field or correct a description cannot, without first deciding what to cut. **THE BENEFIT TO THE USER.** Whatever is decided, it is decided deliberately rather than by whoever hits the wall on a Friday: either the catalogue genuinely gets smaller, or the cap moves with a written reason. **THE BENEFIT TO THE PRODUCT.** The number is the one figure a comparison against another server can honestly quote, so it should not drift by accident in either direction. 🔴 **FILED AT 296 AND OPENED AT 295** — 295 §3.1 measured this and its §6 lists it among the open rows, but the row was never written into this table, so `queue_gate.py --render` showed ten live rows where the handoff's own prose said eleven. That is the exact failure mode this file's header describes, arriving in the document the file was built to replace. 🔵 **296 BOUGHT BACK 24 BYTES** by deleting one false sentence (`run-project-claims-a-handle-it-does-not-mint`), taking the headroom from 10 to 34. That is not the decision; it is a reprieve. 🔵 **THE TWO HONEST MOVES ARE STILL 295's**: pay the surface down where the prose genuinely duplicates the catalog, or move `BYTES_CEILING` with a reason in the ledger. 🔴 **AND ONE CANDIDATE 295 NAMED IS NOT AVAILABLE**: `runtime_peers_digest` is the fattest description on the surface at 1,674 B, and the catalog states in terms that its four preconditions are carried in the tool's own description *rather than only here* — cutting it would be reversing a deliberate decision, not reclaiming redundancy. 🟢 **CLOSED AT 297, AND THE MEASUREMENT MOVED THE QUESTION.** The row offered two ways out and the second one did not exist: there was nowhere in this repository a reason for moving `BYTES_CEILING` was required to go. `SIZE_LEDGER` rows are `(value, reason)` and refuse in three directions when a value moves without its sentence; `TARGETS` rows — which is where every javascript constant lives, this budget included — are `(label, file, regex, runner)` and carry no reason at all. 209 §2 wrote the split down as a division by language and did not notice it was also a division by standard. 🔴 **SO THE GAP WAS FIXED FIRST**: `TARGET_REASONS` joins a sentence to a pinned constant by the row's own primary key, `target_reason_problems()` refuses a stale reason, an unreasoned row over the ceiling, a join that binds nothing, and a bare digit — the last by reusing `reason_problems()` rather than copying it, so the two tables 209 split are now held to one standard. Four directions driven RED against the shipped tree first, two negative controls that must stay green. 🟢 **THEN THE CUT**: six sentences deleted that restated what the same tool's own field descriptions already tell the client, verified one at a time against the schema that carries the fact — 541 bytes, taking the headroom from thirty-four to five hundred and seventy-five. The protected text was not touched |
| pinned-constants-await-their-reasons | DONE | internal | scripts/floor_pin_gate.py | 297 | 299 | — | Most of the constants this repository pins can still move without any sentence saying why, and the ceiling that counts them is the debt rather than the fix 🟢 **CLOSED AT 299 — PAID IN FULL RATHER THAN RATCHETED.** All one hundred and six unreasoned pins now carry a sentence saying what the number governs, which silent failure it catches, and when it may legitimately move; `TARGET_UNREASONED_CEILING` went one hundred and six to zero and `TARGET_REASON_FLOOR` seven to one hundred and ten | 🔵 **WHAT IT DOES.** Every size limit in this project — how big the tool catalogue may get, how far behind the published release may fall, how many checks must run — is a number somebody chose. Roughly a third of them are required to carry a written reason, and a gate refuses the change if the number moves and the sentence does not. The rest, until 297, carried nothing. **WHY IT IS WORTH DOING.** A limit nobody has to explain is one that drifts: the easiest way past a failing check is to raise the number it failed against, and without a reason attached there is no way to tell that from a deliberate decision six months later. **THE BENEFIT TO THE USER.** Indirect but real — these numbers are what keep the shipped package from quietly getting bigger, slower or staler, and a limit that can be waved through protects nothing. **THE BENEFIT TO THE PRODUCT.** The reasons are the institutional memory: they say what each limit is defending and what would justify moving it, which is what lets a later session act instead of guess. 🔴 **297 BUILT THE MECHANISM AND PAID THE FIRST SEVEN**, all of them on the two readers the closing row was about — the tool-surface budget and its collapse guard, the schema mean, the result axis, the self-test's claim floor, and the two registry staleness ceilings. `TARGET_UNREASONED_CEILING` is the rest, and it is a CEILING: it comes down as sessions write reasons, it may never go up, and every session that lowers it owes the `SIZE_LEDGER` row saying what it bought. 🔵 **THE WORK IS TRANSCRIPTION MORE THAN INVENTION** — most of these constants already have prose above them explaining themselves; what they lack is a governed slot for it, and that slot now exists. A cheap tier can do the lifting a few rows at a time 🟢 **CLOSED AT 299.** The row expected a ratchet — lower the ceiling by whatever a session could afford — and the whole debt turned out to be affordable in one, because the prose explaining almost every constant was already written above it and the work was transcription rather than invention. Written per file by subagents reading one file each, then assembled and judged here: the gate refuses a reason that quotes an ungoverned number, one that asks for a `{FLOOR}` it cannot resolve, and one whose key binds no live row, so a wrong answer fails on the run that writes it. 🔴 **The two constants moved in the SAME commit and that is not tidiness.** At a ceiling of zero the ceiling stops being the instrument: it is satisfied by a join that binds nothing at all, which is 201's own collapse shape, so `TARGET_REASON_FLOOR` had to become the number that would notice — set a little under the full table so a pin legitimately retired reddens nothing while a join that quietly stopped binding still does. 🔵 **What is left is not this row.** Every pin can now say why it is what it is; whether each sentence is RIGHT is a reading nobody has done end to end, and a wrong reason is worse than none because it is believed. That is a different row if anyone wants it |
| handoff-section-six-is-prose-nothing-reads | DONE | internal | scripts/handoff_gate.py;QUEUE.md | 296 | 306 | — | A handoff's §6 lists the open queue rows by name and no gate compares that list to the queue, so 295's handoff named a row that does not exist and closed green | 🟢 **WHAT IT DOES.** Each session ends by writing a handoff document, and one section of it lists the work still outstanding by name. **WHY IT IS WORTH DOING.** That list is typed by hand, while the real list lives in a table a gate reads. Session 295 measured a genuine problem, wrote it up, listed it among the eleven open items — and never added it to the table, which had ten. Nothing noticed, because nothing reads that part of the handoff. **THE BENEFIT TO THE USER.** None directly. **THE BENEFIT TO THE PRODUCT.** The next session inherits a to-do list that is either true or refused, instead of one that can quietly disagree with the tracked table it is summarising. 🔵 **THE CHEAP SHAPE**: at the close, `handoff_gate.py` already holds the handoff and the tree, so it can parse the *Oldest first* names and refuse a name the queue does not carry — and refuse a live row the handoff omits, which is the same defect in the other direction. 🔴 **PRICE IT FIRST**: adding a reader to `handoff_gate.py` costs a `COUNTER_READERS` row, a `{SIG:}` entry and a `scope_gate` ledger line (251's rule, one file over), so this is a small build rather than an edit. 🔵 **AND THE HEADER OF THIS FILE IS THE ARGUMENT FOR IT** — the queue exists because the section of a handoff that says what happens next was prose in a gitignored file that no gate ever opened; §6 is the last piece of that prose still unread 🟢 **CLOSED AT 306, AND THE MEASUREMENT TAKEN FIRST IS WHY IT SHIPPED THE SHAPE IT DID.** Run against today's queue over the last six handoffs: 300, 301, 302, 303 and 304 each name 10 of 10 OPEN rows in their §6 — and 305 names **one**, three in the whole document. 🔴 **305 IS THE SESSION THE HAND-MAINTAINED LIST BROKE**, it rewrote §6 as prose, seven tracked rows stopped appearing anywhere in the document, and nothing said a word — while its own §6 warned *do not trust this section over that command*. 🔵 **AND 304's HAND-COMPARISON ONE SESSION EARLIER IS THE ARGUMENT FOR A READER RATHER THAN AGAINST ONE**: *10 OPEN rows, 10 named, no disagreement either way* was true when it was taken and does not run again. 🔴 **THE COMPARISON IS ONLY SOUND AGAINST THE LIVE PAIR**, which is what settled where it runs. The queue carries TODAY's states, so judging a shipped handoff by it refuses honest work — 300's §6 named `release-names-ritual-axis` as owed while it was OPEN and it closed at 304. So `section_six_problems` runs on the CLOSE path, against the document being written and the queue as it stands, and `--selftest` proves the predicate on four fixtures because `HANDOFF*.md` is gitignored and that is the only half CI can see. 🟢 **BOTH DIRECTIONS, AND THEY ARE NOT THE SAME CLAIM**: `SECTION_SIX_OMITS` reads the WHOLE document, because §4's ranked list is a legitimate place to name a row; `SECTION_SIX_STALE` reads §6's OWED subsection only, because a closed row is cited all over an honest handoff as history. `SECTION_SIX_ABSENT` refuses a document with no §6 at all — an omission claim that reads an empty string finds nothing missing. 🔵 **PRICED AT ONE READER AND IT COST ONE**: the row predicted a `COUNTER_READERS` row, a `{SIG:}` entry and a `scope_gate` ledger line; the counter row was not needed because this is a refusal rather than a number, and `coverage_problems` named the `{SIG:}` entry in two seconds instead of CI naming it in twenty-five minutes |
| host-row-moved-claim-is-unread | DONE | internal | scripts/handoff_gate.py | 301 | 303 | — | A status block's `host / addon` row binds a reader only when it says UNMOVED, so a block that says MOVED asserts something nothing compares to the tree — and 300's said it falsely for a session before anybody could tell | 🟢 **WHAT IT DOES.** Every session's handoff opens with a one-line status block, and one line of it reports whether the shipped package's version number moved since the previous session. **WHY IT IS WORTH DOING.** The gate can check that line only when it says the version did NOT move. When it says the version DID move, nothing compares the sentence to the tree. Session 300's block said the version had moved *against the newest block* — and that was wrong, because the block it was measuring against had never been registered. The numeral beside it on the `main` line was caught by name the moment 301 registered the two missing blocks (`MOVED_POPULATION`: *block says (2,), tree says 1*); the sentence about the version was not, and stands exactly as published. **THE BENEFIT TO THE USER.** None directly. **THE BENEFIT TO THE PRODUCT.** The version line is what a release cycle is steered by, and half of what it can say is currently unreadable — so the fact that decides whether a cut is owed is trusted rather than measured. 🔵 **THE READER IS HALF-BUILT**: the alias anchors on the word *unmoved* (`r"^host\s*/\s*addon\b.*\bunmoved\b"`), and `version_interval`, the function that would answer the other half, already exists and is already called. 🔴 **AND 243 §3 IS THIS ROW ONE LINE UP**: `git.unmoved` had exactly this shape — *`MOVED +1` carries a numeral, so `header_atoms` returned it and the tree could contradict it; `UNMOVED` carries none and nothing here would have had an opinion* — and it was closed by making the WORD a claim. This is that same fix owed to the neighbouring row, in the opposite direction. 🔵 **THE EVIDENCE IS ON DISK AND STAYS THERE**: block 300 in `BLOCK_POPULATION` carries `🔴 HOST MOVED against the newest BLOCK`, false against the population as it now stands, left as published on purpose — a block edited to make a gate green is the one repair `population_block_shape` says this table must never accept 🆕 **302 PRICED IT, AND IT IS A BUILD RATHER THAN THE BIND THIS ROW SUPPOSED — four measurements, none of which this row had.** 🔴 **ONE — THE MOVED ROW IS NOT AN UNREAD ATOM, IT IS NOT AN ATOM.** `header_atoms` returns a row only if a numeral survives `HEADER_EXEMPT` — the two version strings do not — or if a zero-arity alias matches the label-prefixed row, which `unmoved` does and `MOVED` does not. Measured on 299 and 300: their `host / addon` rows produce NO atom at all. So the reader cannot be bound by adding a roster row; the atom has to be made to exist first, one function earlier than this row supposed. 🔴 **TWO — AND THE MOMENT IT EXISTS IT COLLIDES WITH `git.moved`.** Measured on both blocks: the host row's cleaned context matches `git.moved`'s alias, a bare word-boundaried `MOVED` with NO row anchor, so `HEADER_AMBIGUOUS` refuses it. 243 anchored `git.unmoved` to the main-or-branch row for precisely this reason and left its numeral-carrying sibling unanchored — safe only because the host row has never been an atom. **The two aliases have always been one atom apart.** 🔴 **THREE — AND `git.moved` CANNOT SIMPLY BE ANCHORED THE SAME WAY.** The word path builds its cleaned text as label-plus-atom; the numeral path does not, so a row-anchored alias matches nothing on the main row today. Prefixing the label on the numeral path is the symmetric fix and its cost is measured: the `branch 301` label CARRIES A NUMERAL, which would enter the claimed tuple of every branch-row atom unless exempted. Discriminating on the numeral instead — requiring `MOVED` to be followed by a signed count — costs one shipped block, 252, whose main row writes `MOVED, and it is THIS` with no count at all. 🔵 **FOUR — AND THIS ROW CAN NEVER HAVE A POPULATION CLAIM OF `UNMOVED_POPULATION`'s SHAPE.** `version_interval` takes its FAR endpoint from `tree_versions(root)` — the tree NOW — so it can only judge the NEWEST block. Measured over the live table: 50 blocks that say `unmoved` measure 1 today, and 300's false `MOVED` measures 0 only because the tree still stands where 301 left it. A `VERSION_POPULATION` written like its neighbour would print about twenty-one false refusals on its first run. The claim this row can honestly buy is the one at the CLOSE, against the block being written, where the tree is the right endpoint — which is also why 300's false sentence is not retrospectively refusable and stays as published. 🟢 **WHAT IS LEFT IS MECHANICAL AND NAMED**: a discriminator for the two `MOVED` aliases, with its cost paid either in 252's row or in a label exemption; a `version.moved` roster row whose word asserts NOT-ZERO rather than 243's literal zero; a `check_header` branch onto `version_interval`; and the `{SIG:}` and `scope_gate` ledger rows 251 requires 🟢 **CLOSED AT 303, AND THE PRICE 302 NAMED WAS NOT THERE.** 302 left exactly one design decision — pay the discriminator for the two `MOVED` aliases in a label exemption or in 252's row — and both arms were measured before either was taken. 🔵 **THE LABEL EXEMPTION ALREADY EXISTS**: `HEADER_EXEMPT` has carried the branch row's own session number since 235, and simulating the label prefix over all 75 registered blocks returns the identical result — 0 atoms lost, 0 newly ambiguous, the same 230 atoms binding one alias and the same 38 binding the known `gh` pair. 🔵 **AND 252's ROW COSTS NOTHING EITHER**, because its `MOVED, and it is THIS` is already read by nothing: the words sit three spaces off the SHA, so they are their own atom, that atom carries no numeral, and no zero-arity alias claims it. There was no trade-off to make. 🟢 **SO BOTH ALIASES ARE ANCHORED TO THEIR ROW**, which is what 243 did for the UNMOVED pair and could not do here, and the numeral path now builds its cleaned text label-first exactly as the word path has since 243. `version.moved` is the new row and its word is a SIGN rather than a digit — arity -1, asserting NOT ZERO, because `MOVED` says a version string changed and does not say how many. 🔴 **SIX DIRECTIONS, EACH DRIVEN RED BEFORE IT WAS GREEN**: the two aliases partition the two rows; a numeral atom's cleaned text opens with its label; the branch label does not become a claim; every registered block that reports a move against a tree that really moved is ACCEPTED (21 of them, 243 §3.2's rule that a reader refusing everything passes every claim about its refusals); a block claiming a move on a tree where nothing moved is refused BY NAME; and the `check_header` branch is bound rather than falling through to the no-reader fallback. 🔵 **END TO END**: 302's own block with `🟢 UNMOVED` changed to `🔴 MOVED` read 3 atoms and no opinion before, and reads 4 atoms and *version.moved — the block says this row MOVED and the tree says nothing moved* after. 🟢 **AND THE DEBT TABLE SHRANK BY THE ONLY MECHANISM IT ALLOWS**: `POPULATION_SHAPE_DEBT`'s *block 268 carries 1 header atom(s) against a floor of 2* cleared without 268 being touched, because the atom was always in the document and the missing thing was the reader — 269's own distinction between a gap in this file and a fact about a document nobody may edit, used for the second time. 🔴 **WHAT IT STILL CANNOT DO, AND SAYS SO IN ITS OWN COMMENT**: there is no `VERSION_POPULATION`. `version_interval` takes its far endpoint from the tree NOW, so 57 of the 75 registered blocks measure 2 today; the claim is only true against the block being written, which is also why 300's false sentence stands as published rather than being retrospectively refusable |
| tabletop-creators-leave-the-caller-elsewhere | DONE | user | host/src/tools/tabletop.ts;addons/breakpoint_mcp/error_remedies.gd | 298 | 299 | — | The four tools that build a card, piece or board scene move the editor to the file they just wrote, and 298 made them SAY so — whether they should instead put the caller back where they were is a change to how a shipped tool behaves, and that is a decision rather than a fix 🟢 **CLOSED AT 299 — STEERED TO RESTORE.** The four creators now reopen the scene that was being edited when the call arrived, once the write is saved. `edited_scene` survives and changes job: it was the warning label, and it is now the proof, still READ from the editor rather than echoed from `path` so a restore that could not happen is visible instead of lost | 🔵 **WHAT IT DOES.** When you ask Breakpoint to build a card template, a piece, or a board, it writes a new scene file and — as a side effect of how it does that — the Godot editor switches to showing that new file. The ten other tools in the same family all act on "whatever scene is open", so the next thing you ask for lands in the template instead of the table you were building. 298 measured this against a live engine: every call answers success and the card ends up in the wrong scene. The fix shipped at 298 makes each of the four tools report which scene the editor is now on, and says so in the tool's own description and in the catalogue. **WHAT IS LEFT.** The tools still move you. The alternative is to put you back on the scene you were composing once the file is written, so the surprise never happens at all. 🔴 **WHY IT IS NOT DONE HERE.** Changing what a shipped tool DOES to the editor is a product decision, and this project's rule is that those are steered before they are built, not chosen mid-session — an author who has scripted around the current behaviour would have their sequence change meaning under them. **THE BENEFIT TO THE USER.** They stop losing work to a silent context switch, which is the single most expensive kind of bug in an authoring tool: nothing errors, and the damage is only visible later. **THE BENEFIT TO THE PRODUCT.** The composite family is the headline reason to choose this server over a bare editor bridge, and a composite that quietly changes where you are working is the one thing that would make an author stop trusting it. 🔵 **A SECOND, CHEAPER HALF, WAITING FOR AN ADDON CUT.** The refusal a trapped caller actually meets — `bad_path`, from `error_remedies.gd` — says paths are "relative to the open scene root" and never says WHICH scene that is, so a reader follows it and is confirmed in the wrong belief. One sentence fixes it, and it lives addon-side, so it costs an addon version bump and an Asset Library submission. Fold it into the next addon cut rather than spending one on it 🟢 **CLOSED AT 299.** Steered rather than decided in-session, which is the rule this row was opened to honour. The argument that settled it: 298's merge was still untagged behind the release backlog, so the warning label and its removal ship in the SAME cut and no caller ever saw the surface promise a movement it no longer makes — the usual cost of reversing a documented contract was not payable here, and will be next time. 🔴 **The restore is best effort and deliberately not a failure path**: the file is written by the time it runs, so throwing would report a create that SUCCEEDED as an error and invite a caller to write it twice. 🟢 **The probe's choreography became the claim.** The unasserted `scene_open` between the creators and the in-scene work — the step 298 named as choreography rather than evidence — is deleted, and `TT_LIVE_RESTORED_WITHOUT_HELP` plus `TT_LIVE_TRAP_CLOSED` assert what it used to paper over: the trap sequence now runs with nothing in between and the card lands in the table. 🟢 **And the probe was moved onto the default surface** by deleting its `BREAKPOINT_PRIVILEGED_GROUPS` pin, which is 298's own finding paid: none of the fourteen tools is capability-tagged, so the pin bought nothing and cost the blindness that hid this. 🔵 **The addon half was reassessed rather than carried.** `bad_path`'s *relative to the open scene root* was dangerous because the open scene was not the caller's; now that it is, the sentence is terse rather than misleading, so it is a wording improvement to fold into the next addon cut and not a defect worth an addon version bump and an Asset Library submission of its own |
| replay-fence-reads-text-not-commands | OPEN | internal | scripts/handoff_gate.py;.github/workflows/sdk-drift.yml | 309 | — | — | Before a session may publish its results, one check reads the project's automation files and confirms that every tool the automated build runs is also named in the session's own replay list, so nobody can quietly add a build step that no session ever runs by hand. It decides what counts as a tool being run by looking for a tool's file name beside a flag anywhere in the text of those files, which means a sentence that merely mentions a command — such as the message a person is shown when a step fails — is counted as a command being run, and the check refuses the push. This is worth doing because it has already cost a session an extra round of work over a message that same session had written hours earlier, and because the only way to keep the check quiet today is to write worse messages for the people who have to read them. The benefit to the user is none directly. The benefit to the product is that a failure message can tell a person what to do without a gate mistaking the advice for an action, and the check keeps its authority over the thing it is actually meant to police | 🔴 **308 §3.4 — THE CLOSE GATE REFUSED ON A STRING THAT SESSION HAD WRITTEN THREE HOURS EARLIER.** `REPLAY_FLAG_MISSING` read an `echo` of an `::error` annotation out of `sdk-drift.yml` as an invocation the replay never runs. It is not an invocation — it is the sentence a person reads when the sweep returns a code nobody declared. 🔵 **THE READER IS DOING WHAT 241 BUILT IT TO DO, ONE RESOLUTION FINER THAN IT CAN SEE**: the comparison is on gate BASENAMES over workflow TEXT, so any string carrying a gate's name beside a flag is a flagged invocation whether or not anything runs it. 🟢 **308 FIXED IT AT THE MESSAGE, NOT AT THE READER AND NOT WITH AN EXEMPTION ROW**, and left the blind spot open on purpose: an exemption would have recorded a workaround for a reader defect. 🔵 **THE REPAIR IS THE SAME AST-SHAPED QUESTION 308 §2.2 ANSWERED TWICE** — `analyze` and `populationSections` both stopped being fooled by content inside string literals by asking the parser instead of the text; this reader is the third of that family and the only one still asking the text. 🔴 **AND IT IS THE ONE WHERE THE FOOLING TEXT IS WRITTEN FOR PEOPLE** (308 §5.9), which is why its workaround has a cost the other two did not: it makes the copy worse |

## What the gate refuses

| claim | refuses |
|---|---|
| `QUEUE_PARSE` | a row that does not parse, a duplicate or non-slug id, a session field that is not an integer, a table under `QUEUE_ROW_FLOOR` |
| `QUEUE_FIELDS` | a state missing the field it requires, or carrying one it must not — an `OPEN` row with a `closed` session is a contradiction, not a note |
| `QUEUE_AGE_DOMAIN` | a row whose `opened` or `closed` session is AFTER the head, or whose `closed` precedes its `opened` — the bounds the derived age is computed under, checked in `parse` so `--render` and `--ages` refuse them too. `target` is exempt: a schedule is a promise about a session that has not arrived, and `QUEUE_SCHEDULE_HONOURED` is the claim with an opinion about it |
| `QUEUE_REACH_JOIN` | a row whose declared `paths` derive a different tier from the `reach` typed beside them, a declared path this repository does not track, a path no `REACH_PATHS` row tiers, or a row opened or closed at 271 or later that declares none |
| `QUEUE_REACH_UNDECLARED` | more live rows carrying `—` for `paths` than the ceiling — the grandfathered back catalogue, counted rather than excused, on a number that can only fall |
| `QUEUE_AGE_CEILING` | an `OPEN` row older than `AGE_CEILING` sessions |
| `QUEUE_SCHEDULE_HONOURED` | a `SCHEDULED` row whose `target` has passed and which is still not closed |
| `QUEUE_TIER_ORDER` | a session that **finished** an `internal` row while any `user` row was still live. It governs closing and not opening, deliberately: refusing to let a session RECORD an internal finding would not defer the finding, it would lose it |
| `QUEUE_NOT_ONLY_NEWEST` | a session that closed **internal** items and **finished** none it did not itself create — a `KILLED` row does not answer it. Scoped to the internal tier at 248: the pathology is a session inventing gate work and closing its own invention, and a `user` row cannot be that, because the tier is defined by coming from outside |

🔴 **`QUEUE_NOT_ONLY_NEWEST` IS THE ONE THAT BREAKS THE LOOP, AND ITS FIRST DRAFT WAS
SATISFIED BY THE SESSION THAT WROTE IT WITHOUT ANYBODY GAMING ANYTHING.** That draft
counted any old row closed at the head, and session 240 closed its own two inventions and
**killed five ancient items it had no intention of doing** — five decisions, no progress,
green. The caveat printed here said it was *"satisfiable by closing a trivial old row"*;
it took one session for the file to demonstrate its own warning. Only `DONE` counts now.
A kill is a decision, and `AGE_CEILING` is the row that asks for a decision; this row asks
for work finished.

It is still not a guarantee that the queue drains, and a session that wants to game it
still can by finishing something trivial. It refuses exactly one thing: a session
reporting closed queue items when nothing it finished was work it inherited. That was
true of 236 through 239 and no instrument said a word.
